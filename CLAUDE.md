# Chang Mou — Portfolio 2026

## Overview
Product designer portfolio. Static HTML/CSS/JS site (no framework). Dark mode, minimal, trendy 2026 aesthetic.

## Structure
- `index.html` — Home (hero + work grid, 6 projects, 1344px content width). Work cards can link to internal case studies (coffee/pollen/nova/homewise) or external live projects (StoryBloom → storybloom.tech/app, Credit Analysis Agent → credit-analysis-agent.vercel.app, target="_blank"). **CURRENT ORDER as of 2026-07-28 (supersedes the row-by-row history below): Homewise · PollenNav / Coffee Chat · StoryBloom / Countersign · NOVA / Mercor · New Craft Society.** Two swaps that day, both per the user, both plain HTML block moves with no rationale attached: **Coffee Chat ↔ StoryBloom** (row 2) and **NOVA ↔ Mercor** (rows 3-4 boundary). Consequences worth knowing: (a) row 2's visual rhythm flipped — StoryBloom is the full-bleed cover and Coffee Chat is a contained screenshot, so row 1's two contained plates now continue one card further before the full-bleed break; (b) **both swaps change the MOBILE stack too** (unlike the 2026-06-18 Coffee↔Countersign swap, which was desktop-only because Countersign is `--desktop-only`) — the phone order is now Homewise · PollenNav · Coffee Chat · StoryBloom · NOVA · Mercor · New Craft Society; (c) NOVA and Mercor are BOTH the documented flat-`#FFFFFF`-plate exception, so they simply traded slots and neither needed a plate change — verified both still render `rgb(255,255,255)` with no `.project-img--lit`. **The Mercor "promote only after its cover and destination are fixed" note below still stands** — this swap moved it 6→7, which is away from the grid opening, so it is not the promotion that note guards against. The only change that day: **Mercor moved 8 → 6**, pushing NOVA 6→7 and New Craft 7→8. Rationale: Mercor is the most role-relevant credential on the site (frontier AI lab, RLHF, 2026) and was sitting dead last, 7 of 7 on mobile. It went to **6, not row 1, on purpose** — its cover is a bare wordmark on a flat white plate, which would read as a logo slide interrupting the opening row next to Homewise's dashboard screenshot. Position 6 puts it mid-grid (5 of 7 on mobile) without asking a weak cover to carry row 1. **Promote it further only after its cover and destination are fixed** (it still links to mercor.com, a third party's page with none of Chang's work — the documented unsolved issue). **Two swaps were considered and deliberately NOT made**, because both are settled decisions and re-doing them is churn: Coffee Chat ↔ Countersign (done 2026-06-18, reverted 2026-06-23) and Homewise ↔ StoryBloom (settled 2026-06-15). The organizing principle is a **quality gradient, not chronology**: rows 1-2 are the most complete stories (shipped / award-winning), row 3 is supporting work, row 4 is the in-progress product. Grid order is curated, not chronological (2026-06-11 audit): row 1 = Homewise + PollenNav, then StoryBloom + Coffee Chat (**Homewise ↔ StoryBloom swapped 2026-06-15** per the user — Homewise now leads the top-left "10-second scan" spot and the award-winning StoryBloom moved to row 2; the earlier rationale had StoryBloom + PollenNav up top for their award badges, so revisit if the badge placement matters more than leading with Homewise), then Countersign + NOVA in the last row (Countersign is the credit-analysis agent, added 2026-06-12 and retitled from "Credit Analysis Agent" to the repo's real name same day; card copy mirrors the repo thesis "the agent analyzes, a human countersigns"; mint plate #DCEFE8/#B9D6CB/#CDE3DA from its UI's teal accents; repo: github.com/chang627627/credit-analysis-agent). **Coffee Chat ↔ Countersign swapped 2026-06-18 (per user), then swapped BACK 2026-06-23 (per user):** the home grid's row 2 is now **StoryBloom + Coffee Chat** and row 3 is **Countersign + NOVA** (the 2026-06-18 change had put Countersign in row 2 / Coffee Chat in row 3; 2026-06-23 reverted it). Since Countersign is mobile-hidden, this is a desktop-only reorder — the visible mobile stack is unchanged. HTML-only, no CSS/version bump. **Countersign is also hidden on mobile** via a new `.project-card--desktop-only` class on its card + a `@media (max-width:768px){ display:none }` rule in style.css (mirrors play.html's `.play-card--desktop-only`), so the mobile stack is Homewise · PollenNav · StoryBloom · Coffee Chat · NOVA (5 cards; desktop keeps all 6). **TickerPulse was removed from the home grid 2026-06-14** and its 5 final-design wireframes moved to the Play page (carousel) — this dropped the grid from 7 cards to 6 (clean 3×2) and paired NOVA (previously alone in the last row) with Countersign; ticker.html still exists but is no longer linked from home (see its bullet). **Homewise's card gained a `Shipped` status chip 2026-06-14** (`Home Services · Dashboard · Shipped`) — user's explicit call; note it sits in tension with the case study's deliberate "prototype, not product" framing, so keep the chip unless the user revisits it. **Card image conventions:** thumbnails export at 1920px wide (dimension-matching, PNG lossless / JPEG 100 — NOT full-res exports; an 8MB thumbnail killed first-load until resized). Row-1 card images load eager; all below-fold card images get `loading="lazy" decoding="async"`. **New Craft Society added as the 7th card 2026-07-22 (per user)** — sits alone in row 4 after Countersign + NOVA, external link → newcraftsociety.com (target="_blank"), chips `Freelance · Design Community · Web App · Building` (the `Building` status chip is the user's call — the product is in active development; **type chip `Personal` → `Freelance` 2026-07-31 per user**, the first use of the Freelance label). Cover `images/newcraft-card.png` (1920px PNG) is a **full-bleed screenshot of the logged-in app home** (watercolor bg + wordmark) — the user pasted it in chat from clipboard; it was recovered from the session transcript JSONL (`~/.claude/projects/<project>/<session>.jsonl` stores pasted images as base64 — useful trick when a paste has no file). The public logged-out site shows a dark typing intro instead, so do NOT try to re-capture this cover headlessly. Card visible on mobile (mobile stack is now 6: … · NOVA · New Craft Society). Copy grounded in the site's own thesis ("captures your process as it happens", "the case study writes itself"); `api/chat.js` PORTFOLIO list got a matching entry (labeled newest, in active development). **Card outcome pass 2026-07-26 (improvement-research P1, PARTIAL per user):** Homewise's description became "AI command center for homeowners. Prototyped in code, shipped by the dev team, managing repairs from one workspace" (the prototype→product beat surfaced from the case study's Collaboration section; consistent with the honesty framing — Chang built the prototype, the dev team shipped the product) and NOVA's gained ", tested with 20+ parents and kids" (from the resume). **The suggested StoryBloom `Live`-for-`Award Winner` chip swap and Countersign `Live Demo` chip were DECLINED by the user ("do not touch others") — do not re-suggest.** Chang is also **one of the product engineers on New Craft Society** (2026-07-26, per user) — recorded in `api/chat.js` + `llms.txt`, deliberately NOT on the card (a third builds-in-code claim in one viewport reads as overclaiming; hero + Homewise card already carry it). **Mercor added as the 8th card 2026-07-23 (per user)** — pairs with New Craft Society in row 4 (desktop grid back to a clean 4×2; mobile stack 7). External link → **www.mercor.com/mission/** (target="_blank"; link iterated homepage → /mission/ → /experts/ (2026-07-23) → **back to /mission/ 2026-07-26, user's call after reconsidering**. The reasoning that won: /experts/ opens on "$119/hr average rate" + role listings + Apply buttons, which reframes the role as hourly gig work for a design-hiring reader, while /mission/ opens on the company thesis ("organizing human intelligence to power the AI economy") that makes the card copy land. The earlier "mission is too abstract" objection is real but secondary — the question is which page makes the reader think better of Chang, not which is more concrete. **Known unsolved issue (Mobbin landing benchmark move #4):** BOTH destinations send the strongest frontier-AI credential to a *third party's* marketing page containing none of Chang's work. The proper fix is pointing the card at his own content (About experience section, or a future write-up of the eval work); /mission/ is just the better of two versions of that problem), NOVA-style **flat white plate** + `images/mercor-card.png` at 55% width: the user-pasted Mercor lockup, extracted from the session transcript, trimmed, and **white keyed to transparent alpha** (the paste was JPEG; an opaque white rectangle gets outlined by the `.project-img > img` drop-shadow filter against the white plate — same reason NOVA's plate needs a transparent PNG). Copy from the resume's Mercor role (Product Design Expert, May 2026–Present, kept lab-anonymous like the resume), currently "Expert UX evaluation for a frontier AI lab. Reviewing AI generated interfaces and turning design critique into feedback that teaches models product taste" (copy iterated 2026-07-23: resume-length → shortened per user → **lengthened again per user**, landing on this mid-length version; the middle clause names the day-to-day work the short cut left implicit, and it still fits 2 lines like all 8 cards. The interim "a leading frontier AI lab" was trimmed — user flagged "leading frontier" as redundant, since "frontier" already means the leading edge); chips `Professional · Frontier AI · RLHF · UX Evaluation` (**RLHF chip added 2026-07-23 per user** — user first floated saying "reinforcement learning by human loop" in the copy; corrected to RLHF, and the signal went into the chip row instead of the sentence so the description keeps its plain design language while the chips carry the technical category. Defensible because Mercor's own resources frame expert evaluation as the human-preference input RLHF runs on — mercor.com/resources/experts/what-is-rlhf). The chat prompt's resume section already lists the Mercor role (no chat.js change).
- `about.html` — About (vertical layout: hero → photo deck → background → experience → education → awards → skills). The photo deck is an infinite drag-to-rotate carousel on desktop, a static 4×2 grid on mobile (see **About photo deck** below). **Polish pass (2026-06-20):** Experience, Education, and Recognition all share one aligned 3-column format via the `.experience-list` CSS **subgrid** (`grid-template-columns: auto minmax(0,1fr) auto`; each `<li>` is `grid-template-columns: subgrid`, `.exp-row` is `display:contents`, so the primary/secondary/date columns align *across* rows; each list sizes its primary column to its own longest entry; mobile reflows to primary+date on the top line, secondary below). **Recognition was converted** from a bulleted `.recognition-list` text list into this same `.experience-list` format (per user "same format like experience and education") — award name white, status grey (Winner / Silver Winner / Featured / Exhibited at Ferry Building / **Recipient** for the scholarships), date grey-right; the interim `.recognition-list li`/`.rec-detail` comma styling was removed. **Skills + Tools now sit side by side** on desktop via a `.about-cols` 2-col grid wrapper (stacks on mobile). **Books + Podcasts kept full-width on purpose** — side-by-side shrinks the covers below legible (~78px), so they keep the full width. Chips (`.skill-list`) were left as-is (already clean bordered pills). style.css bumped **v=174→v=178** across all 8 pages. **Reworked 2026-06-20 (this session — SUPERSEDES the Polish-pass "Skills + Tools side by side" and "aligned 3-column subgrid" notes above):** (1) **Skills + Tools sections DELETED** entirely (per user); the `.about-cols` 2-col wrapper + `.skill-list` chip CSS are now **unused** (left in style.css, harmless). Do NOT re-add Skills/Tools unless asked. (2) **Experience / Education / Recognition converted from the 3-col `.experience-list` subgrid to an inline "name · detail · date" row (the "B1" layout).** *Why:* the three lists were *separate* subgrids, each sizing its first column to its own longest entry, so the middle column started at a different x per section (268 / 346 / 403px) — a cross-section zigzag the user flagged as "messy". Now `.experience-list` is `display:block`, each `li` is flex: `.exp-role` white, `.exp-company` muted with a `::before` middot separator ("Product Designer · UNICEF"), `.exp-date` right via `margin-left:auto`. Only the left edge + right-aligned dates align across sections — no middle column to drift. **Mobile keeps the stacked layout** (role+date line, company below) and hides the middot via `.exp-company::before{content:none}`. Do NOT restore the subgrid (it caused the zigzag). (3) **Testimonials MOVED above Books/Podcasts** — order is now Background → Experience → Education → Recognition → Testimonials → Books → Podcasts (social proof was buried last); `.testimonials` bottom padding dropped 200px→96px (no longer last). (4) **Footer gap:** the last section (Podcasts) carries the case-study-standard 200px content→footer gap (80px mobile) via a new `.media-section--end` modifier on its `<section>` (mirrors `.project-content`'s 200px). (5) **Mid-tier body grey:** new `--text-soft:#a0a0a0` token applied to long-read text (`.about-block p` bio + `.testimonial-quote`); dates/metadata stay `--text-muted #888` (contrast steps white → #a0a0a0 → #888). (6) **`images/food.jpeg` recompressed 868KB→156KB** (q90, kept 600×800) — it was the photo deck's lone heavyweight (siblings 88–164KB). A dashboard/command-center About layout (left tab rail + one-panel-at-a-time content) was prototyped then fully reverted the same session (user: "back to the previous github version"). style.css bumped **v=178→v=184** across all 8 pages. **Books + Podcasts hidden on mobile (2026-06-20, per user):** `@media (max-width:768px){ .media-section{display:none} }` (`.media-section` is About-only) — Testimonials becomes the last mobile section and its 80px bottom padding carries the footer gap (the mobile `.media-section--end` override was dropped as redundant; the 200px desktop `--end` gap stays). Desktop still shows both. style.css **v=184→v=185** across all 8 pages. **"How I use AI" section (added 2026-08-04, per user "this is a question a lot of job application forms ask so why not just answer the question here"; between Background and Experience).** One prose paragraph over a **three-stage `.ai-flow` diagram**: Research (Perplexity, Granola) → Brainstorm (ChatGPT, Claude) → Prototype (Claude Code, Figma Make), arrows between, each card = label + one-line description + its tools with a white monochrome mark. **High level by instruction: NO case-study names anywhere in it, and Mercor is excluded entirely (NDA).** Copy shaped by a 4-agent research sweep (40 sourced findings on how designers at Figma/Anthropic/Vercel/Linear answer this, portfolio "how I use AI" essays, hiring-manager expectations, published AI-workflow taxonomies): the opener **names a failure mode** ("AI's first pass drifts toward generic, so it never gets the last word") because the research flagged "AI is in every loop of my process" as the sentence ~72% of designers could also write, and **"deciding what ships" carries the evaluation beat** the hiring corpus screens for hardest. **A fourth EVALUATE stage was the research's #1 recommendation and was REJECTED by the user** ("of course I evaluate it so it is meaningless") — the beat lives in prose instead; do NOT re-propose the card. A longer Prototype caption ("since you cannot mock every state of a model-driven product") was also tried and reverted to **"Turning the strongest direction into a working build to validate it"** — it broke the gerund rhythm and the even 214px card heights. **Card styling is the content-card family EXACTLY, verified computed-identical to `.testimonial-card` on the same page: `var(--surface)`, `var(--radius)`, `padding: 32px`, and NO border** (content cards are borderless; only chrome cards like `.project-card` carry a stroke), `gap: 24px` (matches `.impact-grid`/`.voc-quotes`), and the canonical **32px prose-to-component margin** (`.project-content` sets it on 16 components). First build shipped at 20px/20px/14px with a border and the user caught it — **when adding any new card component, copy the four values off `.problem-card`/`.impact-card`/`.testimonial-card` rather than eyeballing.** Mobile stacks at **16px, not 24px**, because the arrow rotates into that gap and 24+arrow+24 reads as a section break. Stage descriptions are `div.ai-stage-desc`, NOT `<p>` — the bio-illumination splitter targets `.about-block p` and card copy must stay static. **Tool marks** live in `images/ai-*`: four official vector glyphs from the LobeHub icon set recolored to `#F0F0F0`, plus **`ai-granola.png`, a silhouette extracted from Granola's favicon glyph** — no Granola vector exists on simpleicons, LobeHub, or their own site, so regenerate it that way if needed. `api/chat.js` (new `HOW I USE AI` prompt block) and `llms.txt` carry the same answer — **update all three together**. style.css **v=227→v=228** across all 9 pages. **Bio scroll illumination (2026-08-04, per user, from the 20-site reference sweep — the most repeated pattern in it: youngkim.design / ghalibo.com / anuhyakanukuntla.design / suepark.xyz all run a version):** the 3 Background paragraphs brighten from `--text-soft` toward `--text` as they rise past the lower viewport — scrubbed, fully reversible, line-by-line. **ONE surface by explicit agreement: the bio ONLY, never case-study body copy** (dim-until-reached text between a hiring reader and the argument is friction; none of the four reference sites scrub body copy either). The one sanctioned future extension, NOT built: each case study's opening paragraph — a separate user decision. Mechanics: an about.html IIFE splits the paragraphs into 122 `.bio-word` spans (spaces stay bare text nodes so wrapping behaves as plain text — hero precedent; an element-children guard skips any paragraph that later gains a link/highlight span rather than mangling it), and style.css animates `.bio-word` only — **inert on the other 8 pages since nothing else ever creates the class**; JS-fails → static soft text, same graceful direction as card-rise. Each span carries its own **anonymous `view()` timeline** (inline-block so the spans generate real boxes; same-line words share identical progress, so the sweep reads line-by-line with zero delay bookkeeping; verified no ancestor of `.about-block p` is a scroll container, so the `--card-view` trap does not apply). Longhands + `animation-duration: auto` per the card-rise trap; gated `@supports (animation-timeline: view())` + `prefers-reduced-motion` with mirrored JS guards. **Range is `cover 18% cover 45%`, tuned by measurement — a `cover 0%` start shipped first and was INVISIBLE:** it spread each word's ramp over ~460px of scroll, blurring the whole bio into one near-flat gradient (the documented "too subliminal to earn its place" failure mode); at 18% the ramp ≈ the bio block's own height and the sweep is real (measured 192/185/168/160 front-to-back mid-sweep, byte-identical on the reverse pass). **NEW probe gotcha, cost a false negative: scroll-driven animation styles do NOT update synchronously after `scrollTo()` — a same-task `getComputedStyle` reads stale values and the effect looks dead; probes must wait a double rAF between scroll and read** (same family as rAF-not-while-hidden). Consequence, accepted: on this surface `--text-soft` now means "not yet read" — text rests white once passed, which deliberately softens the 2026-06-20 mid-tier-grey decision here. Verified: scope exactly the 3 bio paragraphs (testimonials/media untouched), mobile 375 clean with the same gradient, no overflow, no console errors. style.css **v=225→v=226** across all 9 pages.
- `play.html` — Play (2-column masonry with card wrappers, lightbox on click). **TickerPulse carousel added 2026-06-14** — the final-design wireframes relocated from the home grid, built with the shared `[data-carousel]` pattern (identical markup to Whisper XR; the init JS at the bottom auto-wires any new `[data-carousel]`, so no JS change was needed), placed in column 2 right after Whisper XR. Filenames carry spaces — fine, the browser encodes to `%20` and the python preview server serves them. **Same day, refined per user:** the sign-in screen (`Wireframe - 18.jpg`) was dropped so the carousel now holds 4 slides (`Wireframe - 19..22.jpg`, all 1920×1258) leading with the **dashboard** (`active`), and the copy was rewritten to describe the **product itself**. Card copy: "An AI trading terminal that monitors real-time news sentiment and explains the why behind every alert, not just the what. Built for active and part-time traders, it filters market noise down to portfolio-relevant moves and backs every verdict with linked evidence, so they can act on the catalyst before the move is over." (Grounded in ticker.html's Problem/Research/Solution — the "why now", portfolio-relevance, linked-evidence, and timeliness beats. **Copy history, both rejected by the user — do not reintroduce:** "Five core screens, from sign in to trade" (too screen-tour) and a Figma-Make / "vibe coding / taste gating" build-method angle (user wanted the project, not the method). Keep the description product-focused.) **"About the Teddy Bear" card added 2026-06-22 (per user)** below "I Believe in a Thing Called Love": a **2020** student 3D animated short made in Maya, standard video-card markup with a YouTube embed (`https://www.youtube.com/embed/zdQ4LYluWnc`). Intro was rewritten from the user's draft to the house voice (noun-phrase+tool opener mirroring UN/FOLD, concrete role verb "I shaped the scenes and motion", show-don't-tell on the abandonment theme, dropped "passionate"). Year **2020 user-confirmed** — the upload's own title is just the course filename "343 final" with no usable date, so the year can't be derived from the video.
- `coffee.html` — Coffee Chat case study. **Design System reframed as an obstacle beat 2026-08-04 (per user, from a cindyquach.xyz benchmark; HTML-only, no version bump).** Both her case studies stage a named thing-that-went-wrong (a mid-project security policy shift; another team's feature making her work obsolete), and it is the highest-signal move on her site. Coffee was the only one of the four studies with real unstaged material: the rebrand. The h2 went from **"Building the atomic foundations of the rebrand"** (a task) to **"The identity was changing under the feature"** (a claim), and two sentences now state the bind before the resolution: nothing settled to design on, the retiring system would ship stale on day one, the replacement did not exist yet. **The point is that the design-system screenshot below it now reads as the answer to a stated problem instead of a side project.** Reflection kept the lesson but dropped its green-to-blue restatement, landing on the deck's own phrasing ("build for the one that was coming, not the one being retired") so page and deck agree. **Gotcha that cost a rewrite: the first draft opened by restating the rebrand AND the two weeks, both of which Goals-and-constraints already states verbatim 300 lines earlier** — the fix references the established constraint instead ("With the identity mid-swap…"). Verified "green", "blue" and the two-week span each appear exactly once in prose. **No 11th section** — this is a reframe of the existing Design System section, per the settled 10-section spine. **NOVA was assessed and deliberately NOT given one: it already has the beat** (Challenge 2, "Some parents wanted full tracking. The children would not wear the glasses if they got it.") and it is stronger than hers, since it comes from a real user-research conflict rather than an administrative one. Homewise and PollenNav have no honest obstacle material and were left alone. **`coffeeslide.html:1025` still carries the OLD heading and paragraph** — the two were deliberately synced on 2026-07-28, so page and deck are now divergent; sync it or decide the divergence is intended.
- `coffeeslide.html` — **Slide-deck version of the Coffee Chat case study** (added 2026-07-17, per user "make coffee chat study case as slide deck under /coffeeslide"). Clean URL `/coffeeslide` via a `vercel.json` rewrite (added alongside `/nova`, `/homewise`). **Fixed 1920×1080 presentation slides** (user explicitly asked for this size): a `.stage` (1920×1080) is scaled to fit the viewport (letterboxed) via a JS `fit()` that sets `--scale = min(vw/1920, vh/1080)`; **all inner sizing is fixed px** (NOT vw/vh/clamp) so the whole stage scales uniformly and looks identical everywhere. **19 slides** following coffee.html's section order: Title → Overview → Impact (63%/200+/$4.5M) → Problem → Research (4 tool tiles + insight) → Design Goals → Constraints → Exploration 1 (pairing card, split layout) → Exploration 2 (before/after flow tag-cards, NOT the full SVG — deck digest) → Iteration 1 → Iteration 2 → **Final Design (5 slides, one screen per case-study deliverable in order: Seamless scheduling · Handling declines · Flexible scheduling · Review proposed times · Propose your own availability, labeled "Final Design · N of 5")** → Design System → Reflection → Closing (recap + CTAs to coffee.html / index / mailto). **Final Design expanded from 2 condensed slides → the full 5 (2026-07-17, per user "a final design omits a lot of things, refer to the case study order"): the first cut collapsed the 5 deliverable screens into scheduling + a merged reschedule/declines slide and reordered them; now each screen is its own slide in coffee.html's exact order (coffeefinal1 · handling-declines · Flexible1 · Flexible2 · Flexible3). Em dashes in body copy were also cleaned to periods/commas per [[feedback]] (page-title em dashes kept, matching the site convention).** **Self-contained** — inline `<style>`+`<script>`, does NOT load style.css (so no version bump), but mirrors the site tokens (#0a0a0a/#1a1a1a/Neue Montreal, warm→blue title+closing gradients like hero g2, `#D0D1FD` lavender app-screen plates from coffee.html). Nav: `.deck-track` translateX(-index*1920px) inside the scaled stage; **keyboard** (←/→/space/PageUp-Down/Home/End), **click** (prev/next circle btns), **swipe** (touch), and **`#N` hash deep-linking** (1-based, e.g. `/coffeeslide#8`; syncs via `history.replaceState`). Chrome (top progress bar in the warm→blue gradient, brand top-left→index.html, `NN / 16` counter, hint) is viewport-fixed (unscaled). **Images are EAGER (no `loading="lazy"`)** — deliberate: `loading="lazy"` computes intersection against the *scroll* viewport, but slides move via `transform`, so off-screen lazy images never reliably load and showed **blank plates mid-deck** (hit on the Iteration-1 "After" plate during build). Slide 1 is text+gradient only so first paint stays instant while the ~13 screenshots (incl. coffeefinal1 3MB, coffeedesignsystem 2MB) stream in behind it. `prefers-reduced-motion` drops the slide/reveal transitions; `<noscript>` points to coffee.html. **NOT linked from the home grid or coffee.html** — standalone at `/coffeeslide` (reachable by URL), like ticker.html. Per-slide content is designed to fit within 1080px (verified in preview at 16:9 — no clipping on any slide, incl. the tall image/split/before-after slides). **Design/spacing pass 2026-07-17 (per user "check the spacing consistency, let slide use site's design system, make it more visual"):** (1) **Spacing scale** — replaced ad-hoc margins with one rhythm adapted from style.css and scaled for the 1920 canvas: label →20px→ title →24px→ body →48px→ media block; grid gaps 24px (matches `.project-grid`). (2) **Site design system** — `.s-label` now carries the **`.section-meta` hairline-rule** treatment (`LABEL ————`, flex + `::after` 1px rule) like the home-page section headers; radius 14→12px. (3) **More visual** — single-image slides (all 5 Final Design + Design System) now let the plate FILL the space below a top-anchored header via `.slide-inner > .plate:last-child { flex:1; min-height:0 }` + `img{max-height:100%}` (bigger, dominant images; also structurally prevents title-clipping since the header is pinned to the top, not vertically centered). Card/prose slides stay vertically centered (`.slide-inner{justify-content:center}`) — a `:has()` rule to top-anchor ALL slides Keynote-style was tried and reverted because it left sparse slides like Impact bottom-empty; centered balances short content better while the plate-fill naturally top-anchors image slides. All CSS-only (self-contained page, no style.css/version bump). **Closing slide DELETED 2026-07-17 (per user "feels repetitive with slide 3"):** the old slide 19 recapped the same 63%/200+/$4.5M stats as Impact plus exit CTAs; the whole section was removed (deck now **18 slides**, ends on Reflection; `.slide--end`/`.end-stats`/`.cta-row` CSS left unused in the inline style block, harmless). Do NOT re-add a stats-recap close; a minimal stat-free sign-off is the only acceptable form if a closing returns. **Figma-style COMMENT MODE added 2026-07-17 (per user "add the comment mode to the slide like figma"):** a speech-bubble `.navbtn--comment` (right:150px, next to prev/next, count badge) or the `C` key toggles `body.commenting`; in that mode each slide's `.pin-layer` (absolutely-inset div appended by JS into every `.slide`, `position:relative` added to `.slide` for it) captures clicks — click anywhere drops a draft popover (name + note, name remembered via localStorage `coffeeslide_commenter`), Post renders a numbered **teardrop pin** (`border-radius:17px 17px 17px 3px`, Figma-style tail at the anchor point). Pins/popovers live INSIDE the scaled stage so they ride the track transform, are anchored via a 0×0 `.pin-anchor` at %-coords of the 1920×1080 canvas, and **inverse-scale** (`transform:scale(calc(1/var(--scale)))` about the anchor corner) so they render constant screen size at any viewport. Pins are visible ONLY in comment mode (like Figma). Clicking a pin opens a view popover (avatar initial, name, date, Delete). **Comments are SHARED as of 2026-08-11 (per user "I want everyone can comment") — SUPERSEDES the per-browser-localStorage note that used to live here.** `api/comments.js` (GET/POST/DELETE, origin-allowlist + 60/5min rate limit, deck whitelist coffeeslide/homewiseslide) over the **PRIVATE `deck-comments` Vercel Blob store** (**ONE IMMUTABLE BLOB PER COMMENT at `comments/<deck>/<id>.json` — v2, same day: the v1 one-JSON-array-per-deck design was replaced because Blob's overwrite cache/consistency window served the FUNCTION's reads stale for minutes cross-region, resurfacing deleted pins and hiding fresh posts even with `cacheControlMaxAge: 0` + `?ts=` busting; immutable objects sidestep that layer entirely and kill the concurrent-post race, since posts create objects, deletes remove them, and nothing ever overwrites**), **provisioned via CLI with `npx vercel blob create-store deck-comments --access private --yes`** — no dashboard needed; BLOB_READ_WRITE_TOKEN landed in project env automatically. **This added the repo's first root `package.json`** (@vercel/blob dependency; no build script, so Vercel still serves the root statically — verified all pages 200 post-deploy). Both decks' pin engines fetch the shared store on load and on entering comment mode, POST/DELETE through it, and **lift any localStorage-era pins into the store once** (`<deck>slide_comments_v1_migrated` flag); if the API is unreachable (LOCAL PREVIEW — /api/* 404s there) the engine falls back to the original per-browser behavior, verified identical. **Deletion is OPEN as of 2026-08-11 (per user: trusted audience, "let everyone delete each other's comment") — the Delete button renders on every pin and DELETE checks nothing but deck+id.** The per-browser author key + GET's computed `own` flag still exist (harmless, kept for a future tightening); the original owner-only enforcement was built and verified first, then relaxed the same day. The mailto pill stays. **Blob gotchas, all hit: the private-store PUT access header is `x-vercel-blob-access` (raw REST is version-pinned pain — use the SDK), and NEVER build overwrite-based storage on Blob (Vercel's own docs say it is not a database; local same-region reads look deterministically fresh while deployed function reads are not, so the failure only shows in production). If a real datastore is ever wanted, `npx vercel install upstash/upstash-kv` gets to the finish line but requires the ACCOUNT OWNER to accept Upstash's terms in a browser — an agent cannot complete it.** Deploys of api/ functions take ~2-5 min to fully serve; a verification loop right after push needs generous retries or it tests the OLD function (this cost two false alarms). The top .cmt-bar pill carries a **"Send N notes to Chang ↗" mailto** that packages all notes to changmou627627@gmail.com. Guards: the deck's keydown handler early-returns for input/textarea targets (typing space/arrows must not drive slides), Esc closes popover then exits mode, swipe-nav is disabled while commenting, and the whole comment chrome is hidden ≤640px (pins would be untappable at phone scale). Custom SVG comment cursor via data-URI on the layer. **Fable-5 polish pass 2026-07-17 (per user "make the slides more polished by fable 5" + "use portfolio-audit as checker"; SUPERSEDES the 18-slide count and parts of the earlier spacing-pass notes):** ran a 5-agent workflow (4 audit lenses over screenshots of every slide + a high-effort judge, all `model:'fable'`) against `~/.claude/skills/portfolio-audit/SKILL.md`; the judge returned 12 prioritized changes and rejected 10 findings (incl. per its own check: no entrance-animation rework, no wholesale retitling, no chrome-type normalization). Implemented: (1) **header anchor** — `.slide:not(.slide--title):not(.slide--end){padding-top:150px}` + `justify-content:flex-start`, so the label/title sit at one canvas y on every content slide (they used to lurch y~84→400 between slides; the loudest "assembled, not crafted" tell). Title + sign-off slides stay centered bookends. Companion: `.split .plate img` max-height 660→600. (2) **chrome lane** — `fit()` now scales to `(innerHeight - 76)/1080` so the fixed brand row/nav circles can never overlap stage content. (3) **slide 2 Overview** now shows real UI (`images/coffeechat-card.png` in a `.split`, 2nd redundant paragraph deleted) — work is visible by frame 2. (4) **plate shrink-wrap** — `.slide-inner > .plate:last-child` adds `align-self:center; width:fit-content; max-width:100%; padding:56px 72px; margin-top:32px` so the lavender mat is symmetric instead of a 9:1 side-mat. (5) **callout → 3-col grid** (was left-bunched flex). (6) **sign-off slide 19 re-added** per the judge (the deck died on grey paragraphs and coffee.html was only linked in noscript): `.slide--end` with THANK YOU label / "See it in full" / `.cta-row` (case study + Say hello) — **stat-free, complying with the no-stats-recap rule**; `.end-stats` CSS deleted, `.cta-row` gained margin-top:48px. Deck is **19 slides** again. (7) **token pass** — deck `--accent` #0094ff→**#1e78ff** (the deck's own gradient blue; #0094FF stays NOVA-only per the site blue-scope rule), `.insight` rgba follows; card-tier text unified at 20px (flowcard li 22→20, tool-con/co-lbl 19→20). (8) **progress bar** paints the full warm→blue gradient and reveals via `clip-path` inset (JS sets clipPath, not width) so it's no longer a squashed sliver early on. (9) **Final Design copy normalized to gerund voice** ≤2 lines each ("Scheduling with…", "Rescheduling from recommended slots", "Reviewing the other person's proposed times", "Proposing your own availability"); Design System title "A design system for the new identity". (10) **micro-copy fixes**: title-sub → "AI-matched pairings that take the social pressure out of meeting hybrid teammates."; slide 9 title → "Remove the messaging, remove the drop-off" (+ "Automated pairing and scheduling" ✓ item); "Won the internal A/B test." grammar fix **applied to coffee.html's matching caption too**; Watercooler Trivia full name; "Cross-department reach"; Impact stat de-hedging; Reflection tightened ("design for the identity that was coming, not the one being retired"). (11) `.ba figcaption` is now flex (tag + span) so wrapped caption lines align past the pill; slide 8's GPT-3 paragraph became a third `.note` ("Powered by GPT-3") matching its siblings. (12) **image fixes**: `images/twowidgets.png` stray light-gray export artifact (104×128px block at x66/y3116) erased in place (PNG lossless — also benefits coffee.html); **`images/coffee-pairing-card-slide.jpg` created (deck-only)** — the original had ~740px of dead white inside the card, so it was cut at content+75px and the original bottom-40px border strip (rounded corners) stitched back on, seam invisible; **coffee.html keeps the original file** (the judge said overwrite same filenames, deviated deliberately because Flexible2/3 + pairing-card are shared with coffee.html). **NOT done from the judge's plan (need real assets/eyes):** P4 widget-level crops of Flexible2/3 for slides 15-16 (the three full-app Final Design shots read near-identical; crops must be new deck-only files, NOT overwrites), P11(b) redrawing iteration2-before.png's orange annotation, P11(d) replacing tool-gather/focusmate screenshot thumbs with real app-icon marks. All verified in preview (slides 2/3/7/8/12/19 recaptured). **Mentor-framework restructure 2026-07-28 (deck now 19 slides — SUPERSEDES the 20-slide count below):** brought in line with the homewise deck and with the same-day coffee.html pass. (1) **Slide 2 is now "The bet"** — states the PMF question ("the question that decides the feature is not whether we can match people, it is whether they will say yes") and **absorbs the old Impact slide's 63%/200+/$4.5M stat row**, which is deleted as a standalone. The $4.5M stays deck-only, never on the page. (2) **Problem cards replaced with the three hardnesses** (Nobody wants to go first / A good pairing has to be inferred / Scheduling has two sides), mirroring coffee.html; title became "Three things made this hard to design". (3) **Design Goals slide DELETED and merged into Constraints as "Goals and constraints"** — the 4 goal cards were a table of contents for the slides that follow and "Cross-department reach" promised a feature the work never delivers; the constraint body now carries the white-highlighted removal license ("and anything that added friction had to go"). (4) **The four numbered labels are gone** — `Exploration · Challenge 1/2` and `Iteration · Challenge 1/2` became `Meaningful pairings` / `The user flow` / `What testing changed` ×2, per the recorded no-numbered-section-labels rule the deck had never been swept for. **`Final Design · N of 5` deliberately stays** — that is a progress indicator inside one beat, not process-volume section numbering. (5) **Hello moved from slide 2 to slide 18** (same reasoning as the homewise deck: in an interview you have already been introduced, so it was spending the first minute before any stakes). **Companion fix on BOTH files:** the coffee.html label rename left three eyebrow/heading pairs saying the same thing twice (`Meaningful pairings`/"Creating meaningful pairings", `The user flow`/"Mapping the user flow", `Design System`/"New identity design system"). The eyebrows are fixed (they mirror the side-nav), so the **h2s became claims**: "Explain the match, and people accept it", "Remove the messaging, remove the drop-off", "Building the atomic foundations of the rebrand". **Lesson: after renaming a section label, diff it against its own h2** — a rename that matches the heading creates redundancy rather than clarity. **Hello slide moved BACK to position 2 on coffeeslide 2026-08-03 (per user "self intro at the beginning") — this REVERSES the 2026-07-28 mentor-framework move that sent it to the penultimate slot.** The recorded rationale for putting it late ("in an interview you have already been introduced, so it spends the first minute before any stakes") was raised once and overruled; treat slide 2 as the settled position for this deck and do not move it again unprompted. Spine is now Title · Hello · The bet · … · Reflection · Sign-off. **Homewiseslide followed 2026-08-03 (per user "move the self intro to the beginning"), so BOTH decks now open Title · Hello · The bet and the asymmetry is resolved.** Homewise's Hello moved 18 → 2, everything between shifted down one (Reflection 17→18, Sign-off 18→19; still 19 slides), and the clip slides moved with it (Final design 10-13→11-14, Design system 15→16) — the JS keys off `slides.length` and DOM position, so nothing needed updating, but **its `homewiseslide_comments_v1` pins are keyed by slide INDEX and now point one slide earlier than intended.** Verified after the move: folio 02/19 on Hello, no overflow, and the Design-system clip still auto-plays. **User-flow diagrams RESTORED, deck is now 20 slides 2026-08-03 (per user "why this slide do not have the diagram?"):** the original build made this beat a "deck digest" — it kept the ✕/✓ tags from coffee.html's two flow charts and dropped the SVGs, leaving the sparsest slide in the deck (title + body + two bullet lists, no artifact) under a heading that asserts "remove the messaging, remove the drop-off" without showing it. **Slide 8 was split into two: 8 "The user flow — before" (Existing tools, 6 boxes) and 9 "The user flow — after" (Our Coffee Chat flow, 9 boxes incl. the decline/reschedule fallbacks)**, each carrying its own chart at full width; everything downstream shifted by one (Sign-off is now 20) and the slide-number comments were renumbered. `const total = slides.length` and the JS-injected folio pick the new count up automatically — **there is no hardcoded total to update**. The two `<svg class="flow-svg">` blocks were copied verbatim from coffee.html and their `.flow-svg`/`.flow-box` CSS inlined in the deck (which does not load style.css); the `arrow1`/`arrow2` marker ids do not collide. **Sizing is the interesting part and the reason it works:** `.flow-wrap` is `flex: 1 1 0; min-height: 0` and the SVG is `height: 100%`, so **height is the constraint and width follows the ratio via preserveAspectRatio** — that one rule fits both the 960×260 and the 960×460 chart with no per-slide sizing. Two moves bought the taller chart its legibility: the viewBoxes were tightened to their real content bboxes (`0 0 960 460` → `10 30 940 420`, measured with `getBBox()`, which showed 40px of dead space at top), and both slides were converted to the deck's own 2-col `.media-head` (title left, annotation right) — the pattern slides 12-17 already use precisely to stop a paragraph eating the artifact's height. Result: the before chart draws at 1.81× (13px viewBox text renders ~23px on canvas) and the after chart at 1.44× (~19px), up from 1.15×/~15px before the fix. **Do NOT re-collapse these into one slide** — a side-by-side pair was measured at ~0.86×, i.e. ~9px text on a laptop, which is unreadable in a room. **Consequence for comment mode:** `coffeeslide_comments_v1` keys notes by slide INDEX, so any pin previously left on slides 9-19 now points one slide earlier than intended. **Competitive-analysis screenshots RESTORED + pairing-card corners fixed 2026-08-03 (per user):** (a) the four tool screenshots deleted in the reskin are back on slide 5, reusing the existing `images/tool-{donut,watercooler,gather,focusmate}.jpg` that coffee.html's table already uses (no new assets — the user's pasted images were byte-identical to these). New `.col-shot` rule frames them uniformly: `aspect-ratio: 3/2; object-fit: cover; object-position: top left` + hairline border + 8px radius, because the four source captures are different shapes (327-530px wide at 320 tall) and only a shared frame makes the row align. **Donut's description was lengthened to "Matching is automated, but it feels robotic and repetitive if not personalized."** — at one line it sat 15px higher than its three two-line siblings and broke the screenshot row; the added clause comes from the case study's own pros column ("Fully automated matching"), so it adds information rather than padding. (b) **`coffee-pairing-card-slide.jpg` → `.png`**: the card's rounded corners had pure-black `(0,0,0)` pixels outside the 24px radius, which read as black notches on the lavender plate. Rebuilt with transparent corners — **PNG is both lossless AND smaller here (623KB vs 776KB)** because it is a flat light UI screenshot, and transparency survives any future plate-colour change. **GOTCHA: cutting at the true 24px radius leaves a dark antialiasing rim** (the JPEG's black-to-border blend); measured combinations and settled on **radius 28 + 2px erode**, the smallest adjustment that yields zero sub-120-luminance pixels within 80px of every corner. Regenerate from `git show <rev>:images/coffee-pairing-card-slide.jpg` if it is ever needed again. **deck.gallery editorial reskin 2026-07-31 (per user, the SECOND deck on the Design Decisions slide-deck grammar — SUPERSEDES this bullet's boxed-card/trailing-rule-eyebrow/gradient-progress details and parts of the fable-5 polish pass; same 19 slides, same copy, same engine/comment mode):** the deck keeps ITS OWN palette per the grammar's palette-is-per-project rule — dark `#0a0a0a` canvas, warm→blue gradient fields on the two bookends, lavender `#D0D1FD` plates, accent `#1e78ff` — and adopts the transferable grammar: (1) `.s-label` is now the full-width header row (label left, static "Coffee Chat · Case study" via `::after`; the old full-width trailing rule is gone) + JS-injected `.page-foot` folio; the fixed `.deck-counter` was **removed**. (2) The 63%/200+/$4.5M stats are hairline ROWS (100px numerals); Problem/Research/constraints/user-flow triads are rule-topped COLUMNS (`.cols`/`.col`, blue indices) — **the research tool tiles' screenshot thumbnails were DELETED** (resolves the old P11(d) "need real app-icon marks" weak point by removal) and the boxed `.insight` became an open payoff line under the columns. (3) Reece-style cover: metadata columns (Role / Company / Timeline / Type "0 to 1 feature"), 280px nowrap "Coffee Chat" wordmark + 430px sub (the sizing pair rule), "Litespace" lives in the Company column now, not the h1. (4) **Reflection (slide 17) is the deck's ONE light slide** (`.slide--lav`, ink on lavender) with the mid-rebrand lesson as a bottom-anchored 46px `.pull` — the deliberate INVERSION of homewiseslide's mid-deck ink statement: a dark deck gets a light peak. (5) Progress bar is **solid `#1e78ff`** (4-color ramp removed per the one-accent rule). (6) Slides 11–16 use the 2-col `.media-head`; the plate shrink-wrap fill (`flex:1 1 auto` + `width:fit-content` + `max-height:100%`) is KEPT from the polish pass. (7) Font link went **400 → 400,500** for the display ladder (faux-bold gotcha unchanged); `fit()` lane went **76 → 160px** so the letterbox chrome clears the in-slide folio. Old pins in `coffeeslide_comments_v1` keep their slide mapping (indices unchanged) but their %-anchors land on shifted layouts.
- **Self-intro slide added 2026-07-17 (per user; deck now 20 slides), then made PERSONAL same day (user: "i mean introduce me, person" — the first cut was the professional one-liner + lone portrait, too LinkedIn):** slide 2 "Hello · I'm Chang Mou" between Title and Overview — `.split` with bio left + a **2×2 `.intro-grid` life-photo collage** right (`profile.jpg` / `food.jpeg` / `pier.jpeg` / `jenga.jpeg` from the About photo deck; square `aspect-ratio:1/1` `object-fit:cover` cells, radius + hairline border; the earlier `.intro-photo` single-portrait class was replaced). Bio is the About-page Background copy nearly verbatim: P1 identity ("product designer in San Francisco with a Master's in Interaction Design… parts of design that AI can't do for you yet") + the personal P3 ("Outside of work, I cook, walk the city on foot…") with **"Good design starts with a good life." white-highlighted** as the personality moment; the process paragraph (P2) deliberately skipped. Org-free per the About-bio rules. Counter fallback 19→20; all later `#N` deep links shifted by one. **Faux-bold titles fixed 2026-07-17 (per user "font weight too much for title, refer to the website"):** `.title-h1` and `.s-title` never set `font-weight`, so UA-default h1/h2 bold (700) applied — and since only the 400 face of Neue Montreal is loaded, the browser **synthesized a fake bold**, making deck titles chunkier than the site's regular-weight headings. Fix: explicit `font-weight: 400` on both. **Gotcha for any new page:** the site is single-weight 400 by design, so every h1–h4 rule MUST set `font-weight: 400` explicitly (style.css does; a fresh page's UA defaults will silently synthesize bold).
- **coffeeslide "What I'd do differently" live-prototype slide (added 2026-08-06, per user; deck now 21 slides).** New slide 20 between Reflection and Sign-off, themed on the user's brief "what I will do differently for this feature": left column claim h2 **"This time, the agent goes first"** (deliberate callback to the Problem slide's "Nobody wants to go first") + 3 `.mini` rows (decision-not-questionnaire / reasons stay visible / feedback closes the loop) + `.demo-note`; right column `.agent-frame` holding a **live, click-through React prototype in an iframe** — "Luna," a conversational rebuild of the pairing flow (user's Figma Make export from figma.com/design/x17GKZDL4zpGvqIpWxKOvX, dropped in ~/Downloads). Built with Vite to a static bundle at **`/coffee-agent/`** (~500KB, `base:'./'`; keeps the export's `noindex` meta, NOT in sitemap — standalone URL works and the slide links it as "open it full size"). **Patched source is checked in at `coffee-agent-src/`** (minimal package.json: react/react-dom/motion/tw-animate-css — the export's versioned-alias deps belong to the unused shadcn `ui/` folder; also patched: `p-8` removed from App.tsx so the 500×700 window fills the iframe exactly, Inter webfont added to its index.html since the export's fonts.css was EMPTY and the app rendered serif, and 12 em dashes in Luna's strings swept to house punctuation). Rebuild: `cd coffee-agent-src && npm i && npm run build && rm -rf ../coffee-agent && mkdir ../coffee-agent && cp -R dist/ ../coffee-agent/`. **Three integration facts that will bite:** (1) **`scrollIntoView` inside a same-origin iframe scrolls the HOST page's ancestors — including `overflow:hidden` boxes** — the app's message autoscroll dragged the whole deck track sideways (~half a slide, copy column off-screen); fixed at source by scrolling the messages container itself (`el.scrollTo({top: el.scrollHeight})`); any future live embed must obey the same rule. (2) The iframe is upscaled `transform: scale(1.08)` (500×700 app → 540×756 canvas) so its 14px UI type stays legible at deck scale; clicks resolve correctly through the nested stage+iframe transforms (verified: book → reflection → history loop all work in-slide). (3) **Lazy `data-src` load wired into `render()`** — the bundle loads exactly on arrival (`index === demoAt`), so first paint never pays for React and Luna's opening script plays as the viewer lands; verified `End` jumping past the slide does NOT load it. Consequences: sign-off is slide 21; `coffeeslide_comments_v1` pins previously on slide 20 (old sign-off) now point at the new slide. **Probe gotchas recorded:** a `navigate` to a hash-only URL is same-document and does NOT reload (stale-bundle false positives — bust with a query param), and synthetic `keydown` dispatched on `document` throws in the deck's handlers (`e.target.closest` — document has no `.closest`); dispatch on `body`. **Brought onto the page 2026-08-06 (per user "bring this experience into the study case"): coffee.html now has an 11th section `#rebuild`** ("What I'd do differently" eyebrow, claim h2 "This time, the agent goes first" — a deliberate callback to the Problem card "Nobody wants to go first") after Reflection. **The settled 10-section spine was consciously extended by the user here** — the two prior 11th-section rejections (named-principles list, HITL section) were about restating arguments already on the page; this is new material with a live artifact, and it honors the case-study-format memory's "end on the outcome" rule (a clickable rebuild is the 2026 outcome artifact). Prose is framed explicitly as a **personal exploration** so no reader thinks Litespace shipped Luna. Structure: prose + `.deliverable` caption ("Try it. This is the working build, not a video") + `.agent-embed` (lavender #D0D1FD plate, iframe at natural 500×700, IO-deferred via the same data-src pattern, rootMargin 200px one-shot) + **`.agent-restart`** (40px ink-on-lavender circle, bottom-right 16px of the plate, per user; reloads the iframe's contentWindow; **lucide rotate-cw glyph — the first closed-ring icon was rejected per user "so closed, make it a little open"**; carries a scoped dark `:focus-visible` outline-color override because the global near-white ring vanishes on the light plate; messages-area `pb-0` → uniform `p-[21px]` fixed the same day per user "no margin at the bottom"). **The whole section is desktop-only per user** — `.project-section--desktop-only` (display:none ≤768, Countersign precedent); bonus: the hidden element has no box, so the IO never fires and **phones never download the ~500KB bundle** (verified at 375: section gone, iframe unloaded even at page bottom, zero overflow, Reflection closes the mobile page as before). Side-nav gained the 11th link (rail is hidden ≤1440 anyway; the scroll-spy builds from nav links generically, so no JS change). App also made responsive in the same pass (`max-w-full h-[min(700px,100svh)]` + `overscroll-contain`) so the standalone /coffee-agent/ URL works on phones — verified at 375. llms.txt + api/chat.js both gained the Luna-rebuild mention (chat.js notes desktop-only). style.css **v=228→v=229** across all 9 pages. **Same-day follow-ups (all per user): (1) deck slide 20 got the restart too** (`#demoRestart`, floated `right:-64px` off the frame's bottom corner — the deck frame has no plate margin to host it; ink-on-cream styling; wired in the engine next to demo/demoAt) **and its `.demo-note` line was deleted** (the orphan `.demo-note` CSS rules stay in the deck's inline styles — an edit removing them was explicitly rejected mid-flight, leave them). (2) **Page caption kept, but its "You can also open it full size." sentence deleted** — consequence: the standalone `/coffee-agent/` URL was briefly unlinked from both surfaces; **2026-08-06 (per user) the deck got it back as an ICON, not a sentence — `a.agent-open`, an arrow-up-right circle in the `.agent-restart` family stacked above the restart button (bottom: 60px), target _blank. The page section remains linkless.** (3) **Chat pacing calmed** ("bubble jump too fast"): reveal 2ch/16ms→2ch/24ms (~83cps), thinking beat 400→650ms, inter-message gap `len*8+250` cap 2200 → `len*12+400` cap 3000 — **ChatWindow's sleep formula must stay synced to ChatMessage's reveal rate** (paired comment in both files). (4) Messages-area `pb-0`→`p-[21px]`. **A 12-agent adversarial workflow over the change set confirmed 7 findings, all applied except one:** default-to-Going payoff deduped ("careful defaults" — the beat already lands at the Problem card, Final Design, and Reflection); "what you would rather do" now lands ONCE in page copy (the caption; both surfaces' prose tails are "and the rest is a conversation."); two demo-honesty fixes (dropped "works as well as any button" — typing breaks at the reflection prompt; dropped "shapes who Luna proposes next week" — the demo's reflection posts an adjustment note but does not drive the next match, so mini 3 says "tells Luna what to adjust next time"); 4 title-case app strings → sentence case ("Your Coffee Chat assistant" / "Confirm time" / "Add your availability" / "Submit availability"); restart wired BEFORE the loader's no-IO fallback return. **Deliberately NOT actioned:** the demo supports typed in-person/online switching, which Iteration 2 says A/B testing removed from the 2023 flow — left as-is because the capability has no visible UI (it surfaces only if asked), which is the remove-friction-keep-capability resolution; revisit only if a reader probes it. **Privacy-transparency intent added 2026-08-06 (per user, out of the "does slide 19 need privacy consent?" discussion — the answer given: the FEATURE needs a consent story, the demo does not need a consent screen; consent's home is onboarding, which the demo deliberately skips, and the 63% opt-in stat is the real feature's consent artifact):** typed "what do you know about me" / "privacy" / "my data" etc. (intent kind `privacy`, checked before `about`, no pattern overlap) gets a two-message transparency answer — the sources Luna reads are exactly what the reason cards cite; reflections stay private from the match; the match sees the same kind of source-cited reasons; opt-in with exit via settings (worded to promise NO typed action the demo cannot handle — the overclaim class the adversarial pass caught). Same surfaces-when-asked pattern as the mode switch, zero visible UI; the handler re-prompts `currentPrompt` so buttons survive the digression. Verified live end to end. **Luna "2026 pass" 2026-08-06 (per user "how to make this feature more 2026?" → research workflow (3 agents: shipped-agent-product patterns, design essays, code-architecture map) → user approved all 4 recommendations):** (1) **Real model behind the free-text path — `api/luna.js`**, a clone of api/chat.js's hardened shape (origin allowlist, 20/5min `globalThis.__lunaRate`, 500-char message cap, 8-turn sanitized history, Haiku `max_tokens: 200`); its system prompt carries Luna's persona + the ENTIRE fictional world serialized from matchData.tsx (**mirror rule: if matchData changes, update the prompt**) + a no-prompt-leak rule. ONLY the unknown-intent default branch calls it (`handleGenerated`); all 11 scripted intents stay client-side and free. **Failure of any kind (404/timeout-10s/empty) falls back to the old honest boundary line — locally /api/* 404s, so the local demo exercises exactly this fallback and still works on stage.** Generated replies carry a muted "· generated" tag (ChatMessage, shows after reveal completes) so scripted vs live is visibly distinct. (2) **Trace step kind** — small check-icon activity lines via `runSteps`: "Scanned calendars · org graph · shared channels" before every proposal (sources ⊂ the privacy answer's list on purpose), **"Pinged {name}'s assistant · confirmed on their side" before "Booked."** (the agent-to-agent beat), "Memory updated · saved in this browser" after reflections, "Memory loaded · your reflections carried over" on return visits. (3) **Cross-visit memory** — `luna_memory_v1` in localStorage (history/seenMatchIds/meetingPreference; lazy state init + persist effect); a visitor who ACTED last time (met someone new or reflected — merely watching doesn't count) gets "Welcome back, Kevin. Last time you met {last}…" and the next unseen match via `proposeMatch(..., preSteps)`; all-seen cycles by resetting seenMatchIds. **Consequence: the deck/page restart button no longer fully resets — memory survives reload BY DESIGN (restart now demonstrates memory); a true first-run needs site-data cleared, which the privacy answer's new third message tells the user.** api/chat.js PORTFOLIO entry + llms.txt note the live-model fact. Production test required post-deploy (endpoint 404s locally). **A 2-agent verify (security + honesty) caught 9 real findings before push, all fixed — the HIGH is a lesson: the first system prompt's world model was written partly from memory and invented wrong Priya facts (Natarajan/New York vs the real Nair/Seoul) and wrong slots; the prompt is now transcribed from matchData.tsx verbatim, and the client sends a sanitized live-state `context` block (met count, past chats, current proposal, preference) that the prompt tells the model to trust over its static text — so memory/proposal drift can never contradict the screen.** Other fixes: privacy source list expanded to cover what the reason cards actually cite (shared docs, directory, shared meetings — both surfaces, handlePrivacy + luna.js rule 4); reflection notes softened to promise only what happens, and option 2 ('Useful, a bit formal') now REALLY sets meetingPreference to in-person; welcome-back requires an actual reflection (not just a second match viewed), never re-proposes the person just met on a cycle reset, and says "next week"; the memory receipt trace is gated on a localStorage capability probe; the deadline banner's impossible date fixed (July 19 is a Saturday in the demo's own Mon-Jul-21 grid → "Sunday, July 20", banner + prompt); '· generated' carries an explanatory tooltip; **and api/chat.js's error path leaked upstream Anthropic error bodies via a `detail` field — closed.** Post-deploy: Haiku slipped an em dash past the prompt rule on the second production reply, so luna.js now strips em/en dashes from replies mechanically (commas, per house punctuation). Production verified end to end: ground-truth Priya answer, live-state count answer, and the '· generated' marker + tooltip rendering in the real UI (a probe reading innerText mid-reveal misses the marker — its gate opens only when the typewriter finishes). **Ops lesson: the preview pane is SHARED — the "phantom" demo interactions chased during verification were the user click-driving the embed in real time** (instrumented no-touch runs were spotless, `isTrusted` logging is the discriminator); unexplained state changes in an interactive embed are probably the user. **Deck Reflection moved to slide 20, AFTER Luna, 2026-08-06 (per user "what I learned at the end"; a first reading of their "18 and 19 should switch" as a DS↔Reflection swap was built then corrected same turn):** closing order is now Final Design ×5 → **Design System 18 → What I'd do differently 19 → Reflection 20 (ink) → Sign-off 21** — the deck ends on What I learned, with the two ink slides back-to-back as a closing dark sequence. NOTE this diverges from the page on purpose: coffee.html runs Reflection → Luna (the rebuild as coda), the deck runs Luna → Reflection (a narrated deck closes on the lesson). Luna's deep-link is now `#19` and `demoAt` self-computes; comment-mode pins on old slides 18-20 shift meaning again (known index-keying consequence). **Deck audit pass 2026-08-06: EXECUTED THEN FULLY REVERTED the same day (user: "revert to the version before i ask") — the deck is back at the pre-audit tree (8974592) except one typo fix. Do NOT re-apply any of it unprompted; the deck's current form is settled.** What was built and discarded: a 6-agent audit (4 lenses + web research + xhigh judge) and 4-agent crop workflow shipped a 10-item plan (claim h2s on 4/11/12/20, honesty recuts on slide 3, scaled triads/numerals on 5/7, a single-row flow SVG for 9, state-detail crops for Final Design 15-17, scale-parity crops for the testing pairs, a palette-half crop for 18, and an in-place ember rebuild of iteration2-before.png's orange box); then an "immersive" pass (animated gradient bookends + a slide-4 pan/zoom) was rejected first ("look ugly as fuck"), after which the user reverted the whole audit too. The findings live in the session transcript if ever wanted, including two open data gaps (the 63% denominator; whether an acceptance/attendance rate exists) and the judge's kill list. **The one survivor, re-applied as its own commit: "User A receive→receives" in BOTH files' flow-chart SVGs — an objective grammar error that was live on production.** **Slides 5/7's empty middles are SETTLED AS-IS (2026-08-06): the user asked about them ("feels empty in the middle. do we need some illustration?"), illustration was advised against (the deck has no illustration language; it would be a template-tell), the scale fix was rebuilt and shown as renders, and the user said "revert" — do not re-propose either scale or illustration for those slides.** Probe lessons kept: the pane's document timeline FREEZES while not compositing (getAnimations() reports `running` with currentTime pinned at 0); headless `--virtual-time-budget` double-capture pixel-diffs prove main-thread (custom-property) animations but NOT compositor (transform/opacity) ones, which snapshot at their start state — for those, force a pane composite with a `computer screenshot`, then read the computed transform matrix.
- `homewiseslide.html` — **Slide-deck version of the Homewise case study** (added 2026-07-28, per user "make the homewiseslide as well? Use study case color, do not use site's gradient"). Clean URL `/homewiseslide` via a `vercel.json` rewrite. Same deck engine as coffeeslide.html (fixed 1920×1080 `.stage` scaled by a JS `fit()`, `.deck-track` translateX nav, keyboard/click/swipe/`#N` hash deep-linking, Figma-style comment mode with its own localStorage keys `homewiseslide_comments_v1`/`homewiseslide_commenter`), but **a completely different skin**. **The color decision is the point: the deck wears HEARTH, Homewise's own design system, and is LIGHT.** The user's "do not use site's gradient" was read as pushing away from the portfolio's identity toward the project's, so the warm→blue hero radials and the `#D0D1FD` lavender plates are gone. Tokens were lifted verbatim from the live `/designsystem` page (homewise-rust.vercel.app/designsystem, read in-browser because the page has no CSS custom properties to scrape and the copy lists every hex): canvas `#F6F4EF` / canvas-soft `#FAF8F3` / card `#FFFFFF` / canvas-deep `#EEEAE1` / ink-900 `#1A1A17`, ink 700-100 `#3C3C38 #6B6B65 #A8A8A1 #CFCEC7 #E7E5DE`, **sage** (trust) 50-700 `#F1F4EF #E2E8DC #C6D2BB #9AAE8C #738A63 #4F6942 #3D5333 #2F4027`, **ember** (caution) 50-500 `#FBF3EA #F5E4CE #EBC89B #D9A461 #B98132 #8E601E`, **sky2026** (info) 50-700 `#EEF3F5 #D9E4E9 #8FAAB3 #4E6F7A #2E4850`. Hearth's own rules are honored, not just its hexes: **shadows are banned** (depth comes from the 5-tier surface ladder + hairlines, so there is not one `box-shadow` on slide content), the **rule-line eyebrow** (`.s-label` uses a 24px `::before` rule + 0.20em caps, matching Hearth's "SM · W-6" eyebrow pair, NOT coffeeslide's full-width trailing rule), and Hearth's voice rules (sentence-case headings, no em dashes) which already match the site's. **Title + sign-off are the ink-900 bookends** with the `.dot-grid` brand texture (18px radial dots) — deliberately dot-grid and NOT Hearth's other texture `.grain-dark`, because [[the no-grain rule]] in Design Decisions records the user rejecting SVG-noise over dark backgrounds as reading like glitch/static; a crisp geometric pattern sidesteps that entirely. A quiet sage/ember radial wash keeps the dark slides from reading as generic black. Progress bar is the tri-role ramp sage→ember→sky, not the site's 4-color gradient. **19 slides** — **restructured 2026-07-28 to Chang's mentor's case-study storytelling framework** (high stakes upfront backed by UXR then narrowed to a scoped design statement · explain why the problem is HARD so the reader does not just see "a page" · design principles that follow from the complexity so the solution feels like the only correct answer · 10-15 min, explainable in under five). It was 21 slides in homewise.html's chronological order; the rebuild imposes an argument on the same material. New spine: Title → **The bet** ("People will hand the hire to an agent they can trust" + the 70/41/33 market stats, so the PMF question is stated as a bet before anything else) → Overview **+ my role merged** (the 4 "What I did" bullets as a `.note-list` beside the cover) → **Evidence · Eight interviews** (the 3 pain cards) → Insight ($250→$7,000 spread) → **Why this is hard** (a user problem / a data problem / one connected system) → **Design statement** (scope before shopping + the three named principles: structured not conversational · verified not scraped · the human signs off) → The workflow (Scope/Match/Compare/Track) → Process → **Final design 1-4** (was 5: "One scope, every contractor" folded into the scope slide's caption, since it is a consequence of that clip rather than its own beat) → Outcome → Design system → **Collaboration + shipped build merged** (the skipped-Figma story narrates over the proof video) → Reflection → **Hello** (the personal intro moved from slide 2 to here — in an interview you have already been introduced, so it costs the first minute before any stakes; as the penultimate slide it lands as "who made this") → Sign-off. The clip count drops 7→6 with the contractors slide. **The case study page was NOT changed to match 1:1** — see the homewise.html bullet; a deck is narrated live, a page is skimmed, and the judge in that audit explicitly killed giving the page a "The bet" section. **Four things that will bite a future edit:** (1) **The clips are 1920×1204, NOT 16:9** (and `homewise-live.mp4` is 1920×994) — an `aspect-ratio: 16/9` frame silently crops ~35px off the top and bottom of every clip. The frame is pinned to `1920 / 1204` with a `.clip--play` override at `1920 / 994`, so `object-fit` has nothing to crop and nothing to letterbox. `mdls` returns null for these mp4s; read `videoWidth`/`videoHeight` off a `<video>` in the browser instead. (2) **The clip's `flex-basis` MUST be 0, not `auto`** — with `auto` the aspect-ratio's own content size (width 100% → 1680 → height 945) feeds the basis and survives shrinking, so the frame overran the slide by 18px and got clipped by `overflow:hidden`. `flex: 1 1 0` + `min-height: 0` makes the height purely leftover free space. (3) **`fit()` reserves a 160px chrome lane, not coffeeslide's 76px** — the stage is CREAM here, so the light brand/counter/nav-circle chrome must stay fully inside the ink-900 letterbox or it lands on the slide and disappears. 160 guarantees ≥80px top and bottom at every viewport (verified 1440×900 and 1024×768: zero chrome/stage overlap). **Clip posters + two-slide warming 2026-08-03 (per user "design system video can not play in the actual live site"):** the video was never broken — live serves 200/`video/mp4` with **Range 206** (Safari's requirement), the `moov` atom is at the front (faststart), and the codec is H.264/`avc1`, not HEVC. The failure was PERCEPTUAL: the five looping clips were `preload="none"` **with no `poster`**, so arriving at a slide showed a flat `--canvas-deep` rectangle until enough of a 6-24MB file arrived (hearth alone is 24MB; on a 10 Mbps line that is ~20s of empty plate, worse on a cold `x-vercel-cache: MISS`). **TWO REAL PLAYBACK BUGS were found and fixed the same day, and the posters alone would NOT have been enough:** (i) **the clips were not actually playing on arrival** — `syncMedia()` called `v.play()` while the element was still at `readyState 0` (which is exactly where `preload="none"` leaves it), that play is aborted by the element's own resource selection, and the old `.catch(() => {})` swallowed it, so the slide sat on its poster looking frozen. Replaced with a `playClip()` helper that flips `preload='auto'` FIRST, calls play, and **retries once on `canplay`** (bailing if the deck moved on). Verify with a pixel diff — draw the video to a canvas twice and compare — because `paused`/`readyState` alone read as healthy while nothing moves. (ii) **preload was flipped to 'auto' on the way past and never flipped back**, so by the late slides every clip (~119MB) was still pulling in the background and starving whatever you were trying to watch; this is what made slide 16 crawl. Now a **sliding window**: current slide + next two are `auto`, everything else is released to `none` (verified: 3 warm at slide 10, 2 at 13, 0 by 16). Fixed by (a) **frame-0 posters for all five clips** (`images/homewise-{scope,match,compare,track,hearth}-poster.jpg`, 1400px/~110-136KB each) and (b) **warming the next TWO slides** instead of one. **Poster extraction technique, since this machine has NO ffmpeg:** `qlmanage -t` picks a *representative middle* frame, which makes the poster jump when playback starts at 0 — instead compile a ~15-line Swift `AVAssetImageGenerator` (`requestedTimeToleranceBefore/After = .zero`, `appliesPreferredTrackTransform = true`) with `swiftc`, which gives exact frame 0 so poster→playback is seamless. **Re-encoding was TRIED AND REJECTED:** `avconvert` exposes no bitrate control, only resolution presets, and `Preset1280x720` turned the 24MB hearth clip into 20MB at 1280x804 — a third of the resolution for a 17% saving. Not worth it; revisit only if ffmpeg is ever installed (CRF would halve size at full resolution). Gotcha: `avconvert` silently writes NO output file if you pass `--disableSubtitles`, and still exits 0.
- (4) **Videos use `preload="none"` + play-on-arrival**, because the six clips total ~100MB; `render()` plays the active slide's clip (which is what triggers its load), pauses and rewinds the others, and warms the NEXT slide's clip to `preload="auto"`. An IntersectionObserver would be wrong for the same reason `loading="lazy"` is wrong in a deck: slides move by `transform`, so they never leave the scroll viewport. Also note `.slide--media` trims only `padding-bottom` (84→56px) to hand the artifact more room — never `padding-top`, which would break the one-canvas-y header anchor. **NOT linked from the home grid or homewise.html** and **not in `sitemap.xml`** — standalone at `/homewiseslide`, exactly like coffeeslide. Self-contained (inline `<style>`+`<script>`, does NOT load style.css, so no version bump), loads Neue Montreal at **400,500** (500 for the editorial display ladder) and sets `font-weight` explicitly on every heading per the single-weight gotcha. **deck.gallery editorial reskin 2026-07-31 (per user "current designs are too ugly", referencing deck.gallery — SUPERSEDES this bullet's boxed-card/tri-ramp/eyebrow details; same 19 slides, same copy, same engine/comment mode):** the layout grammar moved from Hearth-tiled cards to editorial print, patterns lifted from three deck.gallery references (Geist*Studio Impact Report: stat rows + in-slide document chrome; Reece Evans Portfolio: cover with tiny metadata columns top + giant wordmark bottom; Lefty: persistent brand footer). What changed: (1) **every slide is framed by in-slide chrome** — `.s-label` became a full-width header row (label left, static "Homewise · Case study" via its `::after`, hairline under; the 24px `::before` dash is gone) and a `.page-foot` folio (rule + "A case study by Chang Mou" + "NN / 19") is **injected by JS at init** into each `.slide`, so markup stays clean and render() never touches it; the fixed `.deck-counter` element was **removed** (folio carries the number; letterbox chrome is now brand + nav only). (2) **No boxed cards anywhere**: stats are full-width hairline **rows** (`.rows`/`.row`, 112px numerals left, annotation pinned to a right 620px column — the Geist table); triads/stages are rule-topped **columns** (`.cols`/`.col` with tiny sage index, no backgrounds); the $250→$7,000 spread lost its inset well and its figures grew to 128px ember/ink. (3) **Bottom-anchored composition**: artifact blocks carry `.push` (`margin-top:auto; padding-top:44px`) so slides read title-zone-top / artifact-bottom instead of content floating in the top half with a cream void below (the user's core complaint); `.slide-inner > .split` gets `flex:1 1 0; min-height:0` so Overview/Hello center between header and folio. (4) **Title slide is Reece-style**: 4 tiny metadata columns top (Role/Team/Timeline/Built with), `margin-top:auto`, then sage kicker + **280px nowrap wordmark** + description bottom-right (`max-width:430px` — at 300px/520px it clipped off the right edge, the one real overflow hit during the build; keep h1 size and sub width paired). Sign-off mirrors it ("See it in full" at 170px). (5) **Outcome is a mid-deck ink statement slide** (`.slide--ink`: flat ink-900 + sage wash, NO dot grid — the dots stay reserved for the two bookends; 96px white title, CTAs bottom). (6) Media slides use a 2-col `.media-head` (46px title left, 19px annotation right) so the clip keeps its height. (7) Progress bar is **solid sage-400** (the tri-role sage→ember→sky ramp read decorative, replaced). (8) Reflection's closing highlight became a bottom-anchored 46px `.pull` line. **Probe gotcha recorded during verification:** inactive slides' reveal state (`translateY(18px)` on `.slide-inner > *`) inflates scrollHeight/bounding-rect probes by exactly 18px — a uniform +18 "overflow" across non-active slides is the entrance animation, not a layout bug.
- **homewiseslide Final Design tradeoff system + 24-slide restructure (2026-08-11, per user + mentor feedback — SUPERSEDES the "19 slides" counts above; same engine/comment mode).** Note the deck was briefly 25 slides this session; the **Hello self-intro slide was then DELETED per user ("maybe take slide 2 off") — this supersedes the 2026-08-03 "slide 2 is the settled position" note, and it re-opens the coffee/homewise deck asymmetry (coffeeslide still opens Title · Hello · The bet; homewiseslide now opens Title · The bet). The `.intro-grid` CSS + the 4 life photos are unused in this deck (left harmless).** Structural changes this session: (1) **the title slide now carries the product cover** — a `.title-cover` (flex-fill, `aspect-ratio: 2400/1065`, no crop, `rgba(255,255,255,0.16)` hairline) fills the band between the meta row and the wordmark (mentor: product must be visible before The bet; a standalone full-bleed cover slide 3 was built first, then merged into the title per user "merge slide 1 and 3"); Overview's split image became `homewise-track-poster.jpg` so the cover doesn't repeat. (2) **Final Design is FIVE decision/proof pairs** (labels "N of 5"; "One scope, every contractor" restored per user — the 2026-07-28 fold-into-scope is reversed): each decision slide = claim h2 + body + "The tradeoff" block (`.trade-k` eyebrow, `.trade-cols` rule-topped columns, `.tick--bad/--good`, `.trade-shot` evidence images, receipts at 18px); each proof slide is chromeless `.slide--full` (clip at full height 1722×1080, 99px ink mats, folio hidden via CSS, **the `margin: 0` reset on its clip is load-bearing** or the mats go asymmetric). New poster `homewise-contractors-poster.jpg` (frame-0 via the documented Swift recipe). The Design-system slide's annotation ends with an inline "Explore Hearth live ↗" anchor to homewise-rust.vercel.app/designsystem (2026-08-11 per user; `.media-head p a` = the deck's link idiom: ink-700 underline, hairline-toned, `white-space: nowrap`, target _blank — mirrors the page's existing `.hw-cta`; a pill-CTA promotion was offered and not taken). (3) **Every beat MUST carry a tradeoff — the user's standing rule: "a decision without a tradeoff is just a feature description" — and the tradeoff must be about the beat's own homeowner-visible surface** (a contractor bid-form story was built and rejected per user "do not talk bid form" / "focus on the main flow user can see"; a quote-tracking story under the booked-dashboard slide was caught by the user as surface-mismatched). Final pairs after the user's one-by-one review (2026-08-11; each header "The version I didn't ship" ✕ / "Why it shipped this way" ✓ — **the user's placement rule from the review: each tradeoff must argue the beat's OWN claim, not just its own page**): beat 1 scope = the intake chat that could not say no (instant "Photos uploaded", nothing checked) → the scripted photo check that rejects a bad shot with retake advice (✓ copy says "is built to check" per the Luna demo-honesty standard — the check is scripted, not real vision; and the ✕ avoids conflating the instant-flip and any-photo-advances eras, which never coexisted); beat 2 one-scope = the killed "92% confidence" diagnosis score → "Most likely cause" + benchmark (the user relocated this here from beat 1: "already inside of the scope"); beat 3 match = header CTAs + decorative swap icon → "Ready to proceed" dock below the evidence + real per-column swap + 24-hr urgent window (moved from beat 2 per user; the **badge→sage-tinted-column story was RETIRED in the same review** — `homewise-trade-match-*.jpg` stay in images/ parked unused; the quote-tracking crop was deleted outright when its beat was replaced); beat 4 compare = hardcoded "3 deviations flagged" + contact as ghost link → flags in cells + "Ask before booking" above Approve; beat 5 track = the agentic thread as the progress tracker, per user "talk the agentic flow chat to track the progress" (its THIRD version: quote-wait story killed by the user's surface catch "it has already booked the contractor, why compare the quotes?", then the two-homes/one-home story replaced on request): ✕ the old thread did not know the job ended (still "Live thread" / awaiting approval / suggesting "Approve Jason" with the work complete — captured at ff51839 with jobCompleted force-patched) → ✓ the agent narrates each step and writes the ending in (booking confirmed → visit complete → work confirmed → job closed out → "Completed" flip — captured at HEAD with jobCompleted+decisionHandled forced; the conversation thread is an INNER scroll container, so tail captures must scroll every overflowing div, not window). A completion-screen binary-recommend story was cut mid-build when the user redirected to the thread. `homewise-trade-track-*.jpg` joined the parked-unused set. **Interview-risk flag, surfaced to the user: HEAD's live task card still shows a "Task confidence" figure (ActiveTasks.jsx) — the beat-2 tradeoff claims only the scope DIAGNOSIS dropped its score, which stays true, but a prober clicking the live prototype can find a surviving confidence number.** **Provenance is the point: every ✕ is a real built-then-removed iteration screenshotted from the product repo's own git history** — clone github.com/chang627627/homewise to scratch, `git worktree add` the old commit, patch `App.jsx` with a `?page=` URLSearchParams override (+ force `hasStartedFirstTask`/prefills where a state needs to exist), run two vite dev servers, capture with headless Chrome (**`--virtual-time-budget=30000` — at 9000 framer-motion entrances freeze mid-flight and rows render washed**), crop with PIL to per-pair matched aspect ratios, JPEG q90 into `images/homewise-trade-*.jpg` (10 assets: scope/send/match/compare/track × old/new). **Copy was adversarially verified across four workflow rounds (13+4+1+0 findings, all fixed) — the recurring failure classes, worth checking on ANY future repo-grounded claim: (a) an intermediate build described as shipped** (the 3-photo intake gate was itself later simplified to single-photo-with-AI-check in b9a03bd; the evidence-cited exclusions section was cut in af1219b; "match confidence" exists only in an orphaned component at HEAD — also fixed in homewise.html's caption), **(b) a story attributed to the wrong surface** (SMS→phone-first belongs to Quote Compare's drawer, not the match screen), **(c) copy contradicting its own evidence image** (the old send screenshot visibly shows the swap icons; the fix is "decorative, wired to nothing"), plus overclaimed gates ("booking waits for a phone call" — the shipped step is optional) and principle-03 near-verbatim echoes. zsh gotcha that cost a re-capture: unquoted `$spec` does NOT word-split in zsh, so a `for spec in "a b c"` + `set -- $spec` loop shot seven identical screenshots. Comment pins are index-keyed; old pins shifted again (known consequence). **Storytelling pass 2026-08-12 (per user "apply all 7", from a 3-lens + judge workflow; DECK ONLY at first per user "do not touch study case"; the user then asked what the page needed and 4 items were PORTED to homewise.html same day (the five ✓ columns with first-person verbs + both closers, restoring block parity; the circular linter bullet; "the repo they shipped to production" in bullet 4; "Interview 1 of 8" on the spread label; plus the send-caption bridge) — the deck's FD s-bodies (slides 10/12/14) still deliberately diverge from the page's captions, and the page deliberately skipped the We-bet/Reflection-receipts/autonomy-problem changes since it has no bet section, its Reflection is the settled whole-arc form, and its connected-system card feeds Process):** 22 copy-only edits — first-person protagonist thread (slide 2 "We bet", slide 5 "Reading the eight transcripts…" + "Interview 1 of 8" spread label, slide 7 "I set three principles", first-person verbs opening all five ✓ columns), slides 10/12 de-duped ("exact same job" said twice) with slide 12 bridging into its own tradeoff, slide 14 names the word-of-mouth callback, slide 3 mini-cards plant "shipped by them in production" early (the panelist lens's HIGH: the fact was withheld until slide 22) and drop the circular "drift linter that caught drift" line, slide 6 col 03 became "An autonomy problem" (fixing the promised-but-broken constraint→principle 1:1 with "the human signs off") with the connected-system fact moved into slide 9 as the build-first argument, slide 23 Reflection opens with testing receipts instead of closing the bet by assertion, slide 20 ink adds "Every trust call in the last five slides is in there, running for real.", slide 22 drops the third code-first telling + states the "four minute walkthrough". Judge's kill list (do not re-raise): slide merges for runtime, 20/21 swap, FD h2 rewrites, 70/41/33 callbacks on slide 8, any tester-methodology sentence (fabrication ban — NEEDS one true sentence from Chang about who/how the testing rounds ran, flagged and unresolved). Rollback point: commit 087141f.
- `novaslide.html` — **Slide-deck version of the NOVA case study (added 2026-08-13, per user "build the nova as slides by using current slides design systems"). Third deck on the shared engine + shared LIGHT system**, assembled by transplanting coffeeslide.html's head/CSS/chrome/both scripts verbatim (python splice at the deck-track marker) + 16 new slides. **Per-deck tokens: `--accent: #0070C8` (NOVA blue #0094FF deepened for cream, same move as Coffee's #2456D6) and `--plate: #E8E8E8`** (the case study's own phone-panel gray); ink-wash rgba(0,112,200,0.22); bookends keep the warm→blue site gradient like coffeeslide. Spine (16): Title (Partner "CCA × UNICEF Innovation", Type "Sponsored academic", h1 NOVA) · The bet ("A child will wear safety tech only if it feels like freedom" + 80%/20+/50+ rows) · Problem ×3 · Solution (For children / For parents) · Research (activities collage) · Insights ×3 + the software-not-hardware payoff line · Design statement ("Four things at once", ×4 cols) · HUD text (annotated example) · Independence vs tracking (claim h2 "The feature parents wanted would cost us the wearer"; **3 phone screens in ONE plate via inline `display:flex` + `width:auto` imgs — multi-image plates work fine**) · The mascot (before/after 2-up) · Usability testing (**the 3 parent quotes live HERE ONLY as cols with `.col-cat` attributions — deliberately removed from the HUD/mascot slides so each payoff lands once**) · Final Design 1-3 of 3 (**screen CYCLES, added same day per user "each mode need to switch screens like site did": each mode's plate steps through the SAME screens as nova.html's carousels in the same order — 3/9/6 shots — via `.plate--cycle` + an appended cycle engine. Shots stack absolutely with object-fit contain inside the flex-filling plate (the shrink-wrap `width:fit-content` is overridden at higher specificity `.slide-inner > .plate.plate--cycle:last-child`); auto-advance 2600ms ONLY while the slide is active, reset to shot 0 on exit, click steps manually, reduced-motion gets static-first-frame+click; a 250ms poll keys cycles off `.slide.is-active` because the engine's replaceState nav fires no event. PROBE NOTE: mid-session `location.hash` writes DO navigate this engine, but verify nav with synthetic keydown on BODY, and remember `.page-foot` is per-slide static text — querying the first one never reflects the current slide**) · Reflection INK peak ("The most protective version was the one no child would wear" + the default-is-the-design-decision `.pull`) · Sign-off (stat-free). **NO Hello slide** (follows homewiseslide's 2026-08-11 deletion, not coffee's settled slide 2). Luna/demo engine code left in place unused (guarded, inert); `.intro-grid`/agent CSS unused, harmless. `/novaslide` rewrite added to vercel.json; **'novaslide' added to api/comments.js DECKS whitelist** (shared comments need a deploy; the localStorage fallback works meanwhile; keys `novaslide_comments_v1`/`novaslide_commenter`). Verified: 16 folios auto-numbered, zero broken images, slides overflow-clean — **the title slide's ~39px scrollHeight excess is PRE-EXISTING in coffeeslide's title too (measured 39 vs nova 57, the +18 being the documented reveal-state probe artifact); nothing visibly clips — do not chase it as a nova bug.** Standalone like the other decks: not linked from the grid or nova.html, not in sitemap.
- `pollen.html` — PollenNav case study. **Full-page image optimization 2026-07-25 (same pipeline as NOVA's 2026-06-21 pass; part of the 2026-07-25 improvement-research P3): payload ~74MB → 6.5MB.** 13 opaque fake-alpha 1920×1080 PNGs (~2MB each: research-1/interviews/playbook/triggers/map-v1-v2-final/char-v1-v2-v3-final/info-v1-v2-v3) → JPEG q90 via sips, refs `.png`→`.jpg` (stale `?v=` on triggers/info dropped with the rename), old PNGs deleted; `nws.png` **19.6MB/7680px → nws.jpg 0.20MB/1920px**; `pollenonboarding1.jpg` + `pollenscan1.jpg` (7680px) + `pollen-cover.jpg` (3840px) downscaled **in place** to 1920px q90 with `?v=2` cache-busts on their refs. All below-fold imgs got `loading="lazy" decoding="async"` (cover stays eager). Verified in preview: 25/25 images serve, NWS legend text legible.
- `ticker.html` — TickerPulse case study. **No longer linked from the home grid as of 2026-06-14** (the home card was removed and its 5 final-design wireframes moved to the Play page). The page still exists and renders, but nothing links to it now (the only `ticker.html` reference left anywhere is its own `og:url` self-tag) — it's reachable only by direct URL. **User chose to leave it unlinked (2026-06-14)** — this is deliberate, not an oversight; do NOT re-link it from home or Play unless the user asks.
- `nova.html` — UNICEF NOVA case study (linked from the home grid as of 2026-05-10). Visuals are pulled from the framer reference site at `aware-acknowledge-592065.framer.app/nova` and stored as `images/nova-*.png`. **Cover optimized 2026-06-20 (load-speed pass):** the cover is now `images/nova-hero-unigirl.jpg` (1920px, JPEG q90, 349KB) — it was `nova-hero-unigirl.png` at **10448px / 28MB(!)** but displays at ≤960px, so it was downscaled to ~1920px (= 2× the display size) and converted PNG→JPEG via `sips -Z 1920 -s format jpeg -s formatOptions 90`; the 28MB PNG was deleted and the `<img>` src updated. **Full-page image optimization 2026-06-21 (load-speed pass, per user "images majorly large, loading too long"):** the whole page's image payload went **~154.7MB → 14.6MB (−90.5%)**. Audit found ~40 images, most 3840–7680px wide (one 10180px / 16.5MB!) but displaying at ≤960px, most as PNGs of photographic AR/app screenshots. Applied a real-transparency check via PIL (`alpha.getextrema()[0] < 250` — `sips -g hasAlpha` only reports the *channel*, not whether any pixel is actually transparent; most were opaque RGBA), then: **opaque PNGs → JPEG @ ≤1920px q90** (27 files, refs in nova.html updated `.png`→`.jpg`, old PNGs deleted — e.g. `nova-companion-dashboard` 4.6MB/7680×4320 → 0.17MB); **genuinely-transparent → kept PNG, downscaled** (2 partner logos to 220px tall, 3 `nova-c2-*` phone screens, `nova-mascot-exploration` 16.5MB→0.76MB on its #E5E5E5 panel, 3 `nova-research-*`); **recompressed in place** the two heavy comps (`nova-comp-meta-orion.webp`, `nova-comp-apple-roadmap.jpeg`, ~2MB→~0.2MB each). Verified in preview: all 41 imgs load, 0 broken, logos/HUD-text/mascot all visually clean. **Cache caveat:** the 27 renamed JPGs get fresh URLs, but the in-place recompressed files (logos, c2, comps, mascot-exploration, research) keep their filenames, so returning visitors may serve stale cached big versions until cache expires — add `?v=` if that matters. **`og-image.jpg` kept** (it's the share card). **The "still oversized elsewhere" items were RESOLVED 2026-07-25:** `coffeecover.png` (20896px/3.9MB — so large PIL's decompression-bomb guard refuses it; opened with `Image.MAX_IMAGE_PIXELS = None` for the alpha check, converted via sips) → `coffeecover.jpg` 1920px q90 **0.13MB** (ref updated in coffee.html), and `pollen-cover.jpg` downscaled in place (see the pollen.html bullet). nova.html + coffee.html also got the below-fold `loading="lazy" decoding="async"` pass the same day (covers stay eager), and index.html's Homewise card img dropped its incorrect `loading="lazy"` (row-1 = eager per the card-image convention). Downscaling to ~1920px (= 2× the ≤960px display) is compatible with the [[feedback_image_quality]] JPEG-100/PNG-lossless rule — at 2× display size the quality loss is invisible; it only drops resolution the screen physically can't show.
- `homewise.html` — Homewise case study (added 2026-06-12; clean URL `/homewise` via vercel.json rewrite). **Built to a NEW format per Chang's mentor — short storytelling + outcome-based motion artifacts, ending on the outcome** (see the case-study-format memory) — deliberately lighter than the older nova/pollen/ticker text template. Cover is `images/homewise-cover.jpg` (now contained via `.case-cover` — see the Case-study cover note) (the apples-to-apples quote-comparison screen framed on a sage Hearth background, a 20896px design-tool export downscaled to 2400px JPEG 100; replaced the original dashed-placeholder `.hw-cover` div, which was deleted from style.css 2026-06-12). Spine: Problem (VOC-grounded, expanded 2026-06-13 from a thin version: the layered pain — social trust → word of mouth runs out → communication grind → uncomparable quotes — plus a `.voc-quotes` block of 3 real interview quotes tagged Discovery/Transparency/Effort) → Insight (the "$250→$7,000 same-job quote spread" reframe, built as a real CSS graphic `.hw-spread`; body cites Interview #1, who described the product unprompted) → Solution (reshaped 2026-06-13 to a **TickerPulse-style conceptual block** — heading "An agent that scopes the job before it shops it" + a paragraph + a 3-step `.compare-table` (Scope / Match / Compare); no clips here) → **Final Design** (new `#final-design` section, added to the side-nav, that holds the 4 walkthrough clips in order: **define scope → one scope, every contractor (send) → find the contractor (system surfaces verified/recommended matches) → compare-&-approve (pick who to collaborate with)**; first clip's caption is the section's h2, the rest are `.deliverable` h4s, mirroring ticker's Final Design). The contractor-match beat was added 2026-06-12 after the user flagged it missing — it's the on-thesis trust step, mapping to VOC Finding 1 "Recommended by N Homewisers" social proof + Finding 4 "Verified by Homewise"; the separate quote-compare and approval-gate beats were merged into one. (Beat order was iterated live with the user 2026-06-12; the two shipped clips — homewise-scope.mp4, homewise-contractors.mp4 — lead as steps 1–2, the two placeholders follow.) → Hearth design system (a `hw-clip` **walkthrough clip** `images/homewise-hearth.mp4` of the live `/designsystem` page — replaced an earlier CSS color-swatch grid `.hw-hearth` 2026-06-12, whose CSS was deleted from style.css; the section keeps a short paragraph sourced from the repo's `DESIGN.md`: sans-only, shadowless, tri-role color story trust/caution/info, drift linter; the section also carries a `.hw-cta` button → the live `/designsystem` page) → **Collaboration** (`#collaboration`, in the side-nav, added 2026-06-13: the **code-first / no-Figma** story — the team worked closely with the PM and engineers, skipped Figma entirely, and designed in the codebase (a real React prototype on Hearth) instead of static mockups, then handed the dev team the repo itself so they could stand up their own front-end environment and integrate the real backend + APIs. Heading "We skipped Figma and built the design in code." Because of this, the project-details **Tools list deliberately omits Figma** (`React + Tailwind / Claude Code` only) — keep it that way for consistency. **Figma-MCP handoff paragraph added 2026-06-20:** a 2nd `<p>` says skipping Figma did not close the door on it — if the team needs a traditional Figma handoff, Chang can push the prototype from Claude Code into Figma via Figma's MCP server (kept capability-level in the prose, not naming the tool; grounded in research — `generate_figma_design`, the **Feb-17-2026 Claude-Code-exclusive** MCP tool that captures a running UI back into Figma as editable frames; the server is now bidirectional). HTML-only, no version bump. This does NOT contradict the Tools-omits-Figma rule: Figma still was not used to *build* Homewise; the MCP handoff is an on-demand option, not a tool in the stack. Carries a `.hw-cta` button → the repo `github.com/chang627627/homewise`. Note: linking the repo **inside a case study** like this is fine and intended — distinct from the work-card rule that *cards* never link to repos) → Reflection. **No separate Outcome section** (merged 2026-06-13): the outcome — an "A working prototype, not a mockup" closing `.deliverable` + the lone live-prototype CTA (`.hw-cta` → homewise-rust.vercel.app) — sits at the **end of the Final Design section**. (Copy consistently calls Homewise a *prototype*, not a product — it's what was handed to the front-end team to build the real product from; the repo is explicitly a no-backend prototype.) Final section order: Problem · Insight · Solution · **Process** · Final Design (ends on the outcome + live CTA) · Design System · Collaboration · Reflection. **Process section (`#process` + side-nav link, added 2026-06-19, sits after Solution):** the AI-era build-first story. Heading "I designed it by building it" + **one** paragraph (merged from two per user): the traditional discovery→mockups→handoff→build flow is dead, so Chang flipped the order — align with the PM, build, iterate on real feedback from team + users, with Claude Code as a thinking partner weighing each design exploration's pros/cons ("Less time making mockups, more time on the calls only a person can make"). Visualized by **two `.flow-chart` SVG blocks reusing Coffee Chat's `.flow-chart`/`.flow-svg`/`.flow-box`/`.flow-chart-tags` pattern (NO new CSS):** "The traditional process" (linear Discovery→Research→Mockups→Handoff→Build, ✕ tags) vs "How I work now" (Align with PM → Build → Feedback → Iterate, with a **right-angle, NOT curved** loop-back arrow Iterate→Build labeled "repeat until it is right", ✓ tags incl "Claude Code weighs each option"; the Build box is inline-highlighted brighter). **"chat with PM" was rejected as too casual → "Align with PM" (user) — keep it.** Closes with an inline credit to **Jenny Wen** (Head of Design for Claude at Anthropic), linked to her Lenny's Podcast episode "The design process is dead" (`lennysnewsletter.com/p/the-design-process-is-dead`); the inline `<a>` carries an inline `style` (color `var(--text)` + a 0.35-white underline) because case-study prose has no default inline-link affordance. **Mobile fix (applied 2026-06-19, supersedes the earlier "NOT yet fixed" caveat):** the SVG flow-charts scale to page width, so box labels dropped to ~3.3px on phones (same limitation as Coffee Chat's flow-charts). Fixed by wrapping each chart's `<svg>` in a `.flow-scroll` div and adding an inline `<style>` in homewise.html's `<head>` that, **on mobile (≤768px) only**, gives `#process ~ .flow-chart .flow-scroll` `overflow-x:auto` + `overflow-y:hidden` (the explicit `hidden` dodges the overflow-x-forces-overflow-y-auto gotcha) + a thin styled scrollbar, and holds `.flow-svg` at `min-width:800px`. Result: labels render ~9.2px and each chart scrolls horizontally **within its own strip** — the page itself does NOT overflow (verified), and the cut-off right edge + scrollbar thumb are the swipe affordance. **Desktop is untouched** (rules are media-gated, so `.flow-scroll` is a transparent passthrough div there and the SVG still fills the full card). **Homewise-scoped on purpose** (inline in homewise.html, NOT style.css), so Coffee Chat's shared `.flow-chart` is unaffected and still has the tiny-label issue. HTML-only — no style.css change, no version bump. **Process paragraph trimmed same day (per user "too long"): 144→102 words** — cut the prose that re-narrated the two charts' steps (the charts already show Discovery→…→Build and Align with PM→…→Iterate); kept the reframe, the "more time on the calls only a person can make" punchline, and a shortened Jenny Wen credit (dropped "at Anthropic"). Reflection carries two beats: a project insight ("Trust was the hard part, not the automation") + the Claude-Code-first-build/handoff takeaway. Content grounded in the repo's real VOC research (github.com/chang627627/homewise, `docs/` synthesis PDFs). **Clips complete (2026-06-12):** all 4 flow clips are live — `images/homewise-scope.mp4` ("Define the scope"), `images/homewise-contractors.mp4` ("One scope, every contractor"), `images/homewise-match.mp4` ("A match you can trust" — the 3-contractors-side-by-side matching screen with Verified/FLAGGED badges), `images/homewise-compare.mp4` ("Compare apples to apples, then you sign off" — the "Quotes are in" 3-bid same-scope comparison); no `.img-placeholder` boxes remain. A 5th `hw-clip`, `images/homewise-hearth.mp4`, sits in the Design System section (walkthrough of the live /designsystem page). Each live clip is wrapped in `<div class="hw-clip project-img-full">` containing the `<video loop muted playsinline preload="metadata">` plus a `.hw-clip-progress > .hw-clip-progress-bar` **playback progress bar** (thin 4px bar pinned to the video's bottom edge so viewers see start/end and position; the wrapper's `overflow:hidden` + radius clips it to the rounded corners). The inline script before the chat widget does two things per clip: drives the progress bar via rAF (tied to play/pause so it's smooth and resets cleanly on loop) and runs play.html's IntersectionObserver to defer play to the viewport. The `project-img-full` class on the **wrapper** gives the clip the same 24px caption→artifact / 96px artifact→next spacing as the case-study images (the `.img-placeholder` adjacency rules were added to style.css for the same reason). All 5 clips (4 flow + the Hearth walkthrough) use this exact `hw-clip` wrapper. **Clip conversion (no ffmpeg on this machine):** screen-recordings are H.264 Retina .mov (~3456px, ~40MB); transcode with Apple's `avconvert -s in.mov -o images/<name>.mp4 -p Preset1920x1080 --replace` (H.264, NOT the HEVC presets — poor browser support; → ~1920px, ~15MB, fast-start). Videos live in `images/`. **The home card links here** (`href="homewise.html"`) as of 2026-06-12, once all 5 clips landed (it previously pointed at the live demo). Homewise-specific CSS is the `hw-` block at the end of style.css; page chrome stays portfolio-dark while artifacts carry Hearth's warm palette. style.css bumped to v=115 on this page. **Enriched from the Demo Day pitch deck (2026-06-17, source `homewise-pitch.vercel.app` — the PM Accelerator AI Bootcamp deck for the same product):** added, without breaking the lean format — (a) a `.hw-stats` market-stat band in the Problem after `.voc-quotes` (label "Beyond our eight interviews"; 70% fear an unreliable contractor / 41% deceived by a provider / 33% delay repairs; source "Leaf Home / Morning Consult, 2025. HomeServe, 2025." — a dark panel, sibling of `.hw-spread`; the warm `#B98132` top-accent bars on each stat were **removed 2026-06-17** as meaningless decoration per the user, so the numbers stand alone; the `.hw-stats + .project-section { margin-top:96px }` rule re-asserts the Problem→Insight gap that `.voc-quotes + .project-section` used to give); (b) the trust split "14% trust review platforms, 61% word of mouth" woven into the Problem intro; (c) **[added then removed same day, 2026-06-17, per user — do NOT re-add]** a `.hw-flow` map (Photo → Scope → Match → Quotes → Booked) briefly opened Final Design above the first clip; the user deleted it (the four clips already walk the flow) and its CSS was removed; (d) verification specifics in the "match you can trust" caption (license, insurance, real permit history); (e) a Collaboration sentence naming what the prototype became (Claude vision, Shovels.ai permits, Twilio); (f) a Reflection beat "trust is a data problem as much as an AI one"; (g) sidebar engineers 5→7 (it was a 13-person team: 3 PM / 3 design / 7 eng — user kept the "Lead Product Designer" framing). **Deliberately skipped** to protect the lean format: the 3 pitch personas (David/Maya/Ray) and the full architecture diagram. The Insight copy was left as-is (already lands the symptom/cause point). style.css bumped v=160→v=161. **Problem + Solution rework + two videos (2026-06-17 — SUPERSEDES the stat-band "numbers stand alone", the 14%/61% "woven into the Problem intro", the `.hw-flow`, and the "All 5 clips" notes above):** (1) **Problem VOC → scannable pain-point cards** via a homewise-only modifier `.voc-quotes--pain` (so TickerPulse's shared `.voc-quotes` is untouched): each card leads with a white pain **headline** ("No way to know who to trust" / "No benchmark for a fair price" / "Comparing quotes is exhausting") and demotes the real interview quote below it to muted italic evidence (single-weight font, so the hierarchy is color+size, not bold). Intro lead-in changed "Three phrases" → "Three frustrations". (2) **Market stats are now THREE CARDS** (`.hw-stats` → `.hw-stat` styled exactly like the voc-quote cards: `var(--surface)` bg, 1px border, radius, 3-col grid, 44px figures). Iterated boxed-with-warm-`#B98132`-accent-bars → user called the bars "meaningless" (removed) and the box "ugly" → boxless editorial → **three cards** (final, per user "need three card for these"). Do NOT reintroduce the bars or the single box. (3) **Inline 14%/61% trust stat REMOVED** from the Problem prose (redundant with the surrounding word-of-mouth sentences; user: "either don't need the data or card it" → removed). (4) **Solution enriched** (grounded in the repo `github.com/chang627627/homewise`, whose README confirms a structured **state machine, not a chatbot**: Intake→Scope→Contractor compare→Quote compare→Booking→Completion): added a "two things make hiring hard, neither is the automation… so we built a structured workflow instead of a chatbot" rationale paragraph; refined the `.compare-table` to **4 stages — Scope / Match / Compare / Track** ("what the workflow does"); added a **"Why not just a marketplace"** contrast coda in `.solution-coda` (pulled to 44px top-margin via `.project-content .project-section.solution-coda` to outrank the `.compare-table + .project-section` 96px rule). **Trimmed 2026-06-18 (portfolio-audit pacing pass — shorten the runway to the Final Design clips):** the "two things" paragraph was cut from 4 sentences to 2 (kept the "structured workflow, not a chatbot" differentiator, dropped the table-overlapping recap), and the `.solution-coda` "Why not a marketplace" paragraph was **REMOVED** so the `.compare-table` now hands straight off to the Final Design clips. The `.solution-coda` CSS rule is now **unused** (left in style.css; restoring the coda is a one-line HTML re-add). The "What I did" bullets and the Problem intro were deliberately KEPT — on a close read they earn their length (distinct ownership beats / the layered-pain narrative), so the pacing fix was scoped to the Solution only. (5) **Final Design got a 5th flow clip** `images/homewise-track.mp4` ("One place, from booking to done", after Compare) — the **prototype's** dashboard status-tracking flow — so there are now **6 `hw-clip` videos (5 flow + the Hearth walkthrough)**. (6) **Collaboration got the live/backend demo** `images/homewise-live.mp4` — the dev team's **shipped build** (~4-min walkthrough), captioned "See it running for real", embedded **click-to-play** (`controls preload="none" poster="images/homewise-live-poster.jpg" playsinline`), NOT the looping `hw-clip` treatment, and **excluded from the clip auto-play script** (which targets `video[preload="metadata"]` only) so its 39MB never auto-loads. Poster grabbed via `qlmanage -t`. Its on-screen account/address are **fake demo data (confirmed by user)** — no real PII. **Deliberate split (per user):** the Track clip (prototype) lives in Final Design; the live demo (real backend) lives in Collaboration as the "what shipped" proof. **Provenance for honesty:** Chang built the front-end prototype + data structures in code; the dev team built the real product on it (front-end eng wired live data/APIs onto his structures, back-end eng built the backend) — so the live demo is his design, shipped. **Video transcode:** `avconvert -s in -o images/<name>.mp4 -p Preset1920x1080 --replace`; macOS Screen Recording filenames use a narrow no-break space before AM/PM, so match the source with a glob, not a literal path. style.css now at **v=167** across all 8 pages. **Copy polish 2026-06-27 (per user, HTML-only, no version bump — SUPERSEDES the Solution/Collaboration/Reflection wording above):** (1) **Solution** — the "structured workflow, not a chatbot" line now justifies the call AND claims ownership, and is **white-highlighted**: `<span style="color: var(--text)">A chatbot would leave them guessing at both, so I pushed for a structured workflow instead.</span>` (the white span pops against the `#888` `.project-section p` body; in user shorthand "highlight X" = make X white, NOT rewrite it). (2) **Collaboration is now ONE merged, shortened paragraph** (was two): code-first build + repo handoff + the Figma-MCP option live in a single `<p>`; the 2026-06-17 "(e)" sentence naming what the prototype became (Claude vision / Shovels.ai permits / Twilio) was **DELETED per user** — those tool names no longer appear anywhere on the page. (3) **Reflection shortened to ONE paragraph** (was two): keeps the trust-is-the-hard-part + trust-is-a-data-problem beats and the Claude-Code-build / "It was the first version of it" close; dropped the "were not features… earned the right to act" line and the redundant handoff recap. These edits came out of a multi-lens gap review of the case study (the user asked "what's missing"); the other flagged gaps — a real research artifact for the $250→$7,000 spread, and a qualitative "did it land" validation beat — were surfaced but NOT done (both need real content only Chang has; **declined to fabricate a usability-test homeowner quote** for the validation beat — fake research evidence on a hiring portfolio backfires if probed in an interview). **(4) Process section grounded to Homewise 2026-06-27 (review gap #4, option b — kept placement, added one sentence):** after the build-first method statement, added "Homewise especially called for this, because it is one connected workflow where a single change ripples through scope, matching, quotes, and tracking, so I had to build it and validate it early rather than trust static mocks that could not show whether the whole thing held together." Per the user's rationale (interdependent state-machine workflow → validate early by building, not isolated mocks). Converts Process from a generic "design process is dead" manifesto into a why-build-first-*here* argument; flow-charts + placement unchanged. The review's two unactioned options — move Process *after* Final Design (option a), or add the *agent's scope-generation/approval-gates can't be felt in a static mock* angle — were not taken; the connected-workflow rationale was the user's pick. **(5) Sub-heading scannability pass 2026-06-27 (per user):** two h2s reworded so each carries its own beat on a skim (SUPERSEDES the heading strings quoted above). Design System: "Hearth, the warmth at the center of every home" → **"Hearth, one warm system across every screen"** (the old metaphor was about the product's *domain*, didn't signal "design system" when skimmed — it was the one heading that failed the scan test). Process: "I designed it by building it" → **"I flipped the process and designed by building"** (elaborates the reframe and echoes the body's "I flipped the order"). The other ~10 headings were judged already-scannable and left as-is — they're insight-statements, not generic labels, so skimming the h2 stack alone tells the whole story. **(6) Process — PM/PRD collaboration beat added, then re-tightened 2026-06-27 (per user; SUPERSEDES item (4)'s quoted sentence and the 144→102-word note for the *current* Process paragraph text):** the user asked what real PM↔designer collaboration looks like (the PRD as the shared artifact) so it could be written into Process. Added a condensed PRD beat that grounds the otherwise-vague **"Align with PM"** flow box: *"Each loop starts from the PM's PRD, our shared intent: the problem, the users, and the metrics to move. The prototype exposes gaps the document cannot, which feed back into the PRD."* The bidirectional loop (prototype surfaces gaps → back into the PRD) is the deliberate sophistication, and it counters the traditional-chart's own "spec thrown over the wall" critique. Adding it pushed the paragraph to ~155 words; user said "way too long," so the **whole paragraph was rewritten to ~124 words**, cutting the parts the two flow-charts already show: "In 2026 that is backwards", the "build first, then iterate on real feedback, with Claude Code weighing each exploration" mechanics (= the "How I work now" chart's Build→Feedback→Iterate + "Claude Code weighs each option" tag), and the "and a build-and-feedback loop replaces it" tail on the Jenny Wen credit. New opener is "Engineering used to be the slow part…"; the connected-workflow sentence is now "Homewise especially needed this, since one change ripples through scope, matching, quotes, and tracking, so I validated it live." (reworded from item (4), same rationale). KEPT the "more time on the calls only a person can make" punchline + the Jenny Wen link. Flow-charts, the "Align with PM" box label, and placement all unchanged — the PRD lives in prose, not the SVG. **Committed + pushed to main (commit c5e3236) — Vercel auto-deploys.** HTML-only, no version bump. Takeaway: the PRD/PM-collaboration belongs in **Process** (it's the input to the build loop), carried in prose with the flow box untouched, and Process stays lean (~120 words). **Mentor-framework pass 2026-07-28 (HTML-only, style.css untouched, no version bump).** Audited against the same framework the deck was rebuilt to (see homewiseslide.html). Verdict: the page had no stakes claim, no beat saying the problem was HARD to design, and the word "principle" appeared zero times — yet all that material was already written, scattered. So the fix was resequencing, not new prose. Shipped: **(1) new `#complexity` section between Insight and Solution** ("Why this is hard" / h2 **promoted verbatim from the Reflection**, "Trust was the hard part, not the automation") holding 3 `.problem-card`s — a user problem (from the old Solution ¶2), a data problem (from the Reflection), one connected system (from Process). Reuses `.problem-grid`/`.problem-card`, so **zero new CSS**; its `+ .project-section { margin-top:96px }` rule supplies the gap to Solution. Side-nav gained a matching `#complexity` link (now 9 links). Process and Reflection were shortened so they stop introducing beats the new section now owns, and **Reflection's h2 became "The design was not a picture of the product"** (lifted from its own unheaded second beat) since its old h2 moved. **(2) The 70/41/33 `.hw-stats` band now LEADS the Problem**, relabelled from "Beyond our eight interviews" (which demoted it to an appendix) to the claim "Most homeowners already expect this to go wrong", so the interviews read as the explanation of a market-scale problem. **(3) Workflow de-duplicated**: `.compare-table`'s column is now "Why the stage exists" with each cell leading with the failure the stage prevents; it previously shared literal strings with captions 400px below. Outcome lost its workflow restatement, and the live-demo caption states the runtime ("four minutes" — measured 4:06 off `videoWidth`-style metadata, not guessed). **(4) "What I did" bullet 1 stopped spoiling the Insight** (dropped "including the reframe from price to scope", which named the Insight's payload 42 lines early). **Three self-inflicted regressions caught and fixed during the pass, all worth remembering:** (a) **NEVER nest an `hw-` block inside a `.project-section`** — the generic `.project-section p { font-size:16px; margin:0 }` is specificity (0,1,1) and outranks `.hw-stats-label`/`.hw-stats-source` (0,1,0), which silently flattened them to 16px with both 24px margins zeroed. `.hw-stats` must stay a top-level sibling; the prose after it carries an inline `margin-top:40px` to beat `.hw-stats + .project-section { margin-top:96px }`. (b) applying several recommendations independently put **"ratings can be gamed" on the page four times** (Problem setup + principle + table cell + Match caption) — the payoff now lands once, in the Match caption, at the screen that resolves it. (c) the audit's rec-2 **named principles block was built then DELETED by the user** ("feel too wordy"): the three rules (structured/verified/recommended, not X) now live folded into the Solution paragraph as prose — "Every step stays checkable: the scope is editable before it goes out, the trust signals are public records rather than ratings, and the agent recommends but never books." **Do NOT re-add a stated-principles list to this page**; the deck keeps its Design-statement slide, and that divergence is intended. **Hero sub reworked 2026-07-28 (audit rec 7, per user):** now "AI command center for homeowners. A third of them put off repairs rather than face hiring someone. It scopes the job, compares the bids, and waits for your approval." The opening clause stays verbatim per the card-continuity rule; the tail leads with the **stake** (the sourced 33% stat) instead of opening on mechanics that echoed the home card. **Deviation from the audit, deliberate:** the audit specified dropping the mechanics entirely, on the stated grounds that they were "restated at line 198" — but the SAME session's Solution trim deleted that sentence, so `defined scope of work` now appears zero times on the page and **homewise.html has no Overview section**, making the hero sub the only plain statement of what the product does. A naked swap would have left the page never saying it. So a compressed mechanic was kept. **The four meta/og/twitter descriptions were deliberately NOT changed** — they are search and social-preview copy, where a bare stat without context is worse than a plain description of the product. **General lesson: an audit recommendation's rationale can be invalidated by a later edit in the same session; re-check the premise before applying a queued recommendation.** **HITL naming pass 2026-07-31 (per user, from a Substack HITL-design essay):** the page and deck now NAME the pattern the product already implements — "human-in-the-loop" appears exactly twice on the site: in the Solution's white-highlighted sentence (recorded frozen in the audit kills above; **amended with explicit user approval** to "structured, human-in-the-loop workflow") and in the deck's The-human-signs-off card ("Human in the loop, never an autopilot default"). The "Every step stays checkable" clause was recut to the essay's two-gate frame — "you approve the scope before it shops and the hire before it books" (gate-before-the-run + gate-after-the-draft; Homewise already implemented both gates, the page just never claimed the structure). The essay's checklists, autonomy-ladder taxonomy, and edit-rate metric were deliberately NOT imported (no data for the metric — fabrication ban). **A separate HITL section was considered and REJECTED** (user asked 2026-07-31): it is the same shape as the deleted named-principles list, would make an 11th section against the settled 10-section spine, and would centralize an argument that currently pays off at the exact screens proving it. Also note `.project-list` is wrong for any inline rule set here — its `li { padding-left:20px }` pushes text 20px out of alignment with the section's body copy. Body copy ended at ~1432 words (from 1397). **Final Design tradeoff blocks (2026-08-11, per user "do exactly the slides did"; style.css v=229→v=230 across all 9 pages):** each of the five Final Design clips is followed by a `.hw-trade` block in the DECK'S exact grammar — "The tradeoff" eyebrow (label token), two rule-topped `.hw-trade-cols` columns, `.hw-tick--bad/--good` circles in **Hearth pastels** (ember `#F5E4CE`/`#8E601E`, sage `#E2E8DC`/`#4F6942` — per the hw-artifacts-carry-Hearth convention; hairlines stay the page's white-alpha), `.hw-trade-shot` evidence crop, receipt below; mobile stacks to 1 column. Copy is deck-verbatim with ONE deliberate deviation: the match ✓ says "its sources sitting right there in the rows" without re-enumerating license/insurance/permits, because the page's own caption directly above already lists them (the deck's body doesn't). A first port using `.iter-example` (pill-in-figcaption format) was built and replaced the same day per user — the page must mirror the deck's format, not adapt it. **The match caption also dropped "a match confidence"** (honesty fix: the metric exists only in an orphaned component at HEAD and the clip never shows one), and the track caption is "One place, from booking to done" with the original body (a briefly-shipped "from first quote to done" widening was reverted when the user caught its tradeoff arguing about the pre-booking quote-wait screen under the post-booking dashboard clip).
- `404.html` — **Custom 404 page (added 2026-08-01, Mobbin interaction/visual research pass; Vercel serves `/404.html` automatically on static deployments).** Site-grammar error page: standard nav, `.hero` + `.hero-orbs--g2` gradient, plain-text h1 "Lost in the fog." (callback to the old © line; NO per-glyph machinery — a bare h1 inherits the hero type styles fine), sub "This page doesn't exist. The work does.", two `.status-tag` pills (Back to the work → /index.html, Say hello → mailto) in a page-local `.notfound-tags` flex row, and the same gated Lightfall+tilt script as home minus the type machinery. **All asset/nav hrefs are ABSOLUTE ("/style.css?v=224", "/index.html") on purpose — a 404 renders at arbitrary broken URLs, so relative paths would 404 too.** `noindex`, NOT in sitemap.xml, deliberately NO chat widget and NO footer block (single-viewport utility page). GA included per convention.
- `style.css` — Shared styles across all pages
- `api/chat.js` — Vercel serverless function behind the chat widget (Claude Haiku 4.5). Its `SYSTEM_PROMPT` embeds the resume + a PORTFOLIO PROJECTS list. **When the resume, experience, awards, or project lineup change, update this prompt too** — it silently served year-old facts until the 2026-06-11 refresh. Not runnable on the local python preview (`/api/chat` 404s locally); test against production after deploy. **The 2026-06-15 TickerPulse open thread was CLOSED 2026-07-26** (improvement-research P2): its PORTFOLIO entry now says the wireframes are in a Play-page carousel and explicitly tells the model not to direct visitors to a case-study page. **Endpoint hardened the same day:** CORS wildcard replaced with an origin allowlist (www.chang-mou.com / chang-mou.com / chang-mou-portfolio.vercel.app; requests with a foreign Origin get 403), message capped at 2000 chars, history sanitized (user/assistant roles only, 2000-char cap per item — blocks role injection), and a best-effort in-memory rate limit (20 req / 5 min per IP per warm instance, `globalThis.__chatRate`).

## Design Decisions
- **Font:** Neue Montreal (via Fontshare CDN) — single font, weight 400 (regular) everywhere
- **Color:** Dark mode (#0a0a0a background, #1a1a1a surface, #f0f0f0 text, #888 muted). **Accent blue:** `#0094FF` (matches the blue in NOVA's AR UI — sampled from `images/nova-hud-text-example.png`). Used for legend circles, the HUD Text "Selected" image border, and Solution Evaluation Matrix highlighted-row accent + tint. When adding new blue accents, reuse `#0094FF` (or `rgba(0, 148, 255, ...)` for transparent variants) so the page stays unified.
- **Blue scope:** `#0094FF` is the **NOVA case study's** accent (it matches the AR UI). It should NOT bleed into page chrome that lives across the site — e.g. the side-nav active-state indicator uses `var(--text)` (white) instead of the blue, so the chrome stays neutral while the case-study content keeps its own brand voice.
- **Motion tokens (added 2026-07-26, UI-craft review P8):** `--ease-out: cubic-bezier(0.16,1,0.3,1)` (the curve the site already used on its best transitions), `--dur-fast: 100ms`, `--dur-base: 160ms`, `--dur-slow: 400ms`. **Use these for any new transition.** The governing idea is the **acknowledgment/scenery split**: a hover must ANSWER the pointer in ~160ms while decorative travel stays slow. Applied to `.project-card` — border-color + box-shadow dropped **400ms → 160ms** while `transform`/`opacity` stay 400ms and `.project-img img` keeps its 800ms glide (verified computed: `0.4s, 0.16s, 0.16s, 0.4s`). **Do NOT re-unify the card's durations** — the asymmetry is the point. Also converted: nav/footer/about/play link underlines, `.play-card`, `.carousel-arrow`, `.hamburger`, carousel dots (~21 token usages). **Deliberately NOT swept across the ~40 remaining `0.3s/0.2s ease` transitions** — at those durations the curve swap is imperceptible and would touch the chat panel, lightbox and mobile overlay for no visible gain; convert them opportunistically when editing those components. `.impact-link::after` was left on plain `ease` because it is case-study-only (out of the review's scope).
- **Radius:** 8px globally via `--radius` CSS variable
- **Slide decks are LIGHT and share ONE system (2026-08-03, per user "i think we need to slides light mode" → chose "Both decks, one shared light system"). This SUPERSEDES the palette-is-per-project rule below — do not re-split the palettes.** Coffee Chat was converted from the site's dark canvas to **Hearth's ramp, which homewiseslide already used**, so the two decks are now literally value-identical: canvas `#F6F4EF`, card `#FFFFFF`, deep well `#EEEAE1`, ink `#1A1A17` / `#6B6B65` / `#A8A8A1`, hairlines `#E7E5DE` / `#CFCEC7` / `#A8A8A1`, and shared flag colours (good = sage `#4F6942` on `#E2E8DC`, bad = ember `#8E601E` on `#F5E4CE` — Homewise's `.tick--*` convention, now Coffee's `.flow-ic--*`/`.tag--*` too). **The ONLY per-deck difference is `--accent`** (Coffee `#2456D6`, a deepened Litespace blue that holds contrast on cream; Homewise sage) plus Coffee's lavender `--plate: #d0d1fd`. Coffee kept its old token NAMES (`--bg`/`--text`/`--line`) and just repointed the values, which is why the diff is small; Homewise needed no change at all. **Four structural rules that make a light deck work, all learned the hard way here:** (1) **the letterbox must stay ink** — a new `--letterbox: #1A1A17` backs `html/body/.deck` while `.slide` is cream, or the light fixed chrome (brand, nav circles) vanishes into a cream frame. (2) **The two bookends stay INK** — `.slide--title, .slide--end { background: var(--letterbox) }`; the warm→blue gradient is a *wash on the dark base*, and without that base the slide inherits cream and the white wordmark washes out completely (this shipped broken for one render and is the most visible failure mode). (3) **The mid-deck peak inverts**: Coffee's Reflection went from `.slide--lav` (light-on-dark-deck) to `.slide--ink` (dark-on-light-deck), matching homewiseslide's Outcome slide — a light deck gets one dark peak. (4) **Flow-chart SVGs carry INLINE white** (`stroke="rgba(255,255,255,0.4)"` on 18 arrows/connectors plus white marker fills) which is invisible on cream — those attributes had to be recoloured in the markup, not just the CSS. **(5) EVERY fixed chrome element lives in the ink letterbox, so it must use `--on-dark*`, never `--text`/`--text-muted`** — `.deck-brand`, `.deck-hint` (+ its `kbd`), `.navbtn` (all three circles), `.navbtn--comment.on`, `.cmt-count` and `.cmt-bar`. This one is invisible in a contact sheet: the nav circles still drew their light borders, so they looked present, but the chevron/bubble SVGs are `stroke: currentColor` and inherited ink — **three empty rings on a dark letterbox**, which is exactly how the user reported it. **Method note: the first leftover-scan only grepped for WHITE literals and therefore missed the inverse class of bug entirely — dark surfaces that now need to be light.** A second scan for dark literals found `.cmt-pop` (`rgba(20,20,20,0.97)`) and `.pin` (`#1c1c1c` with `color: var(--text)`), i.e. ink text on an ink popover and an invisible pin number. **Scan BOTH directions after any palette flip.** Also easy to miss: `.cta` used `var(--text)` but only ever renders on the ink sign-off (needs `--on-dark`), and `.cmt-ava` was a white tint inside a popover that is now white. Verified end to end: comment mode opens a white card with ink text and cream inputs, and all three nav icons read.
- **Slide-deck visual grammar (established 2026-07-31, user-approved on homewiseslide's deck.gallery reskin — the LAYOUT grammar still applies to any future deck; the palette-is-per-project clause is superseded by the shared light system above):** editorial print, not boxed cards. Source patterns: deck.gallery's Geist*Studio Impact Report (stat rows + in-slide document chrome), Reece Evans Portfolio (metadata-columns cover + giant wordmark), Lefty (persistent brand footer). The PALETTE is per-project (homewiseslide wears Hearth; another deck would wear its own case study's tokens) — the grammar below is what transfers. (1) **Every slide is a framed page**: in-slide header row (section label left, static "<Project> · Case study" right via `.s-label::after`, full-width hairline under) + JS-injected folio footer (hairline, "A case study by Chang Mou" left, "NN / NN" right). No separate letterbox counter — the folio carries the number. (2) **Top/bottom composition**: title zone anchored top, artifact block pushed to the bottom via `.push { margin-top:auto; padding-top:44px }`, folio closing the frame. Never leave content floating in the top half with a void below — that was the exact "ugly" the reskin fixed. Full-height splits get `flex: 1 1 0; min-height: 0` so they center between header and folio. (3) **No boxed cards**: stats are full-width hairline ROWS (oversized numeral ~112px left, annotation pinned to a right ~620px column); triads/stages are rule-topped COLUMNS with a tiny accent-color index (01/02/03), no background fills; hero figures (the money numbers) run 96–128px. (4) **Extreme type contrast**: ~13.5px/0.2em caps labels against 76px titles (54px compact variant for artifact-heavy slides; 46px in media slides' 2-col `.media-head` where the annotation sits right so the artifact keeps its height), ~96px ink statement slides, 170–300px display on the bookends. Single-weight rule applies as everywhere (400/500 only, explicit `font-weight` on every heading). (5) **Cover + sign-off are Reece-style dark bookends**: tiny metadata columns top (Role/Team/Timeline/Built with), giant `white-space:nowrap` wordmark bottom-left + small description bottom-right — SIZE THE PAIR TOGETHER (wordmark px + sub max-width; 300px/520px overflowed, 280px/430px fits), brand texture (dot grid) reserved for the two bookends only. (6) **One mid-deck ink statement slide** at the payoff beat (flat dark + quiet accent wash, no texture) for rhythm. (7) **One accent color** for chrome (solid progress bar; no decorative multi-color ramps). (8) Radius only on media frames (~8px) and pills — layout structure is drawn with hairlines, never rounded boxes.
- **Premium-research quick-win pass (2026-07-31, IN WORKTREE FOR USER JUDGMENT — not yet pushed; from a 5-agent research workflow over craftwork.design's AI category + Linear/Vercel/Stripe/Anthropic/Raycast benchmarks + a code audit + 2026 trend criticism):** style.css **v=221→v=222** across all 8 pages. (1) **Mono metadata tier — TRIED AND REJECTED (2026-08-01, user: "I do not like the monospace change"; do NOT re-propose a second typeface).** A `--font-mono` system-stack tier on chips/eyebrows/hero-meta shipped to the worktree for one day and was fully reverted — the site stays single-face Neue Montreal everywhere. The research case for it (Mistral/Hume/Vercel mono micro-labels) was heard and overruled; the single-font discipline IS the identity. (2) **Unified label token** 13px / 0.12em / uppercase / `--text-muted` / 400 across the five drifted eyebrow rules (`.hero-label`, `.page-hero .hero-label`, `.section-title` 14→13, `.section-meta`, `.about-gallery .section-label` — whose impossible `font-weight: 300` was fixed to 400 — and `.project-section .section-label` 14→13). **NOTE: `.hero-label`/`.page-hero .hero-label`/`.section-title`/`.about-gallery .section-label` are ORPHAN rules — no page instantiates them anymore (the About rework and Play redesign retired the markup); the unification still applied for hygiene. The label token's live surfaces are home (SELECTED WORK via `.section-meta`) and the four case studies (`.project-section .section-label`).** (3) **Type-ramp middle**: `.project-section h2` + `.about-block h3` 22→**28px** with -0.02em (`.deliverable h4` stays 22 so sections have a two-level ladder); `.project-hero-sub` 16/muted→**18px/--text-soft** + max-width 680px; `.project-info h3` 18→20px/-0.01em; `.project-year` 16→14px — verified all 8 cards keep the uniform 21px chip gapBelow and no h2 wraps. (4) **Hero blur 56→32px** — closes the documented hero-vividness open thread; verified the warm/blue seam still lands under headline line 1 and no blob banding. (5) **Hero sub + proof, final shape (iterated 2026-07-31→08-01 per user; supersedes the hero-sub copy quoted in the Hero bullet below):** ONE merged paragraph — "I design intuitive products across SaaS, emerging tech, and social impact, and I prototype in real code, this site included. iF & Red Dot Award winner with 3.5+ years of product design, based in San Francisco." (both protected phrases intact; the proof is a SENTENCE, not a middot fragment list — user 2026-08-01: "do not use x.y.z, use sentence". The interim separate `.hero-meta` line was tried at 13px, "too small", 15px, then merged back per user) — followed by the `.status-tag` mailto pill alone. **A "Built with Claude Code" `.hero-tag` glass chip beside the pill was tried and DELETED same day (2026-08-01, per user); do not re-add a maker's-mark element to the hero — the claim lives in the sub's own words and the footer © line.** (6) **Status pill is a live `<a>` mailto** now (hover border-brighten on the motion tokens, `.arrow` ↗) — distinct from the removed nav CTA because it activates an existing element. (7) **Footer info line — ADDED THEN DELETED (2026-08-01 per user); do not re-add.** Its availability copy lives in the hero pill; the footer © line keeps "Built with Claude Code" (intentional echo of the hero tag — it is a signature). (8) Decks got `text-wrap: balance` on `.s-title` (they never received the site-wide balance pass; their `.title-h1` is nowrap so balance is moot there). **Researched but NOT yet built (need explicit go-ahead): home-page testimonial band, coffee/NOVA stat-grid→hairline-row migration (homewise's 3-card band is settled and excluded), an owned Mercor destination page, case-study prose measure cap (~720px, flag-first). Rejected by the research with reasons recorded in the session: featured/spanning card, serif display mixing, 3D/WebGL hero, fake terminal chips, light-mode toggle, tinted neutrals, third ambient gradient surface.**
- **Mobbin vibe-audit pass (2026-08-01, per user "DO 2,3,4,5" from a 4-surface Mobbin-reference workflow; verdict was 4/5-ish everywhere — "amplitude-and-content gaps inside a sound system"):** style.css **v=222→v=223** across all 8 pages. (2) `.project-info h3` 20→**21px**/-0.015em (the audit's "18px" premise was stale — the premium pass had already set 20; this lands at the approved upper bound; all 8 cards verified still at the uniform 21px chip gapBelow). (3) **`.hw-spread-fig` 30→72px** + tabular-nums, and `--hi` ($7,000) now **ember #D9A461** with #B98132 small — ports the homewiseslide display-scale money treatment to homewise.html; panel/track/dots unchanged. (4) **About testimonials — featured display quote TRIED AND REVERTED (2026-08-01: Isabel at 30px → "too big" → 24px → switched to Zeeshan → user "I do not like this featured quote. maybe go back to three cards"). Back to the original THREE equal `.testimonial-card`s (Zeeshan, Dylan, Isabel); the `.testimonial-feature` CSS was deleted; do NOT re-add a featured-quote treatment.** Kept from the same action: **all seven About h3 headings (Background/Experience/Education/Recognition/People I've worked with/Books/Podcasts) converted to `.section-meta` caps eyebrows** (NO `.section-rule` hairline — added then removed same day per user: the trailing rule is home's SELECTED WORK treatment, and About should match the case studies' bare-label grammar instead; scoped `margin-bottom: 28px` override vs home's 48). `.about-block h3`/`.testimonials h3`/`.media-section h3` rules are now unused (left in CSS). (5) **Hero sub cut to ONE sentence**: "I design intuitive products across SaaS, emerging tech, and social impact, and I prototype in real code, this site included." — the awards/tenure/location sentence REMOVED per the audit (card badges carry award proof; supersedes the merged two-sentence sub recorded in the premium-research bullet). **A first-clause rewrite was considered and DECLINED 2026-08-01** — the user asked whether the line was too generic, was offered trust-thesis variants ("AI products people actually trust" / "from home repairs to bedtime stories"), and chose to keep the current version ("better not change. current version is safe"). Do not re-propose hero-sub rewrites unprompted; the protected phrases rule stands. **Audit action 1 (Mercor eval-work cover artifact) is DEAD — the Mercor work is under NDA (2026-08-01, per user "do not talk about mercor cuz it is under NDA"). Do NOT suggest Mercor cover artifacts, eval-work write-ups, or an owned Mercor page; the card stays wordmark-on-white linking to mercor.com/mission/ as-is.** The audit's 10 dismissed findings (nav glass, pill CTA-ification, chip caps, Say-hello brightness, scroll-CTA runway, footer contrast, etc.) are recorded in the session as settled-or-noise; do not re-raise without new evidence.
- **Interaction six-pack (2026-08-01, per user "do rest" from the interaction-quality audit — a 3-agent workflow: site inventory vs Linear/Vercel/Raycast/Stripe/Resend micro-interaction benchmarks; verdict "pointer-and-scroll layer at or above the bar, gaps concentrated in app-like/async moments"):** style.css **v=223→v=224** across all 8 pages. (1) **`.chat-panel` opens/closes like the lightbox** — motion-gated longhand `transition-property: opacity, transform, display` + `transition-behavior: allow-discrete` + `@starting-style`, enter 240ms/exit 160ms on the tokens; reduced motion keeps the instant swap. (2) **Chat typing indicator is three staggered blinking dots** (`chatBlink` 1.2s, delays 0/0.2/0.4s, static under reduced motion) — **the widget JS line `typing.textContent = '...'` became `typing.innerHTML = '<span></span>×3'` in ALL 8 pages' pasted widget blocks, so the documented `dc03dc69…` md5 for the coffee/homewise/ticker/nova block is STALE** (those four are still byte-identical to each other; pollen is still the odd one out). (3) **Global `::selection`**: `rgba(240,240,240,0.92)` on `#0a0a0a` — the white-inversion form of chrome-stays-neutral; NEVER #0094FF. (4) **Carousel**: entering `.carousel-slide.active` fades in via motion-gated `@starting-style` (exit stays an instant cut under the fade), and `@media (hover:hover) .carousel-dot:hover { opacity: 0.6 }` finally drives the transition the dots declared. (5) **Parity fixes**: `.back-to-top:active` joined the scale(0.96) press list; `.chat-toggle:hover` gated behind `(hover:hover)` (was touch-sticky); `.status-tag:hover .arrow` nudges translate(2px,-2px) like its footer/play siblings; `.side-nav-link` + `.chat-toggle` transitions moved onto `--dur-base`/`--ease-out` per the convert-opportunistically trigger. (6) **`.chat-messages` scrollbar** styled as the slim neutral floating pill (`scrollbar-width: thin` + WebKit content-box-clipped 999px thumb, 0.22→0.35 on hover). **Benchmark patterns deliberately NOT adopted (recorded, do not re-raise): Linear's instant-navigation view-transition kill, Raycast's overshoot press curve, animated/layered focus rings, Stripe's opt-in motion-token refactor, kbd shortcut chips.** **Case-study end navigation ("Next case study" band) was REJECTED by the user 2026-08-01 — several work cards land on LIVE external sites, so a next-project chain breaks; do not re-propose.** The coffee/NOVA stat-row migration remains the one queued unbuilt item from the research threads.
- **Animation audit pass (2026-08-02, per user, from Emil Kowalski's `improve-animations` skill installed via `npx skills add`; 4 read-only category agents produced 44 findings, each verified at its cited line before action; user picked findings 1-5):** style.css **v=224 to v=225** across all 8 pages **+ 404.html** (which the v=224 six-pack had missed - **remember 404.html when bumping; it is the 9th page**). (1) **The "Say hello" sweep was fighting its own stylesheet on all 8 pages** - `.footer-big-text .letter` declares `transition: opacity 0.3s ease` (style.css:1742) while the rAF loop writes that same property every frame, so the cursor spotlight rendered a ~150ms-lagged smear, and `textShadow` (NOT in the transition list) ran ahead of the glow it is paired with. Fixed with `letters.forEach(l => l.style.transition = 'none')` at the top of the sweep block - **the identical fix, with a comment explaining the identical reason, already existed 140 lines above on the scroll-CTA** (index.html:464); the sweep simply never got it. Verified: all 9 letters now change per frame and sit in the documented 0.37-0.53 idle band. **This edits the 8-page pasted sweep block, so its md5 changed again.** (2) **Reduced motion now suppresses smooth scrolling**: `@media (prefers-reduced-motion: reduce) { html { scroll-behavior: auto } }`, plus a `scrollBehavior()` helper in all 5 case studies because their 10 JS `window.scrollTo({behavior:'smooth'})` call sites **override the CSS property** and would otherwise still fling a motion-sensitive reader through thousands of pixels. (3) **The About photo-deck rAF is now IntersectionObserver-gated** (about.html) - it was the ONLY ungated loop on the site, running 60fps with 24 `getBoundingClientRect()` calls while the visitor read 4000px below. **GOTCHA worth keeping: the callback must read `es[es.length - 1]`, not `es[0]`** - a fast scroll batches several entries into one callback and `es[0]` is the OLDEST, which left the loop stopped while the deck was on screen (cost a real debugging cycle; the footer-sweep observer uses `es[0]` and works only because human scrolling rarely batches). (4) **Four `:hover` moves gated behind `@media (hover: hover)`** - `.logo span`, `.footer-links .arrow`, `.play-link .arrow`, and `.status-tag .arrow`; **the last one was a regression introduced by this session's own six-pack**, which added the nudge without the gate its `.project-card .project-arrow` twin has. (5) **`.project-card::before` glow 300ms bare `ease` to `--dur-base`/`--ease-out`**, so one hover no longer resolves on two clocks (border/shadow answered in 160ms while the glow took 300). **Findings deliberately NOT taken:** the decks' 620ms slide travel (an agent called it a HIGH "no animation on keyboard actions" violation - rejected, slide transitions ARE the medium in presentation software; the narrower real issue is the reveal stagger adding ~550ms and Home/End sweeping past blank slides), the "Say hello" hide-on-scroll-up (documented deliberate), the blanket 0.3s/0.2s-to-token sweep (documented as imperceptible, convert opportunistically), and `bounceDown` (orphan CSS, zero markup references). **Still open from this audit, ranked:** footer glow writes `height` from a raw scroll listener on 8 pages (index.html:697); the chat toggle's 8s conic border is an ungated paint animation on a fixed always-visible element; five underlines animate `width` where `scaleX` is pixel-identical; the mobile menu is the one overlay without the lightbox/chat `@starting-style` treatment; the hero entrance changes easing family mid-sequence (h1 on `--ease-out`, sub+pill on bare `ease`); `--ease-out` is re-typed as a literal 4x inside the file that declares it; and the chat reply arrives as a hard cut after the typing dots.
- **Tablet-band side rail (fixed 2026-07-26, UI-craft review P1):** `.about-vertical` / `.testimonials` / `.media-section` now carry `padding: 0 24px` **unconditionally** with `max-width: calc(var(--max-width) + 48px)`. The 24px rail used to live only inside the `@media (max-width:768px)` block, so from **769px to ~1007px** — exactly iPad portrait (810/820/834) — the viewport was too wide for the mobile padding and too narrow for the 960px cap to leave any margin, and every element (headings, bio, testimonials, media grid) computed `left:0 right:<viewport>`, i.e. text touching both screen edges. The `+48px` on max-width keeps the desktop content measure at exactly 960px, so ≥1008px renders byte-identically (verified: content 240→1200 and all 17 `.exp-date` cells still terminate at x=1200). **Any new full-width About-style section needs the same pair.** Companion fix: `@media (max-width:1135px) { .chat-toggle{bottom:96px} .chat-panel{bottom:156px} }` — below 1136px the content column reaches into the fixed chat button's lane (button owns the rightmost 88px; content right edge = viewport/2+480), so at `bottom:220px` it sat mid-screen and clipped the Experience date column. **96px, NOT 24px:** the footer links row occupies the bottom 32–54px and at 24px the button covered the "X" link completely (`elementFromPoint` returned the button — the link was unclickable); 96px clears it by ~42px and only overlaps the `pointer-events:none` watermark.
- **Max-width:** 960px for case study content sections (coffee/pollen/ticker). Home work grid and Play grid are 1344px for a wider workshop feel.
- **Case-study cover + hero spacing (redesigned 2026-06-20):** homewise/pollen/nova/coffee now open with a **contained, rounded cover** via the shared **`.case-cover`** class in style.css (`display:block; width:100%; max-width:var(--max-width)` = 960px; `margin:147px auto 0`; `border-radius:var(--radius)`). It sits **below the floating nav (not under it)** with a **symmetric 72px gap** above (nav→image) and below (image→heading): the `147px` top margin = nav bottom (~75px) + 72px, and 72px is `.project-hero`'s `padding-top` (image→heading). The cover `<img>` is still a direct `<body>` child ABOVE `.project-hero`, so the 72px hero `padding-top` still works (the cover supplies the top content; if a case study ever ships WITHOUT a cover, give the heading ~160px nav-clearance instead). **Mobile (≤768px):** `.case-cover` insets to `width:calc(100% - 48px)` so it lines up with the text — the cover is a body-level element *outside* `.project-content`/`.project-hero`, which get `padding:0 24px` on mobile, so `calc(100% - 48px)` reproduces that same 24px side inset (without it the cover bled edge-to-edge while the text was inset — the bug that prompted this). **TickerPulse is the lone exception** — still the original full-bleed cover (`width:100%`, edge-to-edge, sharp corners), as the unlinked page it was not converted. **History (2026-06-20):** covers were originally full-bleed with the nav floating OVER them; iterated constrain-to-960px → image-below-intro → swap-with-meta → settled on **image-at-top, below the nav, symmetric gaps**. The per-page inline cover styles were consolidated into `.case-cover` in the same pass. style.css bumped **v=171→v=172** across all 8 pages.
- **Nav wordmark + back affordance — considered and DECLINED 2026-08-01 (per user):** two ideas were weighed and dropped in one conversation: (a) a "← Chang Mou" / back-arrow logo on case-study pages (the decks' `.deck-brand` precedent does NOT transfer — decks have no other nav, so an arrow can only mean exit; in the full nav pill it conflates action with identity and is redundant with the labeled Work link), and (b) distinguishing the wordmark (period, monogram, weight) after the user asked if plain "Chang Mou" looks too generic — verdict: plain-name wordmarks are the premium personal-site convention and chrome stays deliberately neutral; the one sanctioned upgrade if ever revisited is the house-voice terminal period ("Chang Mou."), nothing bigger. User closed with "current is good, no need to change" — do not re-propose header/wordmark changes unprompted.
- **Nav link contrast (fixed 2026-07-26, UI-craft review P7):** `.nav-links` colour moved `--text-muted` (#888) → **`--text-soft` (#a0a0a0)**. At 13px over the animated hero gradient the muted grey measured **3.44:1 worst-case** across the irisFlow cycle (sampled 6 frames × 3 links from rendered pixels) — below WCAG AA's 4.5:1 for normal text. #a0a0a0 measures **4.67:1** there and ~7.6:1 on the dark pages. Deliberately not brighter (0.72 white would hit 7.13:1) so the inactive links stay clearly secondary to the white `.nav-active` link. Note this is shared chrome, so case-study navs get the same strictly-better contrast. **The audit's companion claim — that `.footer-links`/`.footer-copy` fail AA on the footer's amber band (3.40:1 / 2.99:1) — did NOT reproduce and was NOT acted on:** it reasoned from the CSS gradient stops (amber `rgba(255,190,35,0.64)` at 88%/100%) but the links actually render over the *red-orange* part of the gradient, sampled `rgb(160,62–67,38)` at every viewport tested (1440×700/900/1200/1600, 1024×768), giving **5.61–5.81:1 for links and 4.64–4.78:1 for the © line — both pass**. The proposed dark scrim on `.footer-bottom` would have dimmed the footer for nothing. **Lesson: compute contrast from rendered pixels, not from stacked CSS colour values.**
- **Nav:** Floating pill, 700px wide (narrowed from 760px 2026-06-20 per user — they wanted the removed scroll-condense's "after-shrink" 700px size kept static, with the lighter "before-shrink" glass opacity `rgba(18,18,20,0.72)`), centered. Links: Work | Play | About | Resume (**the "Get in touch" CTA was removed site-wide 2026-06-20** — redundant with the footer "Say hello"; the `.nav-cta` CSS rule is now unused/harmless). **"Specular Glass" treatment (2026-06-20, chosen via a 5-concept multi-agent pass + 3-judge panel):** the flat `rgba(20,20,20,0.85)` glass became a layered top-lit fill (`linear-gradient(180deg, rgba(255,255,255,0.025), transparent 55%)` over `rgba(18,18,20,0.72)`) + `backdrop-filter: blur(28px) saturate(1.4)` (the `saturate` is the 2026 glass tell) + a box-shadow stack (inset specular lip + inset hairline + lift + contact) + a bright-top gradient hairline ring via `nav::before` (mask-composite `exclude`/`xor`) + an opaque `@supports not (backdrop-filter)` fallback. **Active "you are here" tick:** `.nav-links a.nav-active::after { width:100% }` — a persistent 1px white underline under the current page (reuses the existing hover-underline `::after`; CSS-only, no JS, static per-page — the view-transition crossfade handles page-to-page). **Neutral on purpose** (white/grey/glass only — no NOVA blue, no hero gradient; chrome stays neutral). **Mobile:** nav underlines hidden in the full-screen overlay (`.nav-links a::after{display:none}`), and the glass `box-shadow` + `nav::before` ring are reset under `body.menu-open` so the open overlay is clean. **Skipped** (per the panel): a full-width-bar→pill scroll-morph (animates width/top on the fixed nav → the fixed-element-jump bug class) and a hover-travelling highlight pill (2026 cliché + competes with the hero magnetic tilt). **Scroll-condense was added then REMOVED (2026-06-20, per user "too annoying"):** a desktop, width-only "pill narrows 760→700px on scroll" effect (a `.is-scrolled` class + a per-page rAF-throttled scroll listener) shipped briefly (v=187 added, v=188 made width-only after the user said "only shrink the width, not the height") then was **fully reverted (v=189)** — the `@media (min-width:769px)` condense CSS, the `prefers-reduced-motion` override, and the 8 inline scroll scripts were all deleted. **Do NOT re-add scroll-condense unless asked.** The nav is static again (Specular Glass + active underline stay). **Gotcha (IMPORTANT):** the header glass targets the **bare `nav` selector**, which ALSO matches the case-study `<nav class="side-nav">` — the Specular-Glass `box-shadow` + `nav::before` ring leaked a glass box around the side-nav (shipped briefly in the v=186 push, **fixed v=188**: `.side-nav` now resets `box-shadow:none` + `.side-nav::before{display:none}`, on top of its existing background/blur/border/padding resets). **Any future addition to the bare `nav` rule MUST also be reset on `.side-nav`** (or scope the header to `nav:not(.side-nav)`). style.css **v=185→v=189** across all 8 pages.
- **Desktop fold compression — TRIED AND REVERTED 2026-07-26 (per user, "I do not like it").** The Mobbin landing benchmark's top move was to reclaim ~208px of hero padding on desktop (`.hero` 200/160 → 140/72, h1 margin-bottom 48→32, `.status-tag` margin-top 48→28, `.work` padding-top 80→56, gated `min-width:769px`) so a slice of work row one crops into the fold, the way Framer/Pentagram/Anthropic do. It measurably worked — card top 932→706, plate visible above the fold 0→193px @1440×900, mobile provably untouched — **and the user rejected the look.** The hero's full-height breathing room is the deliberate choice; do NOT re-propose compressing the hero to pull work above the fold. (The companion `.project-grid .project-card:nth-child(-n+2) { animation: none }` exemption was removed with it — it only existed because cards at the fold render at the scroll-reveal start keyframe. If any future change DOES place row one at the fold, that exemption becomes necessary again or the cards render faded.)
- **Hero light lags the scroll (added 2026-07-26):** as the hero scrolls away the whole light field drifts DOWN by up to **30%**, so it lingers behind the page instead of travelling with it — the gradient reads as an environment the page moves through rather than paint stuck to the box. **The technique is the important part:** irisFlow already owns `--g2-*-y`, `transform` (its scale-breathe) and `translate` (Lightfall's cursor drift), so there was no free channel. Instead of fighting it, a SEPARATE `@property --g2-lift` is folded into every blob's Y via `calc(var(--g2-warm-y) + var(--g2-lift))`, and a second animation `g2Lift` drives only that, on a `scroll(root block)` timeline over the first `100vh`. Two animations coexist on one element because they animate **different properties** — irisFlow keeps the document timeline (`animation-timeline: auto, scroll(root block)`). **At rest `--g2-lift` is 0%, so the hand-tuned warm/blue seam under headline line 1 is completely untouched** — the drift only exists while scrolling away. Gated to `min-width: 769px` + `prefers-reduced-motion: no-preference` inside `@supports (animation-timeline: scroll())`; mobile is excluded on purpose because its gradient is separately tuned for the hero/work seam (`.hero::after`). Shipped at 30% after 14% was judged too subliminal to earn its place; the single dial is the `--g2-lift` value in `@keyframes g2Lift`.
- **Hero:** Animated mesh gradient (@property CSS). **Light-sweep reveal + specular type + magnetic tilt — the scramble was REPLACED 2026-07-27 (user: "too boring… more 2026"; the decode-text trope read 2020-2023 and the heading went inert after load).** The entire width-pinning machinery (canvas measureText pools, per-span pinning, the old "Scramble width-pinned" fix) is GONE from index.html — no glyph substitution means that whole bug class (line breathing, 3-line snap, AIM pileups) is structurally impossible now. **(1) Entrance:** each glyph transitions from `opacity:0 / translateY(14px) / blur(9px)`, delayed by its **horizontal position** (`--d` = leftmost 0ms → rightmost 450ms; each glyph 700ms), so BOTH lines wake left-to-right as one wave (line 2 is centered so its first glyph starts ~55ms — position-based, not index-based). Spaces stay bare text nodes so wrapping behaves as plain text. h1 class sequence: `.sweep-in` (hidden state) → `.lit` (added after a **double rAF** — same commit-the-start-state bug class as the rule wipe) → `.settled` (+80ms after the wave). **(2) Specular presence — AMBIENT + cursor (reworked same day, 2026-07-27):** the sheen is driven by the **gradient's own motion**, not only the pointer — the first cut was cursor-only, which made the one distinctive effect an easter egg (a visitor who lands and scrolls never saw it). A ~16Hz rAF loop reads `--g2-warm-x` off `.hero-orbs` via `getComputedStyle` (irisFlow animates it as a registered `@property`, so the computed value IS the live light position — reading it can never drift out of sync with the keyframes) and lights glyphs near that x with a broad gaussian (σ=230px, ceiling `AMBIENT = 0.55`; **0.38 measured near-invisible** — brightness 1.04/alpha 0.13, the documented "too subliminal to earn its place" failure mode). The cursor layers on top via **screen blend** (`I = p + a(1−p)`, cursor peaks at 1, σx=90/σy=170) — `window.__heroSpecular(cx,cy,rect)` is still called from Script A's `apply()` but now only RECORDS the pointer (`mouseleave` passes `null`); **the ambient survives mouseleave** — that is the point. Per glyph: inline `filter: brightness(1+0.10·I)` + two-layer white `text-shadow`, writes skipped when ΔI<0.015; the 60ms tick is enough because `.settled` spans carry `transition: filter/text-shadow 0.45s` and CSS interpolates the gaps. The loop is **IntersectionObserver-gated to the hero** (same discipline as the footer dissolve; verified frozen off-screen / resumed on return). **Cascade trap:** the settled rule must be `.hero h1.lit.settled …` (4 classes, later in file) so its `filter: none` beats `.sweep-in.lit`'s `blur(0)` — blur(0)→brightness() is a MISMATCHED filter-function list and interpolates **discretely** (the glow fade-out snaps); `none` counts as the identity list and eases smoothly. **(3)** `.hero h1 .line` lost its `overflow:hidden` (pre-scramble relic; it would clip the entrance blur halo and the specular bloom — `padding-bottom: 6px` kept so layout doesn't shift). **(4) Retimed follow-ups:** `.hero-sub` delay 2.2s→**1.05s**, `.status-tag` 2.6s→**1.35s** (headline settles ~1.15s; the old delays were tuned to the scramble's longer runway). **(5) Mobile ≤768px:** `.hero h1 .line-inner span { filter: none !important }` (fade+rise only — blur is the costly channel on phones); the specular arms only under `(hover:hover) and (pointer:fine)` so touch never gets it (it DOES arm in a width-resized desktop browser — capability-gated, not width-gated, same as Lightfall; testing artifact, not a bug). Reduced motion: h1 jumps straight to `.lit.settled`, spans are never hidden (the hidden state lives inside `prefers-reduced-motion: no-preference`). **Verification notes:** the same-origin-iframe probe trick still works for catching the entrance mid-flight (entrance verified: first glyph 0.57→1 while mid/last still 0, blur 9→4.7→1.1→0); and **rAF-deferred effects (the specular event path) do not run while the browser pane isn't actively rendering frames** — a dispatch+sleep probe reads stale values and looks like a bug; wait on `requestAnimationFrame` chains instead (same trap family as the not-fronted IntersectionObserver). style.css **v=215→v=217**. **Subtitle carries the builds-in-code claim (added 2026-07-25, improvement-research P4):** "…and I prototype in real code, this site included." — "prototype in real code" is the literal phrase in Anthropic/Cursor/Perplexity design-role JDs (reviewers grep for it) and "this site included" makes the page itself the proof; keep both phrases intact if the subtitle is ever reworded (per the [[feedback_yc_audience]] judge-by-the-reader rule). **Cursor-follow gradient drift — "Lightfall" (added 2026-06-20, desktop only, chosen via a multi-agent design pass over 5 concepts):** on desktop the `.hero-orbs` mesh leans toward the cursor, folded into the SAME mousemove handler as the magnetic tilt (Script A in index.html) so the type + its light read as one motion. It drives the independent CSS **`translate`** property (NOT `transform`), so irisFlow's `transform` scale-breathe keeps running untouched; single rAF-throttled listener, `rect` recomputed per frame (scroll-safe), eased by an inline `transition: translate 0.55s cubic-bezier(0.22,1,0.36,1)`. Amplitude knobs in the script: `ORB_X` (horizontal multiplier on nx∈[-0.5,0.5]) + `ORB_Y` (vertical); shipped at **ORB_X=180 (±90px) / ORB_Y=26 (±13px)** after the user asked for it stronger (iterated ±26→±60→±90; ±26 was invisible under the `blur(56px)`). Vertical is kept small on purpose so the hand-tuned warm/blue seam under line 1 barely moves. At ±90px the script also widens the orbs' off-screen bleed to **`left/right: -12%`** (overriding the CSS -5%) so the larger drift never exposes a gradient edge — verified clean at both extremes. **Gated to desktop + motion-safe:** wired only under `(hover:hover) and (pointer:fine)` and NOT `prefers-reduced-motion`, so mobile/touch get nothing (insets + translate are never written below the gate, so the mobile `.hero::after` seam-fade is untouched) and reduced-motion users get a fully static hero — this also **fixed a pre-existing gap** where the magnetic tilt ran even under reduced-motion; now both are gated together. All in index.html's hero `<script>` — no style.css change, no version bump. **The old "No cursor glow" rule still holds:** Lightfall *moves* the existing light field, it never *paints* a glow disc/spotlight (the exact distinction the prior rejection turned on); do NOT add a painted cursor glow. Hire-state pill `.status-tag` below the subtitle reads "Open to new opportunities" with a `.status-dot` (green #4ade80, soft `pulse` animation that breathes opacity 0.7↔1.0 over 3s ease-in-out — deliberately *not* a scale+opacity blink; calm "alive" signal, not nervous). When Chang lands a role, swap the pill copy (e.g. "Open to interesting conversations") or remove `.status-tag` entirely. **Hero gradient variant system (2026-06-15):** the gradient lives on `.hero-orbs` as swappable variant classes. `.hero-orbs--g1` = the original animated violet/coral mesh (rgba(100,70,220)/coral/purple/peach, `meshFlow` 12s), **saved and restorable**. `.hero-orbs--g2` = the **ACTIVE** gradient (HTML: `<div class="hero-orbs hero-orbs--g2">`): a vivid full-neon iridescent spectrum from a reference image — **warm at the top** (yellow #FFD600 → red → pink) flowing into a strong **blue** (rgba(22,120,255)) behind the heading; **only the 4 bright hues, no teal/green/violet edge tones**. It's "alive" via @property-animated blob positions (`irisFlow` 15s) that drift + a scale-breathe (mirrors g1's motion). To revert to the old look, swap the class back to `hero-orbs--g1`. The `.hero-sub` and `.status-tag` were brightened for legibility over g2 (subtitle → rgba(255,255,255,0.82); pill → 0.92 white text + dark glass backdrop `rgba(10,10,12,0.45)` blur(8px) + 0.22 border). **Decision history — do not re-litigate:** iterated muted → vivid → "not that vivid" → full-neon WARM-top → and on **2026-06-15 pushed warmer still to WARM-DOMINANT** ("as warm as the footer": warm fills the body + behind the heading, blue eased back/down behind the subtitle). User **rejected the extreme** (warm covering ALL the text with blue only as a thin bottom-edge band — "too much") and the earlier over-soft/washed versions. **Motion was reworked livelier the same day** (`irisFlow` now 13s): blob travel widened to ~±12% and the **yellow now animates too** (it was pinned), because the first pass "did not feel alive". **Then reverted to BALANCED and refined (current shipped state, 2026-06-15):** the warm-to-blue cut now lands right **under the FIRST heading line** — "AI-first product designer." glows on warm, while "Building what's next." + the subtitle + pill sit crisp on blue (the `irisFlow` centers that produce this: warm-y ~25, pink-y ~48, blue-y ~78; the two headline lines split at ~box-Y 57%). A subtle heading text-shadow is the lever if the first line ever needs more crispness over the warm. **Mobile (≤768px) fixes (2026-06-15):** the short mobile hero compresses the g2 gradient and lands the blue blob low, so it met the dark work section with a visible **cut / dividing line**. Fixed two ways: (a) `.hero-orbs { bottom: 8% }` (was `-12%`, which shoved the blue to the very bottom edge where `overflow:hidden` clipped a ~4%-opacity sliver into a faint line) pulls the blue up — also makes it more visible; **and** (b) a `.hero::after` overlay (`linear-gradient(to bottom, transparent, #0a0a0a 78%)`, `height: 42%`, `z-index: 1` — above orbs/0, below text/2) fades the hero's bottom to the **EXACT page bg** so the gradient dissolves seamlessly into the work section with **no clipped residual**. (A `mask-image` on the orbs was tried first and abandoned: a mask leaves a clipped-residual line because it makes the orbs transparent rather than painting the bg; the overlay paints `#0a0a0a` so the hero edge and work section are the same color — seamless.) Both mobile-only; **desktop is untouched** (`::after` is `none`, orbs use base `bottom:20%`). Also **mobile `.hero h1` is `clamp(26px, 8.5vw, 32px)`** (was a flat `32px`): keeps ~32px on standard phones (≥375px) but scales down on narrow screens (30.6px@360, 27.2px@320) so the first line "AI-first product designer." keeps its period on **one line** instead of orphaning it (each char is an inline-block, so the line can break right before the "."). **Lovable-resemblance challenge — RESOLVED: KEEP g2 (2026-08-04, user's call; do NOT re-propose changing the gradient).** An outside reader told Chang the site looks like lovable.dev/home. Verified side-by-side: the resemblance is real and specific — g2's yellow→red→pink→blue ramp is Lovable's brand gradient stop-for-stop (their hero runs the same four hues inverted), and the footer glow is even closer; layout is NOT the overlap (dark + centered headline is half the portfolio world). A two-hue amber/blue "g3" variant was built in the worktree (hero `.hero-orbs--g3` reusing g2's `--g2-*` @property/irisFlow machinery so specular/Lightfall/lag worked untouched; red→rgba(255,160,30,.85), pink-slot→deep amber rgba(255,120,45,.72) — a sky-blue bridge there was tried first and MOVED THE SEAM ABOVE LINE 1, the pink slot must stay warm-family; footer pink→sky rgba(96,170,255,.58), red→amber-orange rgba(255,150,40,.60)), verified desktop+mobile, then **fully reverted per user ("I do not want to change")**. The user's standing defense, factually sound: Instagram's 2016 brand gradient (yellow→orange→magenta→purple/blue) is the same family and closer to g2 than Lovable's own ramp — the palette is a decade-old iridescent-spectrum trend nobody owns. The g3 recipe above is the recovery path if this is ever revisited; og-image + favicon also carry the ramp and would need to follow. **The accepted differentiation move instead — "blueFlow" electric-blue sublayers (2026-08-04, per user "make the blue more AI and more dynamic", shipped v=227):** the palette is untouched; two pools now live INSIDE the blue region — an azure core `rgba(10,132,255,.50)` (ellipse 58%×34%) and a cyan glint `rgba(96,214,255,.32)` (40%×24%), layered between the pink pool and the broad blue base — driven by their own `blueFlow` animation (own `@property` vars `--g2-azure-x/y`, `--g2-cyan-x/y`) at **9s against irisFlow's 13s, so the composed field only re-aligns every ~39s** and the blue reads as weather rather than a loop; Lovable's gradient is a static wash, so the motion IS the differentiation. Both pools are position-clamped to **y 66–90%** so the warm top and the line-1 seam never see them, and both fold in `--g2-lift` so the scroll lag moves the whole field as one. **The desktop `@supports` lag block is now THREE index-aligned longhand lists (`irisFlow, blueFlow, g2Lift`) — any future animation added to the hero orbs must extend every list in step or the shorthand-reset bug class returns.** Dials: cyan alpha 0.32→0.45 for an overt glint, 9s→7s for restless. **Footer deliberately unchanged** (one signature motion per surface — the footer's is the pixel dissolve; the reflection reading calmer than its source is intentional physics). Verified: three frames seconds apart show the blue mass reorganizing while warm/pink hold identical; mobile seam-fade clean; specular untouched (reads `--g2-warm-x` only). **Open thread — hero vividness (diagnosed, NOT applied):** the hero looks less vivid than the footer because it's blurred radial blobs (`filter: blur(56px)`) while the footer is a crisp un-blurred *linear* gradient — same palette, but the blur averages the hero's colors down. The lever is **reducing the hero blur (~28–32px punches it up to match the footer)**; user was mid-decision on the level when the session paused.
- **Hero landing-moment pass (2026-08-12, per user, from mentor feedback "utilitarian, product-centric layout… lacks an attention-grabbing landing moment"; Mobbin site-section research; gradient/background explicitly untouched per user — twice confirmed):** six changes. (1) **Headline line 2 "Building what's next." → "I build what I design."** (user authorized the content change "for better signal for hiring manager"; the claim is substantiated by Homewise/Luna/this-site and sets up the sub's "prototype in real code, this site included" as proof; the hero-sub itself stays verbatim per its protected-phrases rule). **Supersedes the Hero bullet's quotes of the old line; consequence: `og-gen.html:63` still carries "Building what&rsquo;s next." so the og-image is STALE until regenerated** (documented pipeline in the Deployment section; bump `?v=` on regen). (2) `.hero h1` clamp max **84→104px** (renders 100.8px@1440, lines 984/783px, verified no wrap/overflow; ≤1200px unchanged since 7vw governs there; mobile clamp untouched). (3) **Ignition beam** — one scripted full-intensity specular pass L→R across the settled headline (starts 180ms after `.settled`, 750ms, σ=150px, travel minX−260→maxX+260, easeInOutSine), computed INSIDE the same intensity pipeline as ambient+cursor (screen-blended) so it can never drift from hover behavior; **phase derives from an absolute `performance.now()` start, so a hidden tab or early scroll-away skips the beam rather than replaying it stale**; frames run unthrottled only while the beam is live. (4) **Wake** — per-glyph inline `transition-duration`: **0.12s when brightening, 1.1s when dimming**, so the beam and any cursor movement paint a slowly-decaying trail; the stylesheet's 0.45s on `.lit.settled` is now only the pre-first-write default. (5) **Magnetic glyphs** — per-glyph `translate` toward the cursor (PULL 3.2px, σ=110px, lerp 0.18/frame, runs every rAF frame — pure math + transform writes, no style reads; the intensity pass keeps its 60ms tick). Forces read the **CACHED base centers** so displacement never feeds back into itself; **the force ramps down inside 24px of the pointer** (direction-singularity guard — without it the glyph under the cursor jitters); `transform` is deliberately NOT in the `.settled` transition list so these writes land instantly (a CSS transition there would smear the spring). **Block tilt halved 6°→3°** so the two position responses don't fight. (6) Sub+pill `fadeUp` easing `ease` → `var(--ease-out)` — closes the "easing family changes mid-sequence" open item from the animation audit. Verified live: beam plateau ~720ms then slow decay; ambient steady state EXACTLY 1.055 brightness (the 0.55 ceiling); magnetic ±2.9px, directionally correct on both sides of the cursor, zero residual transforms after mouseleave; warm/blue seam still splits between the two lines at 100.8px; mobile 375 unchanged (fade+rise only, no filters/transforms/specular, 31.9px, both lines single-line, zero overflow). **New probe gotcha: the same-origin-iframe entrance probe freezes mid-decay when the iframe stops compositing** — a steady 1.076 brightness read was the FROZEN IFRAME, not an engine bug (the main page settles at exactly 1.055); use iframe probes for entrance choreography only, main-page probes for steady states. Offered, not built: pill-ignition beat (status dot pulses as the beam exits), drag-the-light. style.css **v=230→v=231** across all 9 pages. **Follow-up batch 2026-08-13 (per user "do" — approving the recommended package; SUPERSEDES the offered-not-built list above, both are now BUILT):** (1) **Headline line 1 "AI-first product designer." → "AI products people can trust."** — from a 3-reader + judge workflow on "is this combo the best to be hired": all three readers independently called "AI-first product designer." the one commodity phrase in the stack (consensus 6.5-7/10, projected 7.5-8 with the trust-thesis line 1); the judge's exact pick was first-person "I design AI products people trust." but it measures **1294px at 100.8px (mobile wrap at 339>327 + desktop margins gone)**, so the shipped line is reader 1's variant (the AI-startup lens, scored 8) at **1136px — fits every viewport at full scale with zero clamp changes. Lesson: price a copy candidate against the type scale before adopting it; a 31% longer line costs ~24% scale.** The H1 now contains no "product designer" role noun (judge's dissent, accepted): the role lives in `<title>`/meta/JSON-LD/About, and **the meta/og/twitter descriptions + JSON-LD deliberately keep "AI-first product designer"** (search/ATS copy, the homewise precedent). Trust thesis is substantiated one scroll down (Homewise HITL gates, NOVA, Countersign, Luna). og-gen.html lockup updated + **og-image regenerated (`?v=4→?v=5` on all 10 pages that carry it, incl. both decks)**. (2) **Drag-the-light** — mousedown on the hero grips the field: Lightfall amplification ×1.8 X / ×2.5 Y (X extreme 0.6·180·1.8=194px stays inside the −12% bleed ≈207px; **the seam is ALLOWED to move during an active drag — displacement is the point, release eases home**), nx/ny clamped ±0.6, grip ease 0.25s / release 0.9s / restored 0.55s via timeout, window-level mousemove keeps a held grip tracking outside the hero, mouseleave early-returns while dragging (mouseup owns the reset; released-outside runs the reset itself), links opt out via `closest('a')`, preventDefault kills text selection, `__heroDrag` widens the engine's cursor pool σ90→140, cursor grab/grabbing via a motion+pointer-gated media block in style.css. Verified numerically: drag translate = exactly nx×324, release settles to nx×180 (same pointer), transitions swap 0.25s→0.9s→0.55s. (3) **Pill ignition** — engine adds `.dot-ignite` to `.status-dot` when the beam phase passes 1, **gated on `ignSeen` so a visitor who scrolled away and missed the beam gets no orphan pulse**; `dotIgnite 0.9s` compound animation restates `pulse` (3s, 0.9s delay) so the breathe resumes. (4) **Both review-workflow fixes applied** (14-agent adversarial pass, 3 confirmed / 8 refuted): `cache()` subtracts the engine's own `mx/my` so a resize mid-hover can't bake magnetic displacement into the base centers, and the hero IO's deactivate branch **clears `cur`** (Firefox fires no boundary events when content scrolls under a stationary pointer — a phantom cursor would keep glyphs bent+lit on return). Refuted-with-reasons worth keeping: the entrance/settle race is pre-existing and benign; 404.html inherits the 104px clamp safely; the seam moves ≤0.5% at the new scale (it is orb-box-relative, not viewport-relative). style.css **v=231→v=232** across all 9 pages. **Gotcha: a second stale server may sit on port 8888** (the preview harness auto-ports to e.g. 53800 when 8888 is taken) — a pane showing the OLD hero on :8888 is that stale server, not a regression. **TRUST-THESIS LINE 1 REVERTED SAME DAY (2026-08-13) — THE DIRECTION IS DEAD, do not re-propose:** the user read "AI products people can trust." as calling HIM the product ("I am not fucking product. I am designer"). The failure mode: an actor-less noun-phrase headline on a personal site can parse as an appositive describing the person — the judge's first-person version ("I design AI products people trust.") avoided exactly this and was dropped only for layout, but the lesson generalizes: **never ship a line-1 fragment whose subject could be misread as Chang himself, and treat the owner's read as the veto no panel score outranks.** Line 1 is back to "AI-first product designer." permanently. **Minutes later the user killed line 2 as well ("you can not say 'i build what i design' cuz i did not take care of the backend") — an HONESTY veto, consistent with the site's documented prototype-not-product framing: Chang builds front-end prototypes in real code, the dev teams build the backends, so "I build what I design" overclaims full-stack ownership and invites the exact interview probe the Homewise copy is engineered to survive. Do NOT re-propose builds-claims that imply whole-product ownership; the sub's "I prototype in real code" is the correctly-scoped claim and it is protected.** Net: the ENTIRE headline reverted to the long-settled "AI-first product designer." / "Building what's next." — the 2026-08-12/13 copy experiment is fully dead, while ALL of the animation/interaction work (scale 104 cap, beam, wake, magnetic, drag-the-light, pill ignition, easing unification, both review fixes) survives untouched. og system fully restored to pre-batch state: og-gen.html back to the original lockup, `images/og-image.jpg` restored byte-identical from git (f18d907), refs back to `?v=4` on all 10 pages so existing social caches stay valid. The judge-panel scores above stand as record only — the hero copy question is CLOSED unless the user reopens it, and any future copy proposal must clear BOTH vetoes: no actor-less fragments that can describe Chang, no claims wider than front-end prototyping. **Presence + release batch, same day (2026-08-13, per user "ok" — the LAST hero additions; two research agents ran first and both verdicts are recorded here so they are never re-derived): (a) craftwork.design sweep (120 curated entries, 14 sites visited live): the hero is ALREADY past the curated bar on motion — nothing in the set approaches the per-glyph light physics; the gap is PRESENCE and PROOF, and the most recurrent missing pattern is a live local clock. (b) YC-tier scroll research (13 homepages fetched raw, markers grepped, Linear's Hero/useScrollMask source read): top-tier heroes are LOAD-TIME moments — none put scroll-triggered animation in the hero itself (scroll budget goes below the fold: one-shot viewport reveals, a nav scrolled-state; full GSAP choreography only on agency builds, and even Anthropic's hero h1 is static), and ZERO of the 12 fetched sites ship the native `animation-timeline`/`view()`/`scroll()` API this site already uses in four places — mechanically ahead of the tier. Built from those verdicts: (1) `.hero-presence` — "San Francisco · 4:32 PM" line under the pill (13px, white 0.55, fadeUp 1.5s closing the entrance sequence; `Intl.DateTimeFormat` with `timeZone: 'America/Los_Angeles'`, 10s tick, runs on ALL devices outside the pointer/motion gate since it is text-only; JS-fails leaves the static "San Francisco" markup text, still true). (2) Light release on exit (the Linear useScrollMask analog — the tier's ONE verified in-hero scroll-linked effect): `rel = max(0, 1 − scrollY/(heroH·0.7))` computed per frame in the engine, multiplies the final specular intensity AND the magnetic force k, so on scroll-out the type hands its light back to the field and the grip relaxes, completing just before the gradient's own `--g2-lift` lag maxes at 100vh. The writes are scrubbed; the RENDERING is eased by the wake's asymmetric durations — draining rides the 1.1s DECAY (reads as light draining, not a hard scrub), returning rides the 0.12s RISE. Verified numerically: maxB 1.054 at top, 1.027 at 35% scroll (theory 1.028), 1.000 past 70%, 1.055 on return; presence format-checked and ticking. style.css v=232→v=233 across all 9 pages. THE HERO IS NOW DECLARED DONE — standing recommendation on record: no further hero additions; both research reports say the remaining lever is showing the mentor, not building more.** **Same-day amendments (2026-08-13): (a) the presence line was DELETED per user ("delete SF time") hours after shipping — `.hero-presence` markup/CSS/ticker all removed, do not re-add a clock/location line; (b) the light release gained a DIM-BELOW-REST half (per user, after "where is the scrolling effect?" — the ambient-only release measured near-invisible, the documented too-subliminal failure mode): `base = 1 − 0.15·(1−rel)` darkens every glyph toward brightness 0.85 at full release, a `relMoved` flag (ε 0.01 on rel) forces writes through the ΔI-skip so I=0 glyphs still dim, transition direction follows scroll direction for pure-release changes, and idle clears back to the stylesheet only when `!I && rel≥0.999`. Verified: 1.055/1.004 top → 0.952/0.925 at 35% scroll (theory 0.953/0.925) → 0.85 flat past 70% → full restore. style.css v=233→v=234 across all 9 pages (presence CSS removal). The user then asked for BOLD hero directions ("you run so many agents and these are the changes you can only suggest?") — a bold-directions research workflow (3 lenses + feasibility architect + judge) produced a 4-item menu; **the user picked #1: "The Living Gradient" (2026-08-13, per user "let us do #1"), and a WORKING PROTOTYPE now lives in `hero-fluid.js` (worktree, committed, NOT pushed).** All three research lenses independently ranked it first; the elite-portfolio lens's key finding: bold in your OWN medium reads as taste, imported spectacle reads as applying for the wrong job — and every verified hired-at-target-tier portfolio (rauno.me, emilkowal.ski, paco.me, benji.org, jhey.dev) runs a RESTRAINED text hero, so bold is a choice, not a requirement. **Architecture (the load-bearing decisions): (1) the WebGL2 shader reproduces the six g2 pools PROCEDURALLY, transcribed 1:1 from `.hero-orbs--g2` (geometry/ramps/alphas as shader constants — if that CSS block ever changes, the render shader in hero-fluid.js MUST follow; same mirror rule as api/chat.js↔matchData), with per-frame pool positions read LIVE off the hidden-but-still-animating orbs element (`.hero--gl .hero-orbs { opacity: 0 }` — opacity does not stop @property animations), so composition drift can never desync from the CSS and the glyph specular (which reads `--g2-warm-x` off the same element) stays coupled for free. (2) DYE IS NEVER ADVECTED — a Navier-Stokes velocity sim (advect → splat → vorticity confinement ε14 → 18 Jacobi pressure iters → gradient subtract, 192×~108 half-float grid) advects a DISPLACEMENT field that offsets the render's UV lookup (dissipation 0.962/frame): stirring distorts the light, the composition is the attractor it always relaxes back to (verified: displacement 0.010→0.003 over 2.5s). Free dye advection was explicitly rejected by the feasibility pass — yellow+blue mixes to mud and destroys the seam. (3) Integration surface: reads `window.__heroDrag` (drag = wider/stronger splats), zero changes to Script A/type engine; canvas matches the ORBS box (incl. Script A's -12% bleed) at half-res backing with CSS `blur(26px)` standing in for the orbs' blur(32px) (the blur ALSO edge-feathers the canvas exactly like the CSS layer's clip). (4) Fallback chain — mobile/touch/reduced-motion/no-WebGL2/no-EXT_color_buffer_float/failIfMajorPerformanceCaveat/context-lost/JS-throw/`?gl=0` — ALL land on the untouched CSS hero; the class flips only after the first frame renders (no flash). (5) Ambient seeded weather: faint splats every ~2-3s from a sessionStorage-seeded RNG (the generative fold-in — per-visit unique phase). Debug: `window.__fluidStats` (fps/frames), `window.__fluidProbe()` (mean |displacement|). Verified first run: 120fps on a 120Hz display, physics decay correct, composition/seam reads as the same identity vs `?gl=0`. **The "too dark" first render (user-flagged) was a BUG, not a tuning gap — fixed same day: the render shader's back-to-front mix chain produces PREMULTIPLIED color by construction, and the output multiplied by alpha AGAIN, darkening the whole field by its own alpha (worst at low-alpha corners). Output is `vec4(rgb, a)`, never `vec4(rgb*a, a)`. Two gotchas from the fix, both recorded: (a) BACKTICKS INSIDE SHADER COMMENTS terminate the JS template literal — the first fix shipped a SyntaxError and the fallback chain silently showed the CSS hero, which LOOKS like a successful brightness fix in a screenshot; always check `glActive`/console before believing a fluid render, and always `node --check hero-fluid.js` after ANY edit; (b) bump the `hero-fluid.js?v=` query on every change (browser caches the file independently of the HTML).** PENDING (the judge's priced tuning, not yet done): fine seam comparison vs CSS at matched animation phase, battery/low-end perf validation, Safari verification, stir-feel tuning to the user's taste, and the decision whether Lightfall's translate lean should also apply to the canvas. style.css v=234→v=235 (the `.hero--gl` rule). **HERO COPY FROZEN AS-IS (2026-08-13, user's call after a full suggestion round — "keep the content as it is"): headline stays "AI-first product designer." / "Building what's next." and the sub stays verbatim including "intuitive". Offered and NOT taken (on record so they are never re-derived): line 2 candidates "Building what AI can't." (top pick, from the bio's own "parts of design that AI can't do for you yet"), "Deciding what ships.", "From idea to working code."; sub options drop-"intuitive" and concrete-domains ("from home services to AR for children", itself already declined 2026-08-01). Rationale accepted by both sides: the Living Gradient + interaction system now carry the landing moment, every verified hired-at-target-tier portfolio runs restrained copy, and copy is a two-minute change if the mentor ever reacts to it — do not re-propose hero copy changes unprompted; the user asks when they want options. PUSHED TO PRODUCTION 2026-08-13 (f18d907..b24f821 → main, per user "push first"): the Living Gradient is LIVE on chang-mou.com behind its full fallback chain, with Safari/battery validation still pending — any GL failure serves the CSS hero.**
- **Footer:** Big "Say hello" watermark (scroll reveal). **Footer structure (restructured 2026-06-15):** the giant "Say hello" watermark lives in its **own `.say-hello` section** (`padding-top: 160px`, holds `.footer-big`), **NOT inside `<footer>`** — `<footer>` now holds only the `.footer-bottom` links bar. Both, plus `.footer-glow`, are wrapped in a **`.page-end` container** (`position: relative; overflow: hidden`) that **owns the glow** so it spans behind BOTH "Say hello" and the links bar and is clipped exactly at the page bottom. **Why two problems forced this:** (1) when "Say hello" + the links bar shared `<footer>` (links pulled up via `.footer-bottom { margin-top: -80px }`), the watermark **overlapped the LinkedIn link at narrow desktop widths** (the `-80px` was removed); (2) a first fix that put the glow only inside `.say-hello` left a **black gap at the page bottom** (glow stopped ~22px short of the edge) — the `.page-end` wrapper clips the glow at the bottom edge instead, so warm reaches the bottom with no gap. **JS wiring:** the glow-sizing scroll effect targets `.page-end` (`footerSection`); the letter-spotlight hover targets `.say-hello` (`footerEl`), **gated to the word's own bounding box** (first/last letter `getBoundingClientRect` + a 16px pad, added 2026-06-17): the handler only measured *horizontal* distance to each letter and listened on the whole `.say-hello`, so a cursor anywhere in the section's 160px top padding lit letters from far above the text while showing no hand cursor (the `<a>` is `pointer-events:none`, only the glyphs are `auto`) — the gate now resets the letters whenever the cursor leaves the word's box, so the lit area matches the hand-cursor area; applied to 5 pages (index/coffee/homewise/ticker AND pollen/nova — the original "all 4 pages" record here was wrong), while **about/play silently kept the oldest UNGATED variant** (no bbox gate, lit from the padding band) until the 2026-07-28 sweep rollout below. The "Say hello" reveal is the same IntersectionObserver + hide-on-scroll-up pattern on ALL pages as of 2026-07-27 (index's `.footer-big-text` reveal was rewired to the exact coffee/homewise/ticker IO pattern, variable named `footerVisible` to avoid colliding with other index globals). **The index "Like / what / you / see?" scroll-CTA was deleted 2026-07-27 and RESTORED 2026-07-28 at the user's explicit request — the "do NOT re-add" note is void, the section stays.** The 2026-currency audit had called it a 2019-2021 awwwards ghost-words trope with engagement-bait copy (flagged by two lenses); the user overruled that, so treat the section as a settled decision and do not re-propose removing it. Restored verbatim from the pre-deletion commit: `.scroll-cta`/`.scroll-word` CSS (0.15 opacity when `.visible`, `clamp(48px,8vw,80px)`, `min-height:40vh`), the mobile `display:none` block, the markup, and the per-word scroll JS whose trigger line is `vh*0.92 - i*(vh*0.05)` (each word keyed to its OWN viewport position, so the left-to-right stagger works at any viewport height — verified 1000→1100→1110). **One deliberate difference from the pre-deletion version: the words no longer gate the "Say hello" reveal.** The old JS toggled the watermark on `allVisible && inView`, which made index the one page with a special-case footer reveal; the restore keeps the standard IO pattern and only re-adds the word reveal, so the two are decoupled. **`.work` bottom padding went back 200px → 80px** in the same change: the 200px was added *because* the CTA was removed, and with the section back the two together would double the gap. Mobile is untouched either way (`.work` has a ≤768px `padding-bottom:80px` override and the CTA is `display:none` there). If the section is ever removed again, `.work` must go back to 200px or the last card row lands 80px off the footer band. style.css **v=219→v=220** across all 8 pages. **Page-end light pass (2026-07-28, per user, INDEX-ONLY, JS-only — no style.css change/bump):** (1) the CTA's one-shot `.visible` toggle is now the **reduced-motion/mobile fallback only**; on desktop each word's opacity is **scrubbed continuously** from its own viewport position (same `vh*0.92 − i*5vh` trigger geometry so the stagger holds; easeOutCubic over `0.16vh` of travel; inline `transition:'none'` because the stylesheet's 0.3s transition would smear per-frame writes) and is **fully reversible** — scroll back down and the words un-light. As `.page-end` enters, a `glow` factor (the same progress the footer-glow height uses) lifts the words past their 0.15 rest toward **0.4** and adds a **blue** text-shadow `rgba(30,120,255, glow*0.5)` — blue on purpose: the glow's nearest (top) stop is blue, so the words read as lit by the approaching field, keeping the "move the light that is already there, never paint a new one" rule. (2) **INDEX's "Say hello" letter spotlight became ambient sweep + cursor** (the headline-specular grammar: hover-only was an easter egg): a gaussian pool (`BASE 0.35`, `AMB 0.28`, `σ=220px` — idle range ~0.37–0.53, dimmed twice from the shipped-first 0.5/0.6 per user "too bright"; the deliberate contrast is idle peak ≈ HALF the cursor's 1.0 so hovering is a real step up. Floor warning: much below BASE 0.3 the word stops reading as a clickable mailto and becomes pure texture) drifts across the word on a 7s sine; the cursor layers on top via screen blend (`σ=150px`, mousemove only RECORDS the pointer, gated to the word's bbox) and the ambient survives mouseleave; one rAF loop paints, IntersectionObserver-gated to `.say-hello`, `sweepStart` bails while `.footer-big-text` is `display:none` (mobile), and reduced-motion gets a fully static word (no sweep, no spotlight — same gating as Lightfall/tilt). **Sweep rolled out to ALL 8 pages 2026-07-28 (per user):** the block is byte-identical everywhere (md5 `f09a7d71edc3d4dfd1bafe3728928902`, measured from the `// Letter light on "Say hello"` comment through `.observe(footerEl);` + its closing `}`). It is a per-page pasted block exactly like the chat widget, so **any tuning change must be re-copied to all 8 pages** (audit: `for f in *.html; do echo "$f $(grep -c sweepFrame $f)"; done` — every page ≥1). The rollout retired THREE coexisting variants: the gated hover spotlight (coffee/homewise/ticker, 1365 chars; pollen/nova, 679 chars) and about/play's 658-char ungated original — which still carried the light-from-the-padding-band bug the 2026-06-17 gate was built to fix; that fix had never reached those two pages. Dials: `BASE/AMB/SIG_A/PERIOD` consts and the CTA's `0.25` glow-boost/`0.5` shadow-alpha literals. **Probe gotcha (cost real debugging time):** the root has `scroll-behavior: smooth`, so a probe's plain `scrollTo()` ANIMATES — and while the browser pane isn't rendering frames the animation freezes mid-flight, which reads as "the effect is broken" and can complete LATER during an unrelated screenshot, teleporting the page. Probes must use `scrollTo({top, behavior:'instant'})` (same trap family as rAF-not-while-hidden). **Mobile (≤768px) — carries the full glow + dissolve as of 2026-06-18** (it used to hide the whole footer block — `.say-hello`/`.footer-big`/`.footer-glow`/`.px-dissolve` all `display:none` — leaving a bare links row the user found "too simple"; they wanted desktop-level richness): now `.footer-glow` + `.px-dissolve` are **shown**, `.say-hello` stays as the glow band (`padding-top: 180px` on mobile — bumped from 130px 2026-06-18 to give the dissolve room to breathe without the empty band becoming a void) but **only the giant text (`.footer-big-text`) and the © line (`.footer-copy`) are hidden** — `.footer-big` stays in flow at 0 height so it still serves as the dissolve's `data-stop` marker (no JS change). The links bar centers over the glow. **The dissolve animates on mobile too** (same as desktop). **Decision history (2026-06-18, do NOT re-introduce static):** a static-on-mobile variant was tried and reverted the same day — the engine's `reduce` flag briefly OR'd in `matchMedia('(max-width: 768px)')` to skip the rAF loop, but we landed on **animated everywhere** for one consistent footer signature + the craft moment on mobile (where much portfolio viewing happens). The battery/accessibility worry is already covered: the dissolve is **IntersectionObserver-gated** (runs only while the footer is on-screen) and **still falls back to a static frame under `prefers-reduced-motion`**, so motion/battery-sensitive users opt out at the OS level without flattening it for everyone. The © line "© 2026 Chang Mou · Built with Claude Code" shows on **desktop only** (user dropped it from mobile 2026-06-18). All 8 pages share the identical `.page-end` block. Left line: "© 2026 Chang Mou · Built with Claude Code" (was "Designed in the fog" — moved here from the hero in 2026-05-14 so the hero could carry the hire-state signal). Right side: LinkedIn / Email / Resume. **Footer text brightened 2026-06-15** (`.footer-copy` → rgba(255,255,255,0.8), `.footer-links a` → 0.92, both + `text-shadow: 0 1px 4px rgba(0,0,0,0.4)`) so it stays legible over the vivid glow. **Footer glow is the hero's REFLECTION (2026-06-15):** `.footer-glow` is now a smooth **vertical linear gradient** (NOT radial blobs — radials showed a visible blob shape/edge the user rejected) that mirrors g2 — **blue at the inside (top) → pink → red → warm/amber at the bottom**, the reverse of the hero's warm-top→blue, so warm bookends the page top & bottom and blue faces inward (the "mirror" idea from the reference image). It's shapeless + vivid (~0.6–0.68 alpha). The **top** fades from `transparent` (0%) to blend into the dark page above; the **bottom ends on amber** (`rgba(255,190,35,0.64)` at both 88% AND 100%), **NOT transparent** — the page ends at the bottom edge, so a transparent bottom stop just revealed the black background. That was the **"black gap below the footer" (fixed 2026-06-15)**; keep the bottom stop warm. Shared CSS → applies to the footer on **every page**.
- **Lit plates — the card light follows the cursor (added 2026-07-26):** the two-radial plate light scene was a *baked* inline gradient; it is now a real light source. Four cards carry `.project-img--lit` + three inline vars (`--plate-light` / `--plate-deep` / `--plate-base`) and the gradient moved into style.css, where both pools translate together by `--plate-dx/--plate-dy` so the whole field **leans** toward the pointer. The screenshot's contact shadow casts the opposite way via `--plate-sx`. This is the **Lightfall grammar one level down** — move the light that is already there, never paint a new one — so it does NOT violate the standing "no cursor glow disc" rule. **Three things that will bite a future edit:** (1) **the ellipse size must stay pinned at `70% 70%`** — that is exactly what the old `radial-gradient(ellipse at 30% 30%, …)` resolved to via farthest-corner on a 16:9 plate, so the at-rest render is byte-identical (verified: computed value is `70% 70% at 30% 30%`); drop the pin and a moving origin would also *resize* the pool, since farthest-corner is measured from the origin. (2) **`--plate-sx` must NOT be in `.project-card`'s transition list** — it feeds the img's `filter`, and `.project-img img` already carries `filter 0.5s ease`, so easing the property *and* the filter it produces compounds into a ~900ms chase that never lands (measured: shadow settled at 8.3px instead of 10px, and −1.4px instead of 0 after mouseleave). `--plate-dx/dy` DO get the transition (they drive `background-image`, which has no transition of its own). (3) **Only 4 of 8 cards are lit, on purpose** — NOVA + Mercor keep flat `#FFFFFF` (their PNGs carry a baked shadow that halos against anything darker, the documented exception), and StoryBloom + New Craft Society are full-bleed covers with no visible plate. Amplitude is one dial, `PLATE_AMP` in index.html, shipped at **12%**: measured against the composited gradient, 12% swings the plate centre ~20 RGB units, 18% only reaches 25.5, and by 24% the pools slide off-plate so the *edges* move less, not more. Driven by the existing per-card mousemove listener (the one that feeds the `::before` `--x/--y` glow) — no second listener — and gated to `(hover:hover) and (pointer:fine)` + not `prefers-reduced-motion`, matching how Lightfall is gated; when off, the vars sit at their `@property` `initial-value: 0%` and the plates render exactly at rest. style.css **v=209→v=210** (note: the older "v=194" line further down this file was already stale).
- **Hover glow takes the card's own hue (added 2026-07-26):** the `.project-card:hover` `box-shadow` was a hardcoded neutral white haze on all 8 cards; it now spills the card's OWN light past the border, so the plate's lit scene continues past its edge instead of stopping at it. Each card's colour identity moved onto the `<a class="project-card">` itself (`--plate-light` / `--plate-deep` / `--plate-base`), and `.project-img--lit` reads them by inheritance — **one source of truth per card**; do not push them back down onto the `.project-img` div, or the glow loses its input (custom properties inherit down, not up). **The hue had to be REBUILT, not tinted:** every plate hue is a pale pastel, and at these alphas over `#0a0a0a` a pastel and pure white land within ~2 RGB units — the tint would be mathematically present and visually absent. So the glow is `oklch(from var(--plate-light) 0.78 min(calc(c * 5), 0.18) h / 0.10)` — hue only, with lightness and chroma forced. **Two properties of that expression are load-bearing:** (a) `#FFFFFF` has chroma 0, so `calc(c * 5)` stays 0 and **NOVA + Mercor glow neutral with no special-casing** — exactly right for the two plates that must stay flat white; (b) it is `min()`, NOT `clamp()` — a clamp with a floor would drag those chroma-0 whites up to the floor and put a colour cast on precisely the cards that must not have one. The cap exists because unclamped `c * 5` ran 0.105 (StoryBloom) to 0.249 (New Craft Society), so one card glowed 2.4× more vividly than another for no reason a viewer could read; capped, the spread is 0.105–0.18. Gated by `@supports (color: oklch(from white l c h))` inside the existing `@media (hover: hover)`, so browsers without relative colour syntax keep the old white glow. style.css **v=210→v=212**.
- **Thumbnail parallax inside the plate (added 2026-07-26):** the floating screenshot drifts ±8px against its plate as the card crosses the viewport, so the screenshot-on-a-lit-plate material gains real depth. **Two traps, both load-bearing:** (1) **the timeline must be NAMED, not `view()`.** `view()` binds to the nearest ancestor *scroll container*, and both `.project-card` and `.project-img` are `overflow: hidden` — which makes them scroll containers — so an anonymous `view()` on the `<img>` binds to the plate, which never scrolls, and sits dead at progress 0. Fix: `view-timeline-name: --card-view` on `.project-grid .project-card` (whose own nearest scrollport IS the viewport), referenced from the img as `animation-timeline: --card-view`; name lookup walks up the ancestor chain. Same family as the documented body-`overflow-x` trap — watch what silently became a scroll container. (2) **it drives `translate`, NOT `transform`** — hover owns `transform: scale(1.04) translateY(-3px)` on that same element and `.project-img img` transitions `transform` at 0.8s, so a shared property would rubber-band the parallax through the hover transition. Verified coexisting: `translate: -1.71px` alongside `transform: matrix(1.04,…,-3.12)`. Exactly the Lightfall-vs-irisFlow split. **Excluded from the two full-bleed covers** via `.project-img--cover` on StoryBloom + New Craft Society: measured, New Craft's cover fills its plate at **exactly 0px top and bottom gap**, so 8px of travel would swing a bare 8px strip into view (StoryBloom is already cropped past its edges at -10/-4). The six contained screenshots keep ≥20px clearance at full drift (PollenNav tightest at 20px, Mercor loosest at 122px). Inside the existing `prefers-reduced-motion: no-preference` + `@supports (animation-timeline: view())` block, and note the same longhand rule as `card-rise` — `animation-duration: auto`, never the shorthand.
- **SELECTED WORK hairline draws in (added 2026-07-26):** `.section-rule` wipes left-to-right (`scaleX(0)→1`, `transform-origin:left`, `--dur-slow`/`--ease-out`) the first time `.section-meta` enters view. Home-page only — `.section-rule` exists once in the whole codebase. **It is a one-shot IntersectionObserver transition, NOT an `animation-timeline: view()` scrub like `card-rise`, and that is the whole design decision:** `.section-meta` sits at document y≈855, which is ABOVE the fold on any viewport taller than ~860px, so a scrubbed reveal renders a *partially drawn* line on first paint and then just leaves it there — measured **17% drawn at 1440×900, 48% at 1000px tall, 79% at 1120px**. A shorter range does not rescue it (41.6% at 900px). Time-based means it always plays in full, whether it starts on screen or gets scrolled to. Same failure family as the documented fold-compression note about cards rendering at their start keyframe. **Three traps, all hit and fixed during the build:** (1) **the `transition` must live on `.visible`, not on `.rule-wipe`** — with both on `.rule-wipe`, adding that class animates the line from full width DOWN to zero over 400ms and the observer then retargets it to 1 mid-flight, so it wobbles near full width and the wipe never plays; declaring it only on the outbound state makes the collapse instant and the draw animated. (2) **observe the PARENT `.section-meta`, never the rule itself** — collapsed to `scaleX(0)` the rule has zero rendered area, so its `intersectionRatio` is permanently 0 and any non-zero `threshold` can never fire; it is unobservable precisely because it is hidden. (3) **the collapsed state is scoped to `.rule-wipe`, which JS adds at runtime**, so a JS failure leaves the line full-width rather than invisible; the CSS is additionally inside `@media (prefers-reduced-motion: no-preference)` and the JS guards on `reduce`. Verified: below-fold load → 0 then draws 0→0.51→0.83→1 on entry and stays drawn on scroll-back-up (one-shot, observer disconnected); above-fold load → plays and settles at 1. (4) **the observer needs `rootMargin: '0px 0px -25% 0px'` AND a `document.fonts.ready` gate.** Without the rootMargin the reveal fires at LOAD on any screen taller than ~860px (the header is at y≈855, already on screen), so the animation burns while the visitor is still reading the hero and is over before they scroll down — it "does not work" from the visitor's point of view even though it ran. Without the fonts gate the rootMargin is defeated anyway: until the webfont swaps, the hero is a different height and `.section-meta` sits ABOVE the trigger line, so the observer fires on that transient layout. Gating on `document.fonts.ready` (the same thing the hero scramble waits for) makes it read a settled layout. Verified at 1440×998: holds at scaleX 0 at scrollY 0, then draws 0→0.58→0.90→0.98→1 over ~400ms on scroll. **Two testing notes:** IntersectionObserver does NOT deliver callbacks when the browser-pane tab is not fronted — several "the observer never fires" readings during this build were that, not the code; and **`index.html` itself is never cache-busted** (the `?v=` only busts style.css), so a browser can serve stale HTML with none of the page's JS while the CSS is current — check `[...document.scripts].some(s => s.textContent.includes('<marker>'))` before believing a JS effect is broken. style.css **v=212→v=214**.
- **ui-skills polish pass (2026-07-26, first routed use of the UI Skills router — sources: `jakubkrehel/make-interfaces-feel-better` [MIFB], `emilkowalski/emil-design-eng` [Emil]; `leonxlnx/minimalist-skill` loaded and rejected as a light-mode generative protocol; `pbakaus/critique` discarded as harness-bound):** five changes, style.css **v=214→v=215**. (1) **Lightbox now animates (Emil):** it was `display:none→flex` with a `transition: opacity 0.2s` that NEVER ran (newly-rendered elements don't transition) — a full-screen jump-cut. Fixed with `@starting-style` + `transition-behavior: allow-discrete` (holds `display` through the exit fade), img settles 0.97→1 (motion-gated; modals scale from center per Emil's exception), **exit 160ms faster than enter 240ms**. **Longhands, NOT the `transition` shorthand, are load-bearing:** an unrecognized `allow-discrete` inside the shorthand would invalidate the whole declaration in older browsers; as a standalone `transition-behavior` line they drop just it and keep today's snap (verified working: mid-flight opacity 0.20 on open, display held at flex while fading 0.156 on close). (2) **`text-wrap: balance` on h1–h4, `pretty` on p/figcaption** (MIFB #10) — site-wide, zero prior usage; no-ops on the char-split home hero. (3) **Carousel dots: painted 24×4 bar moved to `::before` on a 24×20 transparent button** (MIFB #16) — 4px of vertical hit target was the hardest tap on the site; opacity states cascade to the bar so active/hover/`:active` rules needed no change; dots row stays 84px. 40×40 was NOT reachable without respacing the 11-dot rail — deliberate partial fix. (4) **Play cards get the home grid's `card-rise`** (Emil, cohesion — one motion vocabulary across sibling pages): same longhands/range on `.play-grid .play-card`; verified `.play-column`/`.play-grid` set no overflow so anonymous `view()` binds to the viewport (the `--card-view` trap does NOT apply here), and Play's first visible row loads past `entry 45%` so it renders settled, not faded. (5) **1px pure-white 0.08 hairline `outline` (offset −1) on `.play-card img` / `.deck-card img` / `.lightbox img`** (MIFB #11 — pure white, never tinted; outline not border so it draws on top and follows radius). **Deliberately NOT on home-grid imgs** — NOVA/Mercor are transparent PNGs where an outline would draw the box the alpha exists to avoid (verified home `outlineStyle: none`). Also: `.exp-date` got `tabular-nums` (adapted use — static dates, benefit is the About right-rail digit alignment, not layout shift) and the dead `.card-num` block was deleted (styled, zero HTML references). **Findings rejected in the same pass, do not re-apply:** Emil's sub-300ms rule vs the documented acknowledgment/scenery split; MIFB's concentric-radius formula vs the shipped 8/4 play-card radii (formula wants 24px outer — bubbly, off-brand); minimalist-skill's palette/serif core. the grid's dimensional discipline used to exist ONLY at 1440px — measured at 1024, Homewise's description ran 3 lines while PollenNav's ran 2, so their chip rows sat 24px apart and PollenNav carried **74px** of dead space below its chips vs Homewise's 21px (also broken at 1200 and 900). Cause: `.project-card` was `display:block`, so the grid's row-stretch slack landed as dead space *below* the chips with nothing to push them down. Fix is a 4-part height chain, all home-grid-scoped (`.project-card`/`.project-info` appear ONLY in index.html): `.project-card { display:flex; flex-direction:column }` + `.project-img { flex-shrink:0 }` (never compress the 16:9 plate) + `.project-info { flex:1; align-items:stretch }` (was `center`, which would vertically centre instead of filling) + `.card-meta { margin-top:auto; padding-top:14px }`. **Both `.card-meta` properties are required** — `margin-top:auto` does the pinning but collapses to 0 when there is no slack, so `padding-top` carries the original 14px minimum gap; do not merge them back into `margin-top:14px`. Result: `gapBelow` is a uniform 21px at 1440/1200/1024/900 (was 21/45/50/74). Where chip rows still start at different y (Homewise vs PollenNav at 1024), that is the chip row itself *wrapping* to two lines — bottoms stay aligned, which is correct. **Tradeoff accepted:** total card height is unchanged, but on a card with a shorter description the whitespace now sits *between description and chips* instead of below the chips. Side benefit: the two-line description ceiling is no longer a hard copy constraint (Mercor + New Craft had both been re-cut against it).
- **Sticky-stacked work rows — BUILT AND REVERTED 2026-07-26 (per user).** The 8 cards were wrapped in four `.work-row` divs that pinned under the nav at staggered tops (100/114/128/142px) so each row slid over the last, the covered row scaling to 0.955. Mobile was protected with `.work-row { display: contents }` (measured identical: 327px cards, first card y=588) and reduced-motion fell back to the flat grid. It worked, and the user rejected the look — the top of the viewport ends up permanently occupied by strips of previous rows, and because the cards lead with an image those strips are image fragments rather than a clean title edge. **Do not re-propose sticky/stacking the work grid.** Two findings worth keeping: (1) **`animation-timeline: view()` freezes on a pinned element** — a sticky element stops moving in the scrollport, so its view progress stalls at exactly the moment a stack effect should run; driving the scale needed a small rAF handler instead. (2) **Never fade a stacked card with `opacity`** (the user's call, and correct): transparency lets the row beneath ghost through, reading as a rendering bug rather than depth — scale is the only safe lever. Also: the recede progress must key off actual *overlap* (`(row.bottom - nextTop) / row.height`), not proximity to the next row, or a row starts fading while it is still the top card.
- **Progressive defocus on card exit — BUILT AND DELETED 2026-07-26 (per user).** Cards blurred (`blur(5px) saturate(0.6)`, no opacity) as they left the top of the viewport, via a second `view()` timeline animation (`card-defocus`, `animation-range: exit 0% exit 60%`) sharing `.project-grid .project-card` with the existing `card-rise`. It worked and cost nothing (pure CSS, no pinning, no added page height) — the user simply did not want it. **Do not re-add blur/defocus to the work cards.** One finding worth keeping if two timeline animations are ever stacked on one element again: **a timeline animation still FILLS outside its range**, so the two must own disjoint properties — `card-rise` owning opacity+transform while `card-defocus` owned only `filter` was required, or the exit animation clobbers the entry fade.
- **Project cards:** #1a1a1a background, 1px solid rgba(255,255,255,0.08) border, border glow on hover (box-shadow), image zoom + 3px lift on hover (`scale(1.04) translateY(-3px)`). Title 18px, year 16px right-aligned, description 16px muted with 8px gap. Description reserves 2-line min-height so the tag row below aligns horizontally across cards even when one description fits in a single line. **Thumbnail material language (2026-06-11):** floating screenshots cast a contact shadow on their plates (`.project-img > img { filter: drop-shadow(0 16px 28px rgba(0,0,0,0.16)) }`, deepens on hover — the direct-child selector keeps award badges/overlays exempt), and every flat plate fill uses PollenNav's two-radial light treatment in the card's own hue (`radial-gradient(ellipse at 30% 30%, LIGHT…), radial-gradient(ellipse at 70% 70%, DEEP…), BASE` inline on `.project-img`) — highlight upper-left, deeper pool lower-right, so the grid reads as six consistent lit scenes. New cards should follow both. StoryBloom is exempt from the plate treatment (full-bleed cover hides its plate). **NOVA is also an exception (2026-06-18): its plate is a flat `#FFFFFF`, NOT the two-radial treatment.** `images/nova-card.png` is a transparent product shot of the AR glasses with a soft drop shadow baked as if rendered on white, so the plate must be pure white — anything darker (incl. the radial treatment's lower-right pool, which lands right where the shadow sits) shows a light halo around the shadow. Keep NOVA flat white; do NOT "restore" the radial treatment.
- **Card meta chips:** `.card-meta` container holds 2–3 `.card-tag` chips below each project description. Tags are 11px uppercase, 1px stroke border (rgba 0.12), 4px radius, mute color (rgba 0.55). Pattern: `[Domain] · [Form factor] · [Status]` (e.g. `B2B SaaS · Dashboard · Shipped`, `Health · Mobile App · Award Winner`). Hidden on mobile (≤768px) since stacked cards already show full descriptions. **Project-type chip (`.card-tag--type`, added 2026-06-17):** a filled chip (bg + border `rgba(255,255,255,0.16)`, text `0.92`) that **leads** each home-card meta row, labeling the work `Professional` / `Personal` / `Academic` / `Freelance` (Freelance added 2026-07-31 for New Craft Society, per user). Set apart from the stroke descriptor chips by **fill weight, NOT color** — deliberate: the user asked whether to color it and the answer was no, because the site rule is *chrome stays neutral, color is reserved* (e.g. NOVA's `#0094FF`) and the colorful thumbnails already carry the grid's color (per-type color-coding was also rejected). Labels are authoritative from each case study's own meta Type field: Homewise + Litespace Coffee Chat = Professional, PollenNav = Personal, NOVA = Academic (its page says "Sponsored Academic", shortened to "Academic" on the card). StoryBloom + Countersign have no in-repo case study → labeled **Personal** by inference (Countersign = solo repo; StoryBloom = self-initiated 2026 product); revisit if either is really Professional/Academic.
- **Card award badges:** `.card-awards` is an absolutely-positioned (top:12px, left:12px) flex row inside `.project-img`. Default badge height is 26px (`.card-awards img { height: 26px }`). PollenNav uses this default for its row of 4 horizontal-wordmark badges (iF, Red Dot, NY, European). **Override for lone portrait badges:** when a card has a single portrait-orientation badge (e.g. StoryBloom's NY Silver 2026 medallion), set `style="height: 30px"` inline on that img — the lone medallion reads too small against a single thumbnail at 26px because portrait badges have ~2.5× less surface area than landscape wordmarks at equal height. 30px brings the visible area into the same order of magnitude as the surrounding card content without going as bold as 32–38px (which made the badge feel larger than the matching NY 2025 sibling on PollenNav). Parent `.project-img` must have `position: relative` for absolute anchoring to work. **StoryBloom badges updated 2026-07-22 (per user):** `images/reddot-winner-2026.svg` (Red Dot Winner 2026 stacked lockup, kept as vector; note the SVG has NO width/height attrs — fine in `<img>`, but a standalone/document render needs them injected) added ordered FIRST, before the NY medallion (order swapped per user). **Both StoryBloom badges use the `.card-awards` defaults (26px desktop / 16px mobile media query) as of 2026-07-22 (per user "same size as pollennav")** — the earlier inline `height: 30px` lone-medallion override was removed; inline heights beat the mobile media query and made the badges oversized on phones, so avoid inline badge heights on multi-badge rows. The StoryBloom cover was also replaced the same day with a user-recomposed 1920×1080 export (phones smaller, top-left clear for the badge row — the old cover's phones collided with the badges; rasterized from the user's Figma SVG via headless Chrome at 2×, downscaled to 1920 PNG lossless, 548KB, `?v=4` cache-bust on the img src). Full-bleed `object-fit: cover` treatment retained (a briefly-tried 65%-plate/Homewise-style cutout treatment was discarded when the new cover landed).
- **Chatbot:** Dark background, white icon, white rotating conic-gradient border (0.5 opacity). **The widget is a self-contained ~164-line block** (`<!-- Chat Widget -->` button + `.chat-panel` + its init script) pasted near the end of `<body>`, just before the pixel-dissolve script — it is NOT shared from style.css or a common JS file, so **every new page has to be given its own copy** (same class of omission as the GA snippet and the fontshare preconnect). **`nova.html` shipped without it and went ~2.5 months unnoticed** (added 2026-05-10, caught by the user 2026-07-28); `git log -S 'class="chat-toggle"' --all -- nova.html` confirmed it was never there rather than removed, and every other shared block on the page (GA, preconnect, favicon, back-to-top, `.page-end` footer, px-dissolve) was present. Fixed 2026-07-28 by copying the block verbatim from homewise.html. **The block is byte-identical on coffee / homewise / ticker / nova (md5 `dc03dc69dd0ab0dbf82e27e26950b2b3`, measured from `<!-- Chat Widget -->` to 3 lines past `chatMic.style.display = 'none'`) — pollen.html's differs and is the odd one out, so copy from one of the four, not from pollen.** Quickest audit for a missing widget: `for f in *.html; do echo "$f $(grep -c 'class="chat-toggle"' $f)"; done`. Note `/api/chat` 404s on the local python preview, so the panel opens but every message errors locally; test against production.
- **Layout:** Vertical single-column on about, single-column on play, 2-col project grid on home
- **Mobile:** Hamburger menu → full-screen overlay. No footer watermark on mobile.
- **About photos:** 4x2 collage grid, square aspect ratio (this is the **mobile** state of the photo deck — see below).
- **About photo deck** (`.deck` / `.deck-track` / `.deck-card` in `about.html` + inlined IIFE, added 2026-05-29): infinite editorial carousel of the 8 personal photos with three composable behaviors — **always-on auto-rotate + drag-to-rotate + an always-on hover "light shimmer" tilt**. Markup triples the 8 cards (24 total) so a full set always bleeds off both edges. Built from a reference (pearl-social-nine.vercel.app) but **adapted**: the reference hijacks the mouse-wheel because its carousel IS the whole viewport (`h-screen`, page non-scrollable); our deck sits mid-page, so wheel-hijack would trap scrolling. **We use drag-to-rotate instead** — a direction-intent guard commits to a horizontal drag (grab + momentum flick, friction 0.92) but releases vertical gestures back to the page (`touch-action: pan-y`), so scrolling is never trapped. Mechanics: continuous drift `SPEED = 48` px/sec via rAF with a sub-pixel accumulator (`acc += SPEED*dt`; `Math.floor` into `scrollLeft` so it never stutters) — **the drift NEVER pauses** (see history); seeded-middle on first paint (`offsetLeft + offsetWidth/2 - clientWidth/2`, in the load handler so no flash); symmetric wrap at `>=2*setWidth` / `<setWidth`. Per-frame magnetic focus (`focus = max(0, 1 - dist/halfSpan)`, `halfSpan = trackWidth*0.28`) written as `--focus` and driving `scale(0.82 + 0.22*focus)`, `opacity(0.62 + 0.38*focus)`, **squared**-falloff drop-shadow + warm-coral halo (`rgba(255,150,120,…)`, matches the footer-glow palette — deliberately NOT the reference's gold). **Hover tilt ("light shimmer"):** a global cursor (`mouseX/Y/mouseOver`) is tracked, and every frame `paintCards()` resolves which card the cursor is over and tilts it in 3D toward the pointer (`--tilt-rx/ry`, ±`TILT_MAX=11`°, lerped `0.14`) — recomputed per frame so the tilt tracks the cursor AND the drifting cards (the card shimmers as it passes under a still cursor; deck keeps moving). Requires `perspective: 900px` on `.deck-track` (the cards' DIRECT parent) — `overflow` forces `transform-style:flat`, so a preserve-3d chain from `.deck` can't reach the cards; without direct-parent perspective the tilt flat-skews ("just leans left"). Cards face flat at rest (rotate 0). Bottom-fade `mask-image` dissolves the deck into the page (use `filter: drop-shadow`, not `box-shadow`, so it renders through the mask). Sits at a balanced gap below the hero via `.deck { margin-top: -84px }` (pulls up into the hero's empty bottom padding so heading→deck ≈ heading→header; the shared `.page-hero` is untouched) — the mobile block resets this to `margin-top: 0` because the mobile hero is much shorter and -84px drags the grid into the heading. `.deck-track` uses `overflow-x: scroll; overflow-y: hidden` — the explicit `hidden` is required because `overflow-x:scroll` would otherwise force `overflow-y:auto`, making the deck a vertical scroll-container that traps the page's wheel. **Mobile (≤768px): the JS bails immediately** (`matchMedia('(max-width: 768px)')` → add `is-ready`, `return`) and CSS collapses the same markup to the original static 4×2 grid (`.deck-card:nth-child(n+9){display:none}` hides the 2 duplicate sets) — no drift/drag/tilt. Respects `prefers-reduced-motion` (static scrollable row). Deliberate exception to the "no motion on About" preference (which still holds for Skills/Tools chips) — the deck IS the focal surface here. **History (don't re-add the PAUSE):** a hover-*pause* (deck freezes on hover so you can hold-and-tilt a card, via a `driftFactor` eased 0↔1) was tried twice and removed — even eased, stopping/resuming the drift was the source of every "jump"/jolt the user reported. Decision (2026-05-29, after several back-and-forths): **the drift must never stop.** The hover tilt simply plays over the moving cards; at 48px/s a card crosses the cursor slowly enough to see the tilt without needing a pause. Keep it no-pause.
- **Play grid overflow bug (fixed 2026-07-26, UI-craft review P2 — long-standing, NOT a regression):** `.play-grid` used `grid-template-columns: 1fr 1fr`, and bare `1fr` means `minmax(auto, 1fr)` — a track can never shrink below its widest content. Whisper XR's 11-dot carousel rail has a hard min-content of 456px (11×24px dots + 10×8px gaps + 112px arrow-clearance padding), so the right column floored at ~498px: at 800px viewport the columns measured **274px vs 498px** and the right column ended at x=852 on an 800px screen, clipping ~72px off every right-column card. It was invisible in casual testing because `html { overflow-x: hidden }` swallowed the overflow (the page never showed a scrollbar). Fix = `grid-template-columns: minmax(0, 1fr) minmax(0, 1fr)` + `flex-wrap: wrap` on `.carousel-dots`. **Keep the `padding: 24px 56px` on `.carousel-dots`** — that 56px is clearance for `.carousel-arrow`, which is `position:absolute; left/right: 0`; zeroing it (as a naive min-content fix would) puts dots under the arrows. Verified 375→1920: columns equal at every width, zero clipped cards, carousels functional, desktop 656px columns unchanged. **Lesson: `overflow-x: hidden` on `html` hides layout overflow — test with `document.documentElement.scrollWidth > innerWidth`, not by looking for a scrollbar.**
- **Pinned horizontal Play — BUILT AND REVERTED 2026-07-26 (per user).** `.play-grid` was wrapped in `.play-pin` > `.play-stage` (sticky, 100vh, overflow hidden), `.play-column` collapsed with `display:contents` (the same trick mobile already uses for `--mo`), and the 20 cards became one 9,008px flex row panned sideways by a rAF handler reading `.play-pin`'s offset. It worked cleanly — stage pinned, strip panned 0→−7,662px, no overflow, carousels/lightbox/video-autoplay all intact, mobile untouched — and the visual was genuinely good (the GPT Image 2 pieces read far better at 420px in a gallery row than in the masonry). **Rejected on scroll cost:** page height went 7,400px → 9,566px, of which **8,564px is captive pinned scroll** (~25–30s of continuous scrolling to pass the section, versus a few seconds to skim the masonry), and only 3 cards are visible at a time instead of ~6. Same tension that killed the sticky-stack: more cinematic, less scannable — and for a page whose job is "here are 20 things I made", scannability wins. **Do not re-propose pinning/horizontal-scrolling the Play grid.** Untried dials if it ever comes back: pan faster than scroll (travel ratio 0.5×) or narrower cards (340px).
- **Play projects:** 2-column flex masonry via `.play-column` children of `.play-grid`. Each project wrapped in `.play-card` (var(--surface) bg, 1px border, 20px padding, 8px outer radius). Image on top via CSS order, info block below with 20px margin-top on `.play-info`. Info block is `.play-info-top` (flex row: h3 left + `.play-year` muted 16px right, 8px margin-bottom) followed by `<p>` description. Pattern mirrors home page work cards. Inner image radius 4px (concentric with 8px card). Click images to zoom via lightbox. `.play-card--desktop-only` (hidden ≤768px) thins the mobile stack: **Whisper XR** plus **four more cards added 2026-06-21 (per user) — Buffering Ocean, No Signal Summer, Escape Velocity, and TickerPulse** all carry the class, so they show on desktop but are hidden on mobile (HTML-only; the class already exists in style.css — no version bump). Carousel arrows sit below image in the dots row (bottom: 24px within 84px min-height dots area) — symmetric 24px spacing on both sides. **First-click jump fixed 2026-06-21:** carousel slides are `display:none` + `loading="lazy"`, so a non-active slide's image wasn't fetched until first shown; on the first arrow click the track collapsed (323px→0px on Whisper XR) then snapped back when the lazy image painted. Fix = give each slide `<img>` intrinsic `width`/`height` attributes (Whisper `1920×1080`, TickerPulse `1920×1258`) **plus `height:auto` in the inline style** so the aspect-ratio reserves the box before load. The `height:auto` is REQUIRED — without it the `height` attribute acts as a fixed presentational hint and the image renders at full 1080px (caught + fixed mid-change). HTML-only, no CSS/version bump; lazy-loading preserved. Same pattern applies to NOVA's `.final-carousel` if it ever shows the same jump. **Video cards grouped at the bottom 2026-06-22 (per user "all the video related things should under"):** the 4 video-player cards — **Futuristic Starship Cockpit, I Believe in a Thing Called Love, About the Teddy Bear, UN/FOLD** — were moved to the **end of column 2** so they're the last 4 cards on mobile (the masonry flattens col1-then-col2, so "bottom of mobile" = tail of col2). To keep **desktop** balanced after pulling 3 of them out of col1, the two desktop-only image cards **No Signal Summer + Escape Velocity were moved into col1** — they're `display:none` ≤768px, so this rebalances desktop (10/10 cards, ~6% height diff) with **zero mobile effect**. **Midjourney stays in col1** (it has `<video>` clips in its grid's bottom row but reads as AI-art, not a video — user's call; the question was scoped to the 4 true video players). HTML-only reorder of whole `.play-card` blocks, no CSS/version bump. **Curated into themed desktop sections 2026-06-23 (per user, "for desktop" reorders — SUPERSEDES the simple video-grouping above):** the desktop reading order is now **GPT2 art → UI screens → other → film videos**, with the 7 GPT Image 2 cards distributed ~evenly across both columns at the top (4 col1 / 3 col2) so the "AI art" band leads both columns, then the product/UI work, then the rest. **Final layout — col1:** Scope · Buffering Ocean · Octocrab · Golden Flux · Midjourney *(AI-art cluster; Midjourney sits directly under Golden Flux per user)* → Luma · Redbrick Coffee · TickerPulse · Market Street Booklet Kit · Missing Pavilion. **col2:** The Last Human Gestures · No Signal Summer · Escape Velocity · Whisper XR *(Whisper directly under Escape Velocity per user)* · CoPatible · Futuristic Starship Cockpit · Mirror · I Believe · About the Teddy Bear · UN/FOLD. **Key per-user placement rules baked in:** Whisper XR directly under Escape Velocity; **one carousel per column** (Whisper col2 / TickerPulse col1) for height balance — "TickerPulse should not be last" was read as keeping it in col1 repositioned off the bottom, NOT moving it to col2; the swap of Market Street↔Mirror put Mirror under Starship in col2 and Market Street in col1, which **dropped the earlier "Starship above Market Street" rule** (Starship now sits above Mirror — user-approved). **Starship Cockpit is no longer treated as a bottom-grouped "video"** — it's the interactive Figma-Make piece, placed mid-col2; only the 3 true film videos (I Believe / Teddy Bear / UN/FOLD) stay grouped at the very bottom (still last on mobile). Columns balance to **~2% height diff** (10/10 cards). All HTML-only reorders of whole `.play-card` blocks; no CSS/version bump. **Two more swaps 2026-06-23 (per user):** Market Street↔Mirror (above) then **Missing Pavilion↔I Believe** — so col1 now ends `…TickerPulse · Market Street Booklet Kit · I Believe in a Thing Called Love` and col2 ends `…Mirror · Missing Pavilion · About the Teddy Bear · UN/FOLD`. Both desktop columns now end on a film video; balance landed at **~1.3%**. **Independent mobile order — DECOUPLED 2026-06-23 (per user "give mobile its own independent order"; SUPERSEDES every "mobile = col1-then-col2 flatten" note above):** mobile no longer derives from the desktop DOM order. Each `.play-card` carries an inline **`style="--mo:N"`** (mobile-order int) in play.html, and the `@media (max-width:768px)` block renders `.play-card { order: var(--mo, 99); }`. **Desktop never consumes `--mo`**, so the 2-column desktop layout and the mobile sequence are fully independent — change one without touching the other. (This replaced the old `.play-card{order:1}` + `.play-card--m-feature{order:0}` mobile rule; `.play-card--m-feature` is now an **unused class**, left in markup, harmless.) **To reorder mobile:** edit the `--mo` numbers in play.html only. **To reorder desktop:** move `.play-card` blocks between/within columns as before; set the moved card's `--mo` too if its mobile spot should change. The shipped mobile order (themed, videos grouped at bottom): Scope · The Last Human Gestures · Octocrab · Golden Flux *(GPT2 art)* · Midjourney · Luma · Redbrick Coffee · CoPatible · Mirror *(UI)* · Missing Pavilion · Market Street *(other)* · **Futuristic Starship Cockpit · I Believe · About the Teddy Bear · UN/FOLD** *(all 4 videos last — note Starship IS grouped with the films on mobile here, unlike desktop where it sits mid-col2)*. The `--mo` map in play.html also assigns the 5 desktop-only cards logical values (they're `display:none` ≤768px so it's moot, but keeps the sequence sane if ever un-hidden). style.css bumped **v=191→v=192** across all 8 pages. **"Fish Ocean" card added + "CoPatible" deleted 2026-06-24 (per user):** Fish Ocean (a predator-prey ASCII-terminal ecosystem, "Made with Claude Code", live link `fish-ocean-gray.vercel.app`) is a **desktop-only** card placed in **col2 directly after Futuristic Starship Cockpit** (user first said "under TickerPulse", then corrected to after Starship). It's a **click-to-play** video card (NOT an autoplay loop): `<video src="images/fish-ocean.mp4" controls preload="none" poster="images/fish-ocean-poster.jpg" playsinline width="1280" height="804">` + a `.play-link` "Play it here ↗" — mirrors the homewise-live pattern so the **44MB** mp4 never auto-loads (`preload="none"` keeps it at readyState 0 until clicked; it's also excluded from the autoplay observer which targets `video[preload="metadata"]`). **Why click-to-play, not a ~3-4MB autoplay clip like Midjourney's:** the source is a 44s / 3456×2168 / 268MB screen recording; transcoded with `avconvert -p Preset1280x720` (720p = 2× the ~620px card display; 1080p was overkill → 86MB) to 44MB, poster grabbed via `qlmanage -t -s 1280` then `sips`→jpg. The user's copy had an em dash ("old course project — now living") → changed to a **comma** per [[feedback]] no-em-dash rule. **CoPatible was deleted** the same turn (whole `.play-card` block removed; `images/copatible.jpg` left as a harmless orphan). Net effect: Fish Ocean (+1 col2) then CoPatible (−1 col2) leaves **col2 back at 10**, columns **10/10, ~0.8% balance**. Mobile is unaffected by Fish Ocean (desktop-only); CoPatible's removal drops the mobile stack to **14 visible cards**. HTML + 2 new media files, no CSS/version bump. **"Running Dog" card added + UN/FOLD moved + Buffering Ocean hidden 2026-06-27 (per user):** (1) **Running Dog** — a SolidWorks tri-gear tilting mechanism (the project's real name; year **2020**, user-confirmed) added in **col2 directly under Missing Pavilion** (`--mo:15`, tie-breaks to sit right after Missing Pavilion on mobile too), a regular image card (shows on mobile). Copy: "A tri-gear mechanism modeled in SolidWorks. Rotating the primary crank transfers motion to two more gears in one linked movement, and a secondary crank on the central bar converts that rotation into vertical motion through a crank pin." Image `images/solidworks-trigear.jpg` (192KB, 1920px) from `~/Downloads/Frame 18 (1).png` — note the user **pasted a white-gap version in chat but the Downloads export already had BLACK gaps** between the 3 composite panels (what the user wanted), so no recolor was needed. **PIL is under `/usr/bin/python3`, NOT the default `python3` (now 3.14, no PIL).** (2) **UN/FOLD moved to col1** (appended after I Believe at col1's bottom) — both desktop columns now end on a film; `--mo:20` unchanged so it stays last on mobile (desktop-only change, zero mobile effect). (3) **Buffering Ocean hidden everywhere** — added inline `display:none` to its already-`--desktop-only` card (so now hidden on desktop too, kept in markup for easy restore). Net desktop balance: **10/10 visible, ~1.7%**. HTML + 1 new image, no CSS/version bump.
- **Play video clips:** Videos live in `images/` (no separate `videos/` folder). Markup pattern is `<video loop muted playsinline preload="metadata" style="…">` — **no `autoplay` attribute**: a small inline IntersectionObserver at the end of play.html calls `.play()` when a clip scrolls near the viewport (200px rootMargin) and `.pause()` when it leaves, so the ~10MB of clips never load on initial page view (2026-06-11 payload pass). `muted + playsinline` are still required for browsers to allow inline programmatic play; iOS stays non-fullscreen. Use the same `aspect-ratio:16/10;object-fit:cover;border-radius:8px` styling as the surrounding images so they sit in the same grid. The lightbox JS in `play.html` only handles `<img>`, so videos won't click-to-zoom — that's fine. Used in the Midjourney card to extend the 3-col still grid into a 3x3 grid where the bottom row is moving clips.
- **Case study patterns (shared):**
  - `.deliverable` — h2 caption + tight description above a full-width product image (Final Design walkthroughs on pollen.html and ticker.html, matches Coffee Chat Final Deliverables h2 style)
  - `.iteration-strip` — paired V1+V2 grid with full-width Final below (PollenNav Explorations)
  - `.voc-quotes` — 3-col grid of user quote cards with theme tag (TickerPulse Research)
  - `.pairing-annotated` — image left + annotation notes right with dashed arrows (Coffee Chat)
  - `.tool-cell` — name + small thumbnail in compare-table cells (Coffee Chat competitive analysis)
  - `.research-image-row` — flex row for 2+ research process images at the **same height but proportional widths**. Each `<figure>` has inline `style="flex: <aspect-ratio>"` (e.g. 1.14 / 1.35 / 1.66) and `flex-basis: 0`, so widths scale with each image's natural aspect ratio while heights stay equal. Each figure holds an `<img>` + a small muted center-aligned `<figcaption>` underneath as a label. Used in NOVA Research (field activities / interviews / affinity map).
  - `.problem-grid--stack` — 1-column variant of `.problem-grid`. Use when stacked full-width `.problem-card`s read better than 3 narrow cards — e.g. when the cards sit directly under a 3-image row and a 3-col grid would imply a 1:1 mapping that doesn't exist. Used in NOVA Research insights.
  - `.eval-matrix` (on `.compare-table`) — evaluation matrix table with `table-layout: fixed`, smaller padding/font, and a fixed 168px first column. Each cell pairs an `.eval-dot eval-dot--good/mid/bad` (8px circle, green/yellow/red — `#22C55E`/`#FACC15`/`#EF4444`) with the value text. Apply `.eval-row--highlight` to a `<tr>` to highlight the chosen approach (subtle blue tint `rgba(0,148,255,0.08)` + 2px `#0094FF` left accent on first cell). Used in NOVA Solution Evaluation Matrix.
  - `.comp-grid` — 2x2 image grid for competitive analysis (gap 16px, each image 16:9, center-aligned figcaption underneath). Stacks to 1 column under 768px. Used in NOVA Competitive analysis (Meta Orion / Snap Spectacles / Meta Ray-Ban / Apple Vision roadmap).
  - `.hud-grid` — 3x2 image+title+description grid for design exploration (e.g. HUD text styles). Title above image, image (16:9), description below. To highlight the chosen option, add `.hud-grid__item--selected` — applies a 3px `#0094FF` border + 1px rgba halo to **just the image** (not the whole card). Used in NOVA Challenge 1.
  - `.hud-example` — annotated example pattern: a numbered legend list (`.hud-example__legend` with `.hud-example__num` blue circles) placed **above** the image, with each `<li>` reading "[circle] **Label:** description." The matching numbered markers appear on the image itself. Used in NOVA Challenge 1 to walk through HUD Text in action.
  - `.phone-flow` — light-gray (`#E8E8E8`) panel matching the Companion app onboarding bg, holds 2–3 mobile screens in a 3-col grid (`.phone-flow__panel`). Each phone is wrapped in `.phone-flow__cell` (figure + dark `figcaption`) so labels sit **inside** the panel below the phone in small dark text. Used in NOVA Challenge 2 (Joint Onboarding / Safe Zone Setup / Safety Controls).
  - `.final-carousel` (on `.carousel` with `[data-carousel]`) — reuses the Whisper XR carousel pattern from `play.html` (`.carousel-track` / `.carousel-slide.active` / `.carousel-dots` / `.carousel-prev|next`) inside case-study Final Design sections. Modifier class only adds the standard 32px top-margin in `.project-content`; the carousel CSS is shared. The init JS (~25 lines: queries `[data-carousel]`, builds dots, wires prev/next click handlers) must be inlined in each page's script — there's no shared JS file. Used in NOVA Final Design (Companion app onboarding / Navigation mode / Freedom mode).
  - `.solution-diagram` — abstract system diagram for showing the product concept without real UI. CSS-only with two inline SVG glyphs (e.g. AR glasses + phone) flanking a dashed `#0094FF` connector with arrows. Each side has a label + small muted sub-label. Sits on a `.surface` panel. Used in NOVA Solution as a hero visual above the descriptive cards. Pattern: two abstract endpoints + bidirectional connector = "system with two parts that talk to each other."
  - `.partner-logos` — small partner-credit strip ("In collaboration with [logo] [logo]"). Logos render in **full original color at 60px CSS height** — no brightness/invert flatten, no opacity dim (changed 2026-05-11 per user; the brand colors carry signal). **Both logos must still be tight-cropped** so equal CSS `height` produces equal visible mark size — when a logo has internal padding (e.g. a tagline below the wordmark), the visible letters render smaller than the partner's at the same 60px box. Use `img.getbbox()` in PIL to verify the tight crop, or swap to a tighter-cropped source file. Used in NOVA Overview to credit CCA + UNICEF Innovation.
  - `.iter-example` — before/after iteration row (NOVA Usability Testing + Challenge 3). One `<h3>` title (optional), then 2-col grid with `<figure>` cells. Each cell: image + figcaption that starts with `<strong class="iter-example__mark iter-example__mark--bad|--good">Before:|After:</strong>`. The `::before` pseudo-element renders a red ✕ pill (`rgba(239,68,68,0.18)` bg + `#EF4444` text) or green ✓ pill (`rgba(34,197,94,0.18)` bg + `#22C55E` text) before the label. Two iter-examples stacked = paired before/after pattern from the framer reference. Also used in Challenge 3 (without mascot vs with mascot AR view). **Coffee Chat Iteration 1 & 2 were converted to this `.iter-example` format 2026-06-21 (per user, "same format as NOVA Challenge 3" with red/green labels)** — replacing the old `.before-after`/`.before-after-label` blocks (now unused CSS). Because the Coffee widgets are **transparent PNGs on a lavender (#D0D1FD) plate**, each cell wraps the image in a `.iter-plate` helper: a square (`aspect-ratio:1/1`) lavender plate with the image **absolutely positioned** (`inset:24px`, `object-fit:contain`) so a tall "before" screenshot can't blow the square out (an in-flow `%`-height image was overriding aspect-ratio and stretching the plate). NOVA's own iter-examples stay bare (full-bleed AR images, no plate).
  - `.mascot-exploration` — full-width figure for displaying the mascot character lineup. Inner `<img>` has `background: #E5E5E5` + padding so transparent-bg mascots render on a light gray "marketing display" panel matching the Companion app onboarding bg. Used in NOVA Challenge 3 below the before/after pair.
  - `.img-placeholder` (with `data-label` attribute) — dev-time placeholder for upcoming images. Dashed-border gray box with the label text from `data-label` shown via `::before`. Modifiers: `.img-placeholder--16-9` (AR view aspect), `.img-placeholder--mascot` (wide 16:7 panel). Swap with `<img>` once the real image lands.
- **Back-to-top button (`.back-to-top`, decoupled from the side-nav 2026-07-28):** the case-study back-to-top used to be hidden by `@media (max-width: 1440px) { .side-nav, .back-to-top { display: none } }` — a rule added way back in `7f936fc` whose comment ("hide side nav on smaller screens where it would overlap content") only ever justified the **side-nav**, which lives in the left margin. The button is `position: fixed` bottom-right and overlaps nothing, so it had been swept into the selector by accident and was invisible on **every common laptop width (1440 / 1366 / 1280)** — it only appeared above 1440px, which is why it looked fine in wide-window screenshots and "disappeared" on a normal laptop. The 1440 rule now hides `.side-nav` alone, and an explicit `.back-to-top { display: none }` was added to the ≤768px block so **mobile behaviour is unchanged**. Verified at 1700 / 1440 / 1024 (visible, opacity 1, hit-testable, `bottom: 280px` clearing the chat toggle at 220px desktop / 96px below 1136px) and 375 (hidden, no horizontal overflow). style.css **v=220→v=221**. **Gotcha:** the toggle JS is `scroll`-event-only with no initial call, so on a mid-page reload the button stays hidden until the first scroll — pre-existing, and it also means a probe must `dispatchEvent(new Event('scroll'))` after `scrollTo` or it reads a false negative.
- **Chat teaser:** Bottom-right tooltip bubble showing "Hey, I'm Chang's AI. Ask me anything." **Fires on FIRST SCROLL as of 2026-07-26** (was a 4s timer — at ~1280px viewports the bubble overlapped the hero headline, the rubric audit's one first-screen flag; scroll-triggered means it appears over the work grid, never the hero). `{ once: true, passive: true }` scroll listener; still hides after 6s, once-per-browser via localStorage, hidden on mobile (≤768px). A visitor who never scrolls never sees it — accepted trade.
- **Play lightbox:** Click any image inside a `.play-card` to expand. Dark 0.95 overlay, 8px radius close button (var(--radius)) with glassmorphism matching nav. Esc or click-outside to close.
- **Footer glow palette (2026-06-15, hero reflection):** vertical linear gradient — transparent → blue rgba(30,122,255,0.68) @30% → pink rgba(255,48,176,0.64) @52% → red rgba(255,40,70,0.60) @70% → amber rgba(255,190,35,0.64) @88% → amber rgba(255,190,35,0.64) @100% (**bottom stays warm, NOT transparent** — a transparent bottom stop caused a black gap at the page's bottom edge; fixed 2026-06-15). Reverse-orientation reflection of hero g2 (see Footer above). Replaced the old "Dusk" palette (violet/coral/purple/peach) when the hero moved to g2. To revert the whole gradient direction back to the old neutral footer, restore that Dusk radial set.
- **Footer pixel dissolve (`.px-dissolve`, 2026-06-15):** an animated "pixel dissolve" on the **footer only** — a `<canvas class="px-dissolve" data-edge="top" data-stop=".footer-big">` inside `.page-end` (right after `.footer-glow`) paints twinkling dark square "gaps" (`#0a0a0a`, 20px cells) over the **static** glow's top edge, dissolving the blue→dark transition into pixels **above "Say hello"** (`data-stop` keeps the band's bottom above `.footer-big`; `data-edge="top"` dissolves upward into the dark). The gradient holds still — only the squares animate (the AWS-Summit-card look the user referenced). One inline engine, **byte-identical on all 8 pages** (md5-matched), drives it via `requestAnimationFrame`, **pauses off-screen via IntersectionObserver**, has a `prefers-reduced-motion` static-frame fallback, is resize-aware, and is **shown + animated on mobile (≤768px) as of 2026-06-18** (the whole footer block used to be `display:none` on mobile; see Footer). A static-on-mobile variant was tried and reverted the same day — kept **animated everywhere** for one consistent signature; the battery/accessibility cases are already covered because the loop is IntersectionObserver-gated and still falls back to a static frame under `prefers-reduced-motion`. Canvas `z-index:1` (above glow 0, below content 2); the band sits above the text so it never occludes it. **Hero was deliberately left SMOOTH (no dissolve):** the hero gradient trails off right where the hero text sits, so an edge-dissolve there reads as "cut off" with a faint gradient clip-edge line — the footer's glow is a *bounded block* with a clean edge, so only it takes the effect cleanly. A hero version (frozen gradient + dissolve + removing the "Open to new opportunities" pill for room) was tried **and reverted 2026-06-15** (cut-off/line problem + it fought the pill for space); the **footer-only dissolve is the shipped decision** — a distinct, deliberate footer "signature" (hero/footer differing in treatment is fine — they're never co-visible and share the warm→blue palette; user OK'd it citing YC-site footers that differ from their hero). `style.css?v=156` across all 8 pages. To remove entirely: delete the `.px-dissolve` canvas + the engine `<script>` from each page and the `.px-dissolve` CSS rules.
- **Favicon (`favicon.svg`, updated 2026-06-15):** a warm→blue **vertical linear gradient** orb (#FFD600 → #FF1E3C → #FF3CB0 → #1E78FF) matching hero g2, replacing the old peach/coral/violet radial. Single file linked by all 8 pages; carries `?v=2` cache-bust on the link (favicons cache hard — new visitors get it immediately, returning visitors after a hard refresh).
- **Cache version (2026-06-15):** `style.css?v=159` set **uniformly across all 8 pages** (previously scattered 46–146, then 147, 150→151 footer `.page-end` restructure, 152→156 footer pixel-dissolve, 157→159 mobile-hero gradient + headline fixes), so every page busts cache. **Note:** the CSS is cached hard per `?v=` — when changing `style.css`, bump the version on the page you're previewing or the browser serves the stale stylesheet (a `?cb=` on the HTML URL alone does NOT bust the linked CSS). The **og-image was regenerated 2026-06-21 to the hero g2 palette** (warm→blue with the hero's true-red band, `?v=4`) — the old violet/coral cohesion gap is now closed. **v=160 (2026-06-17):** home-card project-type chips (`.card-tag--type`). **v=161 (2026-06-17):** Homewise pitch-deck enrichment (`.hw-stats` market band + `.hw-flow` map) + the strengthened type-chip fill — both set uniformly across all 8 pages. **v=162–167 (2026-06-17):** iterations of the Homewise Problem/Solution rework — stat band boxed→boxless→**3 cards**, scannable pain headlines (`.voc-quotes--pain`), `.hw-flow` removed, enriched Solution + `.solution-coda`. (The 2026-06-17 Track clip + live-demo embed were HTML/asset-only, no further bump.) **All 8 pages were at v=167.** **v=168–171 (2026-06-18, this session):** mobile-footer work (glow + pixel-dissolve brought to mobile and made animated everywhere; glow band 130→180px) and the home-grid Coffee Chat ↔ Countersign swap + `.project-card--desktop-only` mobile-hide rule. **All 8 pages were at v=171.** **v=172–178 (2026-06-20):** `.case-cover` consolidation, page crossfade view-transition, About polish pass. **v=184 (2026-06-20, this session):** About rework — Skills/Tools deleted; Experience/Education/Recognition → inline "name · detail · date" rows (subgrid removed); Testimonials moved above Books/Podcasts; `--text-soft` mid-tier grey; `.media-section--end` footer gap. All changes are About-scoped (other pages' rendering unaffected); version set uniformly across all 8 pages. **All 8 pages were at v=184.** **v=185 (2026-06-20, this session):** Books + Podcasts hidden on mobile (`.media-section{display:none}` ≤768px). **All 8 pages were at v=185.** **v=186 (2026-06-20, this session):** nav "Get in touch" CTA removed site-wide + header "Specular Glass" material (top-lit fill, `saturate(1.4)` blur, specular box-shadow stack, `nav::before` gradient ring, opaque fallback) + persistent `.nav-active` underline tick. **All 8 pages were at v=186.** **v=187 (2026-06-20):** scroll-condense added. **v=188 (2026-06-20):** scroll-condense made width-only (no height/position change, per user) + fixed the Specular-Glass `box-shadow`/`nav::before` leak onto the case-study `.side-nav`. **All 8 pages were at v=188.** **v=189 (2026-06-20, this session):** scroll-condense REMOVED entirely (user "too annoying") — CSS block + 8 inline scroll scripts deleted; static Specular Glass + active underline + the `.side-nav` leak fix remain. **All 8 pages were at v=189.** **v=190 (2026-06-20, this session):** nav width 760→700px (static "after-shrink" size kept, with the lighter "before-shrink" glass opacity). **All 8 pages were at v=190.** **v=191 (2026-06-21):** Coffee Chat Iteration 1 & 2 before/after converted to the `.iter-example` red/green format + new `.iter-plate` square-plate helper. **All 8 pages now at v=191.** **v=223 (2026-08-01):** Mobbin vibe pass (card titles 21px, 72px hw-spread figures, About eyebrows + the later-reverted featured quote). **v=224 (2026-08-01):** interaction six-pack + 404.html — see those bullets. **v=222 (2026-07-31):** premium-research quick-win pass (unified label token, type-ramp middle, hero blur 32, hero meta row, live status pill, footer info line; the mono-tier trial that shipped alongside was REJECTED and reverted 2026-08-01) — see the Premium-research bullet. **All 8 pages now at v=222.**
- **Interaction state layer (added 2026-07-26, UI-craft review P3, end of style.css):** before this the 84KB stylesheet had ZERO `:focus-visible` rules, ZERO `:active` rules, and exactly one `outline` declaration (`outline:none` on `.chat-input`) — keyboard users got Chrome's stock #005FCC ring, an off-system blue. Now: (1) a global `:focus-visible { outline: 2px solid rgba(240,240,240,0.9); outline-offset: 3px }` — **white per the chrome-stays-neutral rule, NOT #0094FF**; `outline` (not box-shadow) so it never collides with component shadows and follows border-radius; placed at the END of style.css to win the tie against `.chat-input{outline:none}`; verified with real Tab keypresses (programmatic `.focus()` does NOT trigger `:focus-visible` — test with actual keyboard input). (2) `:active { scale(0.96) }` press feedback on small controls only (carousel arrows/dots, hamburger, lightbox-close, chat buttons) — large cards deliberately excluded, scaling a 660px card on press reads as a glitch. (3) `-webkit-tap-highlight-color: transparent` on body. (4) Four touch-leaky hover rules (`.play-card:hover`, `.photo-wrap:hover img`, `.carousel-arrow:hover`, `.lightbox-close:hover`) wrapped in `@media (hover: hover)` — untreated, a tap on mobile left the card's border+glow stuck lit while scrolling. All additive; new interactive components should get focus-visible for free (global rule) but need their own `:active` + hover-gating. style.css **v=193→v=194** (v=193 was the same-day tablet-rail + play-grid fixes).
- **Scroll-driven card reveals (added 2026-06-11, end of style.css):** pure-CSS progressive enhancement — `.project-grid .project-card` rises 18px + fades during viewport entry via `animation-timeline: view()`, `animation-range: entry 5% entry 45%`, gated behind `@supports` + `prefers-reduced-motion`; unsupported browsers get the site unchanged. **Two traps, do not regress:** (a) use animation **longhands** with `animation-duration: auto` — the `animation` shorthand's implicit 0s duration instantly finishes a timeline-driven effect (fill-mode then pins the end keyframe; looks like "animation does nothing"); (b) **`body` must NOT get `overflow-x: hidden`** — a non-visible overflow makes `<body>` a scroll container, and every `view()` timeline attaches to it instead of the viewport, silently breaking all scroll-driven animations. Horizontal clipping lives on `html` alone (identical clipping; verified no horizontal overflow). Comments at both spots in style.css explain this. **History + current state:** cross-document view transitions first shipped as a **paired `view-transition-name` card→cover MORPH** on 2026-06-11 and were removed the same day per user (the card thumbnails and the covers are *different* images, so the morph read as a glitchy crossfade-zoom). **Re-added 2026-06-20 as a plain root CROSSFADE (NOT the morph)** — `@view-transition { navigation: auto }` + custom `::view-transition-old/new(root)` animations at the end of style.css (old fades out 0.3s; new fades in 0.34s; **PURE OPACITY, no transform**; keyframes `vt-fade-out`/`vt-fade-in`). **The root snapshot must NOT translate:** a 12px "fade-up" rise was tried first but it lifted the `position:fixed` chrome — the floating nav, the chat button, the case-study side-nav — 12px on every navigation (fixed elements ride the root snapshot, so the nav visibly jumped), so it was changed to a still crossfade (fixed 2026-06-20). Site-wide on same-origin navigation (work card→case study, nav links, back-to-home), progressive enhancement (unsupported browsers navigate instantly), gated off under `prefers-reduced-motion`. **Do NOT re-add the paired-name card→cover morph — that's the rejected version; the user chose the crossfade.** **Also do NOT re-add the 12px root fade-up** — it makes the fixed nav jump (see above). style.css bumped v=172→v=173 (first ship) → v=174 (rise removed).

## Links
- LinkedIn: https://www.linkedin.com/in/chang-mou/
- Email: changmou627627@gmail.com
- Resume: https://drive.google.com/file/d/13UgKrgvZFLd8lhiJlr9OQ_S4UGyCcK2I/view?usp=sharing
- X: https://x.com/changmou627 (added to footer 2026-05-14, all 7 pages)
- Vimeo (play page): https://vimeo.com/441663647, https://vimeo.com/486270690

## Deployment
- **Live URL:** https://www.chang-mou.com (custom domain via Squarespace DNS → Vercel)
- **Vercel URL:** https://chang-mou-portfolio.vercel.app
- **GitHub repo:** https://github.com/chang627627/portfolio-2026
- **Auto-deploy:** Push to `main` on GitHub triggers Vercel deploy automatically
- **Manual deploy:** `npx vercel --prod --yes` (from project folder)
- **`vercel.json` rewrites:** Used for clean URLs on specific pages (e.g., `/nova` → `/nova.html`). Add new rewrites here when a page needs an extension-free URL. Site-wide `cleanUrls: true` was avoided because it 301-redirects every `.html` URL and would break existing shared links.
- **SEO / AI-readability layer (added 2026-07-26, improvement-research P6):** `robots.txt` (allow all + sitemap pointer — deliberately NO Disallow for ticker.html, or crawlers could never see its noindex), `sitemap.xml` (7 canonical pages: / about play coffee pollen nova homewise; excludes ticker/coffeeslide/og-gen), `llms.txt` (markdown site+projects summary for AI assistants — update it when the project lineup or roles change, same trigger as the chat prompt), self-referential `<link rel="canonical">` on all 9 HTML pages (`.html` variants are canonical since internal links + og:url use them; coffeeslide's canonical is the clean `/coffeeslide` matching its og:url; canonicals resolve the `/nova`-vs-`/nova.html` duplicate routes from vercel.json), JSON-LD (Person on index.html — jobTitle/address/alumniOf/awards/sameAs LinkedIn+X/knowsAbout; CreativeWork on the 4 case studies with each page's own meta description), `<meta name="robots" content="noindex">` on **ticker.html** (deliberately-unlinked page was crawlable out of context) and **og-gen.html** (internal tool that ships to prod), and index.html's meta description upgraded to carry name/role/location ("Chang Mou is an AI-first product designer in San Francisco…" — og:/twitter: descriptions kept as the shorter social copy on purpose). New pages need: canonical + sitemap entry + (if a case study) CreativeWork JSON-LD.
- **Vercel project:** chang-mou-portfolio
- **Domain registrar:** Squarespace Domains (domains.squarespace.com)
- **Analytics:** Google Analytics 4 (Measurement ID `G-RRQ4XH93F3`) injected in `<head>` of all six HTML pages. Any new page needs the gtag snippet added.
- **Font loading:** every page carries `<link rel="preconnect" href="https://cdn.fontshare.com" crossorigin>` immediately before the Fontshare stylesheet link (the font binaries live on cdn.fontshare.com, a different origin than the api.fontshare.com CSS — preconnect shortens first-visit FOUT). New pages need this line too. index.html loads weights 400,500,700 (600 was never used in CSS and was trimmed); other pages load 400 only.
- **Social share card:** `images/og-image.jpg` (1200x630, used as the LinkedIn/social thumbnail). Redesigned 2026-06-12: hero-mesh hues filling the ENTIRE frame per user ("do not let black come in") — deep-violet linear base under four wide corner pools, full-bleed inset so blur never shows an edge; reads violet→coral with no dark areas (straight-copied hero alphas over #0a0a0a rendered too dark as a static frame) + centered two-line lockup — "AI-first product designer." (white) / "Building what's next." (muted 0.5). **Card carries no other text** per user ("do not say other things") — the earlier status pill, sub-line, and "Chang Mou" signature were removed; don't re-add them on regen. Referenced via og: and twitter: meta tags on all eight pages. New pages need the same meta block. **Regenerated 2026-06-21 to the hero g2 palette** (the `.mesh` in `og-gen.html` now stacks the hero's exact g2 bands — yellow → **true red `rgba(255,30,58)`** → pink → blue as wide horizontal bands so the red reads as strongly as the live hero. A first pass used a soft pink-red corner pool that washed the red out (user: "my hero's gradient has some red, why this one does not"), fixed same day by mirroring the hero's `.hero-orbs--g2` band colors/alphas. Replaced the violet/coral mesh that no longer matched the site). **To regenerate:** edit the `.mesh` in `og-gen.html`, sync it to `/tmp/portfolio/` (preview server on 8888), then render with headless Chrome — `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --hide-scrollbars --force-device-scale-factor=2 --window-size=1200,630 --virtual-time-budget=6000 --screenshot=/tmp/og.png "http://localhost:8888/og-gen.html"` (the `--virtual-time-budget` lets the Fontshare webfont load) → 2400×1260 PNG → `sips -z 630 1200 /tmp/og.png -s format jpeg -s formatOptions 95 --out images/og-image.jpg`. (The older `node /tmp/og-snap.mjs` puppeteer path is gone — its `/tmp/ogsnap` deps were cleared; Chrome headless replaces it.) **Cache-busting:** the og:image/twitter:image URLs carry a version param (`og-image.jpg?v=4` as of 2026-06-21, all 8 pages) — bump it whenever the image changes, because X/LinkedIn cache cards per-URL for ~a week and X no longer has a force-refresh tool (LinkedIn can be forced via Post Inspector).

## Dev Notes
- Node.js v24.14.0 installed via nodejs.org
- Preview server serves from `/tmp/portfolio/` — sync files there after edits
- Git initialized — commit before major changes
- Cache-busting: current `style.css?v=68` on play.html, v=48 on index.html — bump version after CSS changes. Keep version consistent per page.
- GitHub CLI: `/tmp/gh/gh_2.89.0_macOS_arm64/bin/gh` (not in PATH, use full path)
- Framer reference site: https://aware-acknowledge-592065.framer.app/
- Preview server: launched via `.claude/launch.json`, which now runs **`python3 .claude/serve.py`** - the script lives IN THE REPO and serves the repo directory directly. **NEW WRINKLE (2026-08-06): the preview harness sometimes resolves `launch.json` from the PRIMARY checkout instead of the worktree — and the primary at `/Users/changmou/Desktop/portfolio 2026` is months stale (pre-2026-08-02 config demanding `/tmp/portfolio/serve.py`) and holds uncommitted parallel work (do NOT pull or edit it). Recovery when the server fails with "can't open /tmp/portfolio/serve.py": recreate `/tmp/portfolio/serve.py` (threaded, dotfile-403, chdir /tmp/portfolio) + rsync the worktree there (no --delete), then preview_start — both config paths then work. The durable fix is the user pulling main in the primary checkout someday.** **Changed 2026-08-02 after `/tmp/portfolio/serve.py` was wiped twice in minutes**, breaking the preview both times; serving the worktree also removes the rsync-after-every-edit step and the whole class of stale-preview bugs it caused. `ThreadingHTTPServer` is still REQUIRED (single-threaded serves video-heavy pages sequentially and they look broken), and the handler 403s dotfile paths so `.git`/`.claude` are not served. The old `/tmp/portfolio` staging copy is obsolete; ignore the sync instructions below. The script `chdir`s to `/tmp/portfolio` before starting `http.server` to avoid getcwd permission errors. Sync files there after edits. **Use `http.server.ThreadingHTTPServer`, NOT the single-threaded `TCPServer`/`SimpleHTTPRequestHandler` default (fixed 2026-06-27):** the single-threaded server serves one request at a time, so pages with many large video clips (homewise ≈135MB across 7 clips, the Play page's clips) download sequentially and crawl — the clips render as empty 150px boxes and look "disappeared" (`networkState:2`/`readyState:0` while they queue). Threaded server serves them in parallel and they load fine. This is a **preview-only** issue (Vercel serves in parallel; production is never affected). If clips look missing in preview, suspect the server is single-threaded before suspecting the markup. **`/tmp/portfolio` also gets wiped periodically** (lost `serve.py` AND most of `images/` twice on 2026-06-27) — when the dev server fails to start or images 404, re-sync from the worktree (`rsync -a --exclude=.git --exclude=.claude ./ /tmp/portfolio/`, NO `--delete` or it removes `serve.py` which lives only in /tmp) and recreate `serve.py` (threaded) if missing.
- Do not push to git unless user explicitly asks
- **Portfolio audit checker:** When the user asks for portfolio suggestions or feedback, reference the rubric from `~/.claude/skills/portfolio-audit/SKILL.md` (from https://github.com/hey-stefan/portfolio-audit). Cite which principle each suggestion comes from. When suggestions are personal interpretation rather than direct rubric items, say so explicitly.
- **UI Skills router (added 2026-07-26):** `.claude/skills/ui-skills/SKILL.md` is the routing skill from `npx ui-skills` (ibelick), checked into the repo so it loads every session. Before visual/layout/typography/motion/interaction work, route to the **smallest useful** expert skill: `npx ui-skills categories` → `npx ui-skills list --category <c>` → `npx ui-skills get <slug>`. Prefer 1 skill, never more than 3. Registry is 185 skills / 47 authors / 26 categories as of 2026-07-26. **Two gotchas recorded in the SKILL.md itself:** (a) `get` resolves top-level slugs ONLY — companion files a skill links to (e.g. `emilkowalski/review-animations` → `STANDARDS.md`, which holds its actual easing/duration tables) return "Skill not found", so such skills load only partially and the missing numbers must NOT be invented; (b) several skills carry generic rules that **contradict deliberate decisions in this file** — most sharply "UI animations must be under 300ms" versus the documented acknowledgment/scenery split (hover answers in 160ms, decorative travel like the 800ms image glide and 400ms plate light stays slow on purpose). Sort any skill's findings into "real" vs "conflicts with a documented decision" and defend the latter rather than silently complying.

## Recent audit-driven changes (April 2026)

**Coffee Chat:**
- Replaced Rule-Based vs ChatGPT-Enhanced table with annotated pairing card image (.pairing-annotated component with dashed arrow annotations)
- Added product thumbnails (Donut, Watercooler, Gather, Focusmate) to competitive analysis table via `.tool-cell`
- Tightened Exploration 1 and 2 paragraphs to 2 sentences each
- Kept `align` padding equal (48px) on design system and pairing image containers

**PollenNav:**
- Collapsed Exploration V1/V2/V3/Final sections into `.iteration-strip` (paired early versions + full-width Final)
- Merged 7 Final Deliverables subsections into a single Final Design walkthrough using `.deliverable` component (h2 captions, 1-sentence descriptions)
- Renamed Constraints section → "Challenges" with "Three challenges shaped the design direction" framing
- Rewrote Overview to "one billion people" hook (cut hero redundancy)
- Tightened What I Did from 4 generic bullets to 3 specific
- Sharpened Reflection to one project-specific insight about making invisible data tangible
- First deliverable heading establishes "Maya" (so subsequent "She..." captions have referent)
- Merged closing beat into walkthrough as final caption

**Case study section order (May 2026):**
- **RESTRUCTURED 2026-07-27 (audit finding #3) — the three orders below SUPERSEDE the pre-restructure lists.** All three legacy studies went to **10 sections**, matching homewise.html's shape. **Compression and renaming ONLY:** the process-then-payoff arc is untouched (the April-2026 "show the win first" inversion stays reverted) and NO TL;DR/summary layer was added (rejected 2026-07-26 as repetitive). **The rename is the point:** side-nav links like "Exploration 2" / "Challenge 1" advertise process volume rather than what got decided, and they are the single loudest Behance-template tell — a reader clocks the era from the nav rail before reading a word. Every new name was lifted from an h2 already on the page. Do NOT reintroduce numbered section labels on any case study.
- **Coffee Chat mentor-framework pass 2026-07-28 (HTML-only, style.css untouched, no version bump)** — same framework and same workflow shape as the homewise pass. **Measured premise correction: coffee.html is LIGHTER than homewise.html (1114 vs 1432 words), not heavier.** Its problem was never volume, it was beat density and repetition, so the pass cut *beats*, not words (net −30). Shipped: **(1) The three `.problem-card`s were one causal chain split into three parallel boxes** (card 3 causes card 1, card 2 is its consequence) and named no difficulty at all, so a hiring reader concluded the problem was easy and every decision downstream read as taste. They now name the three real hardnesses, each lifted from a sentence already on the page downstream: "Nobody wants to go first" (social pressure, from the Research caption), "A good pairing has to be inferred" (thin signals, from the GPT-3 section), "Scheduling has two sides" (from the user-flow intro + the flow chart's own ✕ tags). **Deliberately NOT homewise's "A user problem / A data problem / One connected system" triad** — a literal clone across two case studies makes the template visible. The GPT-3 sentence and the user-flow intro were trimmed of what the cards now carry. **(2) The four `.goals-grid` cards were DELETED** — they were a table of contents for the three sections that follow, and the fourth ("Cross-Department Matching") was a promise the page never pays off. The section, its label, h2 and the two-week constraint sentence all stay; that sentence is now the section's whole payload. Same defect PollenNav's Challenge cards had. `.goals-grid`/`.goal-card` CSS is now unused on this page (still used nowhere else — safe to leave). **(3) "What I did" bullets 3-4 stopped spoiling two payloads** (the pairing reveal ~148 lines early, and both removals). **(4) The removal license moved out of the Reflection** — the page makes four removals and the sentence licensing them all sat after every one, so it now extends the two-week constraint at its source ("…and anything that added friction had to go"); Timeline field became "2 weeks · Sep 2023" so the best scoping fact is on the first screen. **(5) Overview leads with the bet** ("Coffee Chat is the 0-to-1 feature of Litespace, …" — was behind a sentence defining SaaS), and the prose `.highlight` moved off the market gap onto the scoped statement, since it is the page's only skim affordance in prose. **(6) The "pressure free" thesis, asserted 4× and argued 0×, is now attached to the two decisions that deliver it**: default-to-Going ("so nobody has to be the one who says yes") and no-reason-on-decline ("Asking for one would put the pressure back"), plus silence-as-a-supported-exit promoted out of an SVG branch label into the sentence beside it. Prose, **not** a stated-principles list — that shape was rejected on homewise. **(7) REAL BUG FIXED: the 2026-07-27 restructure renamed the side-nav but never the on-page `.section-label` eyebrows**, so two consecutive sections both read "Exploration", the third read "Iteration", and clicking "The user flow" in the rail landed on a section headed "Exploration". All three now match the rail verbatim (anchor ids `exploration-1/-2`, `iteration-1` deliberately unchanged). **Lesson: when renaming sections, grep BOTH `side-nav-link` and `section-label` — they are separate strings and the 2026-07-27 pass only did one.** Also gave the last two Final Design deliverables the headings they were missing entirely. **Killed by the judge, do not re-propose:** importing the deck's $4.5M stat onto the page, minting an 11th section for the insight, collapsing/reordering the five Final Design deliverables, deleting either before/after pair, and adding interview evidence that does not exist.
- Coffee Chat (13 → 10): Overview *(absorbs the old Impact grid, so 63%/200+ land in the first screen — Homewise has no separate Impact section either)* → Problem → Research → **Goals and constraints** *(the two-week pre-rebrand constraint folded in as one line)* → **Meaningful pairings** *(ex-Exploration 1)* → **The user flow** *(ex-Exploration 2)* → **What testing changed** *(both Iterations merged, both before/after pairs intact)* → Final Design → Design System → Reflection.
- PollenNav (12 → 10): Overview → **Recognition** *(the old "Impact" — it holds four award cards, NOT outcome stats, so it was renamed rather than folded into Overview; folding would open the case study on a wall of awards, against the no-award-bragging rule)* → Problem → Research → **Goals and challenges** → **Making the data readable** *(Explorations 1+2 merged, both solve data legibility)* → **Approachable, not clinical** *(ex-Exploration 3, the visual-identity beat, genuinely separate)* → Usability Testing → Final Design → Reflection. **Structural note:** the three Challenge cards mapped 1:1 onto the three Explorations and each exploration intro already restated its own challenge, so Challenges was functioning as a table of contents for the next three sections; it now reads as part of the brief. **KNOWN UNFIXED:** `images/pollen-playbook.jpg` still renders "How might we equip people like Maya…" *inside the image* — the HTML text was removed but the phrase survives in pixels, and it needs a re-export from Chang. Lesson: verify rendered output, not just markup, when removing copy.
- TickerPulse keeps its original order (Background → Problem → Research → Solution → Final Design → Reflection). Its Solution block is a narrative bridge to the finals; moving Final Design would orphan it.
- UNICEF NOVA (12 → 10 on 2026-07-27): Overview *(absorbs the old Impact grid)* → Problem → Solution → Research *(absorbs Ideation as its closing beat)* → **HUD text** *(ex-Challenge 1)* → **Independence vs tracking** *(ex-Challenge 2)* → **The mascot** *(ex-Challenge 3)* → Usability Testing → Final Design → Reflection. **Two real fixes beyond renaming:** (a) **duplicate stats removed** — Impact and Usability Testing both carried an 80% card with near-identical wording ("more comfortable" vs "feel more comfortable") and both counted the same 20 parents; Impact now leads under Overview, Usability's duplicate grid is deleted, and its one unique fact ("5 major problems solved") was folded into that section's prose. Verified 80% now appears exactly once. (b) **Ideation's "How might we…" was a text HMW** (unlike PollenNav's, which is baked into an image) and is now a declarative stating the same four requirements. The pre-restructure order was: Overview → Impact → Problem → Solution → Research → Ideation → Challenge 1/2/3 → Usability Testing → Final Design → Reflection. Mirrors the framer reference: Solution sits right after Problem so the system is named early, then Research justifies it. Uses `.problem-grid--2col` modifier (CSS: `grid-template-columns: repeat(2, 1fr)`) for the two-card "For children / For parents" Solution layout. Each Challenge ends with a parent-quote in `.table-caption` style. **Trimmed 2026-06-18 (portfolio-audit pacing pass — surface the AR work sooner):** the **Design Goals section was DELETED** (the "Four success metrics" `.goals-grid` 4-card block + its `#design-goals` side-nav link; it was the weakest-earning section, restating the independence-vs-reassurance balance Problem/Solution already establish, so Ideation's "Design opportunity" now hands straight to Challenge 1 — `.goals-grid`/`.goal-card` CSS is now unused, left in style.css). **Usability Testing was reduced to a validation summary:** its two `.iter-example` before/after blocks (Simplifying AR dialogue, Enhancing safety setup) were **removed** (they re-covered Challenge 1/2 ground), and the impact-grid's "2 Major iterations: simplifying AR dialogue, rebuilding safety setup" stat became **"5 — Major problems solved from usability feedback, from navigation cues to the hardware concept"** (an earlier 5-item `.project-list` of those problems was added then removed per user; the heading "Iterative feedback to refine the experience" + intro were KEPT). **That sentence was rewritten 2026-08-04 because the promise had gone unpaid.** Removing the list was fine while the Impact grid still carried the `5 — Major problems solved` card, but the **2026-07-27 dedup pass deleted that card** (only 80%/20+/50+ survived, in Overview), leaving a sentence that promised an enumeration the page delivered nowhere. The intro now ends "…and it pushed the hardware concept toward thinner, more child-friendly glasses" and the "five major problems" phrasing is gone. **The five (user-supplied 2026-08-04) are: navigation cues clarified · safety alerts, SOS and danger warnings · companion-app onboarding, safe zones and parent-child contact · reduced visual clutter · hardware refined toward thinner, more child-friendly glasses. Four of the five restate Challenge 1 / Challenge 2 / the Final Design deliverables, which is exactly why the list was cut — only the hardware refinement is new information, so it is the only one named.** Do NOT restore the full list. **Lesson, and the second recorded instance of it: a later pass can invalidate an earlier pass's premise.** 2026-06-18 removed the list *because* the stat card carried the number; 2026-07-27 deleted the stat card without knowing that; neither pass could see it had left a dangling promise. Same family as the homewise "audit recommendation's rationale invalidated by a later edit in the same session" note. Challenge 3's own `.iter-example` (mascot before/after) is untouched.
- **Reflection convention, unified 2026-08-04 across all four case studies (per user; HTML-only, no version bump).** Three rules now hold everywhere. **(1) The h2 is a CLAIM, never "What I learned."** The 2026-07-27/28 passes converted section h2s to claims but missed Coffee Chat and PollenNav, which still trailed off into the generic label. **(2) The body reflects the WHOLE case study and closes on what Chang learned AS A DESIGNER** — not one section's decision. **This rule exists because the first attempt got it wrong and the user corrected it.** The pattern was derived from Homewise and NOVA as "insight → that is why [specific decision] → takeaway", which produced a Coffee reflection aimed at the design system and a PollenNav one aimed at a single exploration — both single sections near the end of their pages, i.e. NARROWER, not broader. What actually makes Homewise and NOVA work is that they generalize the case study's **central** tension into a transferable craft lesson. **(3) Length follows from covering the arc**, roughly 75-125 words; padding a narrow reflection is the wrong fix. Final state: NOVA 124w "The most protective version was the one no child would wear" · Coffee 98w **"When the barrier is social, the design work is subtraction"** (walks the four removals — explained match, no message thread, default-to-Going, no reason on decline — then generalizes, with the mid-rebrand tokens-and-components lesson as a subordinate second beat) · PollenNav 87w **"The hard part was not accuracy, it was making invisible data feel tangible"** (20 interviews → "not another symptom tracker" → the V2 density bars that read as precise and still failed → legibility and feeling are the product) · Homewise 74w. **GOTCHA hit on both files: promoting a body sentence to the h2 REQUIRES trimming it from the body**, or the rename manufactures the heading/body redundancy the 2026-07-28 lesson warns about. **`ticker.html` still says "What I learned" and is deliberately left** (unlinked + noindex). **Coffee's removals thread WAS used here with explicit user approval** — note this does NOT reverse the 2026-07-28 decision, which moved the removal-*licensing* sentence ("anything that added friction had to go") out to Goals-and-constraints; generalizing the removals into a designer lesson is a different move, and the licensing sentence stays where it is. **Consequence: Coffee's Reflection h2 changed twice on 2026-08-04, so any note quoting "Design for the identity that is coming" as the heading is stale — that line is now the Design System section's territory only, and `coffeeslide.html` still carries BOTH the old Design System heading and a generic "What I learned" Reflection slide, so the deck is divergent from the page in two places.**
- Earlier (April 2026) we tried promoting Final Design to right after Research as a "show the win first" move. Reverted in May 2026 — the classic process-then-payoff arc reads stronger for these two case studies. **A "summary block after the hero" skim layer for NOVA + PollenNav (2026-07-26 improvement-research P8) was also REJECTED by the user ("feels repetitive" — the heroes already carry the intro beats). Do not re-suggest either variant; the case studies keep their single process-then-payoff narrative with no TL;DR layer.**

**TickerPulse:**
- Added `.deliverable` captions for all 5 wireframes (Sign in, Dashboard, Watchlist, Alert, Trade)
- Replaced Pain Points table with 3 voice-of-customer quotes (`.voc-quotes` component) — Trust, Personalization, Timeliness themes
- Rewrote Background, What I Did, Problem, Solution, Reflection — all tighter, more specific
- Built competitive audit table (Bloomberg, TradingView, StockTwits, TickerPulse) with Source Linking + Explains Why columns (content accuracy verified via web search)
- First Final Design heading uses h2 style; section label stays on top

**About:**
- Hero tagline: "I'm Chang Mou, a product designer / shaping AI products with taste."
- Background trimmed from 3 generic paragraphs to 2 sharper ones (no buzzwords, no award-bragging)
- Keeps "Good design starts with a good life" as closing personality moment
- Experience list top row updated 2026-05-11: was "Freelance · TickerPulse, HouSmart, CoPatible · 2026"; now "PM Accelerator · 2025 – Present" to match resume. Other rows (UNICEF / Litespace / Asset Layer / BC & Alberta Guide Dogs) are company-name only, so this row drops its project list to match that rhythm.
- Resume URL **updated 2026-07-23 to `13UgKrgvZFLd8lhiJlr9OQ_S4UGyCcK2I`** (`Chang_Mou_Product_Designer_Resume.pdf`; was `1dNdWQ1MNE_v-08U8VexrqYV2ZzS8lzFW` set 2026-06-21, and `1z-tqSdPhGxQ3G0n6LHwy1gbi5O1VscmQ` before that). Lives in nav + footer of all **8** HTML pages (16 links total; coffeeslide.html has no resume link). Update via `grep -rl <old-id> --include="*.html" . | xargs sed -i '' 's|<old>|<new>|g'`. **Verify the new Drive file is world-readable before shipping** — fetch `https://drive.google.com/uc?export=download&id=<ID>` anonymously and confirm it returns HTTP 200 with `%PDF` bytes (the `/view` page renders a "Sign in" button in its chrome even for public files, so scraping that page for "sign in" gives a false positive).

**Homepage + infra:**
- Chat teaser bubble ("Hey, I'm Chang's AI...") fires 4s after page load (after status tag animates), hides after 6s, once-per-browser via localStorage, hidden on mobile
- Footer glow palette boosted (coral 0.13→0.22, peach 0.08→0.18) for more warmth alongside dominant purple/violet
- Award category labels updated on PollenNav (iF Branding & Communication, EPDA Information & Data, NYPDA Silver Winner)

**Play page complete redesign:**
- From single-column vertical to 2-column flex masonry with card wrappers
- Each project in `.play-card` (bg, border, padding) matching home page project card language
- Click-to-zoom lightbox with Esc/backdrop to close
- CoPatible and Luma added as new concept projects (from Figma Make work)
- Descriptions tightened (Whisper XR, Luma, CoPatible from 4 lines to 2)
- Inner image radius 4px for concentric feel with 8px card radius
- Carousel arrows below image, vertically aligned with dots, symmetric 24px spacing to image and title

## User preferences noted

- No em dashes in descriptive copy (prefers periods/splitting sentences)
- **Card → case-study hero-sub continuity (added 2026-07-27, per user):** every case study's `.project-hero-sub` must OPEN with its home card description's first clause **verbatim**, then continue with something the card does NOT say. Shipped state: Homewise "AI command center for homeowners." → mechanism; PollenNav "Street level pollen guidance for safer city travel." → the city-averages contrast; Coffee Chat "AI powered coffee chat for intentional, pressure free pairings across hybrid teams." (opening only, nothing added); NOVA "AR navigation system for children that balances independence and parental reassurance." → the CCA/UNICEF context. **The user chose this over full duplication** (the alternative offered): the shared opening confirms the click, the divergent tail avoids making the reader re-read a sentence they finished one second ago. **Two consequences to preserve:** (a) they can never be byte-identical because card copy drops the terminal period (rule below) while hero subs are full sentences; (b) Coffee Chat's hero sub deliberately **drops the 63%/200+ stats** — the card carries them as the hook and the Impact grid restates them 200px down, so repeating them in the sub was pure duplication. Also note the site's house style leaves compounds unhyphenated ("AI powered", "Street level", "pressure free") — the coffee hero sub was un-hyphenated to match its card rather than hyphenating the curated card copy. If a card description is ever rewritten, its hero sub opening must be updated to match.
- Home work-card descriptions drop the TERMINAL period (caption convention, unified 2026-07-23 per user): internal periods where grammar needs them, but the description never ends with one (e.g. "…manages repairs from one workspace" not "…workspace."). All 8 cards audited to comply; keep new cards consistent.
- Do not brag about awards in copy; let the Recognition/Impact sections do that visually
- **About-page Background bio (`about.html`) rules, learned 2026-06-13 — apply to any bio rewrite:** (a) **No organization names in the bio** — orgs live in Experience/Education below; don't repeat them up top. (b) **Don't overclaim "agentic"** as the identity — only 2 of 7 projects are truly agentic (Homewise, Countersign); StoryBloom is generative, TickerPulse/Coffee Chat are AI-powered tools, PollenNav/NOVA aren't AI. Lead with broad "AI products" and keep the agentic angle at the *project* level (cards/case studies), not the bio headline. (c) **Figma and code (Claude Code) are separate systems** — never phrase as a Figma-to-code pipeline ("take the work all the way into code" is WRONG); Chang mocks up in Figma on some projects and builds directly in code on others (e.g. Homewise). Keep Figma positive (the role market wants Figma + Cursor, not anti-Figma).
- Keep "Play" as page name (not "Side") — matches the craft/experimentation signal the checker rewards
- Do not push to git unless explicitly asked
- Check sync to `/tmp/portfolio/` before screenshots (some resets happen)
- No category tags on play cards (denied this approach earlier)
- No accordion on play cards (tighten text instead of hiding it)
- Copy targets a YC/startup hiring audience — keep resonant buzzwords like "first-principles thinking." Don't suggest cutting them as "designer-cliché." Judge phrases by the intended reader, not by general good-writing principles.
- Avoid decorative numbering (`01 — SELECTED WORK`, `01/02/03/04` card chips) on sections that don't have a real numbered series — implies an ordering that doesn't exist. Section labels can use the editorial hairline-rule treatment (`.section-meta` with `.section-label-text` + `.section-rule`) without the number.
- Don't put a 3-column image row directly above a 3-card grid when the images and cards aren't 1:1 related — readers will infer a mapping that doesn't exist. Either label each image (figcaptions) and switch the cards to a different shape (e.g. `.problem-grid--stack` for full-width stacked cards), or use a different image count.
- **No reading-measure cap on About prose.** A `max-width: 62ch` on `.about-block p` + `.testimonial-quote` (UI-craft review P5, 2026-07-26) was applied and REVERTED same day per user — "too much white space". The user prefers full-container-width text on About over the editorial 45–75cpl measure; the typographic-ideal argument was heard and overruled. Do not re-suggest. (The other P5 parts stayed: `.hero-sub` line-height 1.75→1.6 and `.project-info p` 1.4→1.5 with its paired `min-height` bump.)
- No motion on About → Skills + Tools chip rows. Tried a Bubbl-inspired stagger drop-in + hover wobble on 2026-05-11, reverted. The section is scan-and-leave, so constant micro-motion competes with readability instead of adding signal. Keep chips static; if motion is wanted, put it on hero or play-page surfaces instead.
- No grain/noise texture over the dark gradients. Tried an SVG-turbulence film-grain overlay on the hero mesh + footer glow on 2026-06-11, removed same day — gray noise over the near-black background reads as glitch/static ("the color difference is too obvious"), not as craft texture. If texture is ever wanted, it would need soft-light blending scoped to the gradient glow only, but the default answer is no.
- No overlay furniture on work-card thumbnails. Hover arrow chips (↗/→ glass circles) and on-image status pills (glass "Shipped"/"Speculative Design" bottom-left) were prototyped 2026-06-11 and reverted same day — added chrome on the thumbnails reads as clutter. The accepted direction for card upgrades is **image/light physics** (contact shadows, lift, plate light scenes — all kept), not added elements. Award badges top-left are the one sanctioned overlay.
- Work cards link to the live experience (or case study), never to a GitHub repo — for a design-hiring audience the card's single click should land in the work itself. When the code is worth surfacing, put the repo URL in the chat prompt (`api/chat.js` PORTFOLIO PROJECTS list) instead. Decided 2026-06-12 when weighing Homewise's repo vs live link (chose live/case study over repo). Homewise's card now points at its case study `homewise.html` (which itself ends in a live-demo CTA), once the clips landed; Countersign's card → live demo; both repos surface only in the chat prompt + CLAUDE.md.
- Date format conventions for `.exp-date` in Experience / Recognition / Education on about.html: single year (`2026`) = one-time award or event; comma list (`2025, 2026`) = multiple discrete wins of the same award in different years (e.g., NY Product Design Awards Silver — PollenNav 2025 + StoryBloom 2026); en-dash range (`2024 – 2025`) = continuous tenure or one award covering an academic year (e.g., Bernard Osher Scholarship). Don't mix — using a range for two discrete wins implies a continuous award; using a comma list for a continuous period implies two separate awards.
